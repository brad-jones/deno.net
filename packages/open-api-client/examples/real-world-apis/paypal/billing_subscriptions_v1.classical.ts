// deno-lint-ignore-file no-explicit-any
// This file was auto-generated by @brad-jones/open-api-client

import {
  type OpenAPIClientConfig,
  openAPIFetch,
  type OpenAPIResponses,
} from "jsr:@brad-jones/deno-net-open-api-client@0.1.6";
import { z } from "npm:zod@^4.1.12";

export type _400 = unknown;

export type _401 = unknown;

export type _403 = unknown;

export type _404 = unknown;

export type _422 = unknown;

/**
 * The account identifier for a PayPal account.
 *
 * @format ppaas_payer_id_v3
 */
export type AccountId = string;

export type CardBrand =
  | "VISA"
  | "MASTERCARD"
  | "DISCOVER"
  | "AMEX"
  | "SOLO"
  | "JCB"
  | "STAR"
  | "DELTA"
  | "SWITCH"
  | "MAESTRO"
  | "CB_NATIONALE"
  | "CONFIGOGA"
  | "CONFIDIS"
  | "ELECTRON"
  | "CETELEM"
  | "CHINA_UNION_PAY"
  | "DINERS"
  | "ELO"
  | "HIPER"
  | "HIPERCARD"
  | "RUPAY"
  | "GE"
  | "SYNCHRONY"
  | "UNKNOWN";

/** Array of brands or networks associated with the card. */
export type CardBrandList = CardBrand[];

export type CardType = "CREDIT" | "DEBIT" | "PREPAID" | "STORE" | "UNKNOWN";

/** The API caller can opt in to verify the card through PayPal offered verification services (e.g. Smart Dollar Auth, 3DS). */
export type CardVerification = {
  method?: "SCA_ALWAYS" | "SCA_WHEN_REQUIRED" | "3D_SECURE" | "AVS_CVV";
};

/**
 * The [2-character ISO 3166-1 code](/api/rest/reference/country-codes/) that identifies the country or region.<blockquote><strong>Note:</strong> The country code for Great Britain is <code>GB</code> and not <code>UK</code> as used in the top-level domain names for that country. Use the `C2` country code for China worldwide for comparable uncontrolled price (CUP) method, bank card, and cross-border transactions.</blockquote>
 *
 * @format ppaas_common_country_code_v2
 */
export type CountryCode = string;

/**
 * The [two-character ISO 3166-1 code](/api/rest/reference/country-codes/) that identifies the country or region.<blockquote><strong>Note:</strong> The country code for Great Britain is <code>GB</code> and not <code>UK</code> as used in the top-level domain names for that country. Use the `C2` country code for China worldwide for comparable uncontrolled price (CUP) method, bank card, and cross-border transactions.</blockquote>
 *
 * @format ppaas_common_country_code_v2
 */
export type CountryCode2 = string;

/**
 * The [three-character ISO-4217 currency code](/api/rest/reference/currency-codes/) that identifies the currency.
 *
 * @format ppaas_common_currency_code_v2
 */
export type CurrencyCode = string;

/** The regular and trial execution details for a billing cycle. */
export type CycleExecution = {
  tenure_type: "REGULAR" | "TRIAL";
  /** The order in which to run this cycle among other billing cycles. */
  sequence: number;
  /** The number of billing cycles that have completed. */
  cycles_completed: number;
  /** For a finite billing cycle, cycles_remaining is the number of remaining cycles. For an infinite billing cycle, cycles_remaining is set as 0. */
  cycles_remaining?: number;
  /** The active pricing scheme version for the billing cycle. */
  current_pricing_scheme_version?: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
};

/** The trial and regular billing executions. */
export type CycleExecutionList = CycleExecution[];

/**
 * The date and time, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Seconds are required while fractional seconds are optional.<blockquote><strong>Note:</strong> The regular expression provides guidance but does not reject all invalid dates.</blockquote>
 *
 * @format ppaas_date_time_v3
 */
export type DateTime = string;

/** The year and month, in ISO-8601 `YYYY-MM` date format. See [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
export type DateYearMonth = string;

/** Representation of card details as received in the request. */
export type CardFromRequest = {
  expiry?: DateYearMonth;
  /** The last digits of the payment card. */
  last_digits?: string;
};

/**
 * The internationalized email address.<blockquote><strong>Note:</strong> Up to 64 characters are allowed before and 255 characters are allowed after the <code>@</code> sign. However, the generally accepted maximum length for an email address is 254 characters. The pattern verifies that an unquoted <code>@</code> sign exists.</blockquote>
 *
 * @format merchant_common_email_address_v2
 */
export type Email = string;

/**
 * The internationalized email address.<blockquote><strong>Note:</strong> Up to 64 characters are allowed before and 255 characters are allowed after the <code>@</code> sign. However, the generally accepted maximum length for an email address is 254 characters. The pattern verifies that an unquoted <code>@</code> sign exists.</blockquote>
 *
 * @format ppaas_common_email_address_v2
 */
export type EmailAddress = string;

export type Enrolled = "Y" | "N" | "U" | "B";

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export type ErrorLinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "PATCH";
};

/** This is either a system or application error, and generally indicates that although the client appeared to provide a correct request, something unexpected has gone wrong on the server. */
export type Error500 = {
  name?: "INTERNAL_SERVER_ERROR";
  message?: "An internal server error occurred.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server is temporarily unable to handle the request, for example, because of planned maintenance or downtime. */
export type Error503 = {
  name?: "SERVICE_UNAVAILABLE";
  message?: "Service Unavailable.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

export type ErrorLocation = "body" | "path" | "query";

/** The error details. Required for client-side `4XX` errors. */
export type ErrorDetails = {
  /** The field that caused the error. If this field is in the body, set this value to the field's JSON pointer value. Required for client-side errors. */
  field?: string;
  /** The value of the field that caused the error. */
  value?: string;
  location?: ErrorLocation;
  /** The unique, fine-grained application-level error code. */
  issue: string;
  /** The human-readable description for an issue. The description can change over the lifetime of an API, so clients must not depend on this value. */
  description?: string;
};

/** Request is not well-formed, syntactically incorrect, or violates schema. */
export type Error400 = {
  name?: "INVALID_REQUEST";
  message?: "Request is not well-formed, syntactically incorrect, or violates schema.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** Authentication failed due to missing Authorization header, or invalid authentication credentials. */
export type Error401 = {
  name?: "AUTHENTICATION_FAILURE";
  message?: "Authentication failed due to missing authorization header, or invalid authentication credentials.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The client is not authorized to access this resource, although it may have valid credentials.  */
export type Error403 = {
  name?: "NOT_AUTHORIZED";
  message?: "Authorization failed due to insufficient permissions.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has not found anything matching the request URI. This either means that the URI is incorrect or the resource is not available. */
export type Error404 = {
  name?: "RESOURCE_NOT_FOUND";
  message?: "The specified resource does not exist.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has detected a conflict while processing this request. */
export type Error409 = {
  name?: "RESOURCE_CONFLICT";
  message?: "The server has detected a conflict while processing this request.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server does not support the request payload's media type. */
export type Error415 = {
  name?: "UNSUPPORTED_MEDIA_TYPE";
  message?: "The server does not support the request payload's media type.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The requested action cannot be performed and may require interaction with APIs or processes outside of the current request. This is distinct from a 500 response in that there are no systemic problems limiting the API from performing the request. */
export type Error422 = {
  name?: "UNPROCESSABLE_ENTITY";
  message?: "The requested action could not be performed, semantically incorrect, or failed business validation.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

export type ErrorDefault =
  | Error400
  | Error401
  | Error403
  | Error404
  | Error409
  | Error415
  | Error422
  | Error500
  | Error503;

/** The frequency of the billing cycle. */
export type Frequency = {
  interval_unit: "DAY" | "WEEK" | "MONTH" | "YEAR";
  /** The number of intervals after which a subscriber is billed. For example, if the `interval_unit` is `DAY` with an `interval_count` of  `2`, the subscription is billed once every two days. The following table lists the maximum allowed values for the `interval_count` for each `interval_unit`:<table><thead><tr><th><code>Interval unit</code></th><th>Maximum interval count</th></tr></thead><tbody><tr><td><code>DAY</code></td><td align="right">365</td></tr><tr><td><code>WEEK</code></td><td align="right">52</td></tr><tr><td><code>MONTH</code></td><td align="right">12</td></tr><tr><td><code>YEAR</code></td><td align="right">1</td></tr></tbody></table> */
  interval_count?: number;
};

/**
 * The [language tag](https://tools.ietf.org/html/bcp47#section-2) for the language in which to localize the error-related strings, such as messages, issues, and suggested actions. The tag is made up of the [ISO 639-2 language code](https://www.loc.gov/standards/iso639-2/php/code_list.php), the optional [ISO-15924 script tag](https://www.unicode.org/iso15924/codelists.html), and the [ISO-3166 alpha-2 country code](/api/rest/reference/country-codes/) or [M49 region code](https://unstats.un.org/unsd/methodology/m49/).
 *
 * @format ppaas_common_language_v3
 */
export type Language = string;

export type LiabilityShift = "NO" | "POSSIBLE" | "UNKNOWN";

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export type LinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "HEAD" | "CONNECT" | "OPTIONS" | "PATCH";
};

/** An array of request-related HATEOAS links. */
export type DefinitionsLinkDescriptionList = LinkDescription[];

/** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links). */
export type LinkDescriptionList = LinkDescription[];

export type MerchantInventory = unknown;

/** The unique ID for a customer generated by PayPal. */
export type MerchantPartnerCustomerId = string;

/** The currency and amount for a financial transaction, such as a balance or payment due. */
export type Money = {
  currency_code: CurrencyCode;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/api/rest/reference/currency-codes/). */
  value: string;
};

/** The breakdown details for the amount. Includes the gross, tax, fee, and shipping amounts. */
export type AmountWithBreakdown = {
  gross_amount: Money;
  total_item_amount?: Money;
  fee_amount?: Money;
  shipping_amount?: Money;
  tax_amount?: Money;
  net_amount?: Money;
};

/** The details for the failed payment of the subscription. */
export type FailedPaymentDetails = {
  amount: Money;
  time: DateTime;
  reason_code?:
    | "PAYMENT_DENIED"
    | "INTERNAL_SERVER_ERROR"
    | "PAYEE_ACCOUNT_RESTRICTED"
    | "PAYER_ACCOUNT_RESTRICTED"
    | "PAYER_CANNOT_PAY"
    | "SENDING_LIMIT_EXCEEDED"
    | "TRANSACTION_RECEIVING_LIMIT_EXCEEDED"
    | "CURRENCY_MISMATCH";
  next_payment_retry_time?: DateTime;
};

export type LastPaymentDetails = unknown;

/** The name of the party. */
export type Name = {
  /** The prefix, or title, to the party's name. */
  prefix?: string;
  /** When the party is a person, the party's given, or first, name. */
  given_name?: string;
  /** When the party is a person, the party's surname or family name. Also known as the last name. Required when the party is a person. Use also to store multiple surnames including the matronymic, or mother's, surname. */
  surname?: string;
  /** When the party is a person, the party's middle name. Use also to store multiple middle names including the patronymic, or father's, middle name. */
  middle_name?: string;
  /** The suffix for the party's name. */
  suffix?: string;
  /** When the party is a person, the party's full name. */
  full_name?: string;
};

export type ParesStatus = "Y" | "N" | "U" | "A" | "C" | "R" | "D" | "I";

/** The JSON patch object to apply partial updates to resources. */
export type Patch = {
  op: "add" | "remove" | "replace" | "move" | "copy" | "test";
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location at which to complete the operation. */
  path?: string;
  value?: unknown;
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location from which to move the value. Required for the <code>move</code> operation. */
  from?: string;
};

/** An array of JSON patch objects to apply partial updates to resources. */
export type PatchRequest = Patch[];

export type PayeePaymentMethodPreference = "UNRESTRICTED" | "IMMEDIATE_PAYMENT_REQUIRED";

/** The application context, which customizes the payer experience during the subscription approval process with PayPal. */
export type ApplicationContext = {
  /** The label that overrides the business name in the PayPal account on the PayPal site. */
  brand_name?: string;
  locale?: Language;
  shipping_preference?: "GET_FROM_FILE" | "NO_SHIPPING" | "SET_PROVIDED_ADDRESS";
  user_action?: "CONTINUE" | "SUBSCRIBE_NOW";
  /** The customer and merchant payment preferences. */
  payment_method?: {
    payee_preferred?: PayeePaymentMethodPreference;
  };
  /**
   * The URL where the customer is redirected after the customer approves the payment.
   *
   * @format uri
   */
  return_url: string;
  /**
   * The URL where the customer is redirected after the customer cancels the payment.
   *
   * @format uri
   */
  cancel_url: string;
};

/** The customer who approves and pays for the order. The customer is also known as the payer. */
export type PayerBase = {
  email_address?: Email;
  payer_id?: AccountId;
};

/** The payment preferences for a subscription. */
export type PaymentPreferences = {
  /** Indicates whether to automatically bill the outstanding amount in the next billing cycle. */
  auto_bill_outstanding?: boolean;
  setup_fee?: Money;
  setup_fee_failure_action?: "CONTINUE" | "CANCEL";
  /** The maximum number of payment failures before a subscription is suspended. For example, if `payment_failure_threshold` is `2`, the subscription automatically updates to the `SUSPEND` state if two consecutive payments fail. */
  payment_failure_threshold?: number;
};

/** The payment preferences to override at subscription level. */
export type PaymentPreferencesOverride = {
  /** Indicates whether to automatically bill the outstanding amount in the next billing cycle. */
  auto_bill_outstanding?: boolean;
  setup_fee?: Money;
  setup_fee_failure_action?: "CONTINUE" | "CANCEL";
  /** The maximum number of payment failures before a subscription is suspended. For example, if `payment_failure_threshold` is `2`, the subscription automatically updates to the `SUSPEND` state if two consecutive payments fail. */
  payment_failure_threshold?: number;
};

/**
 * The percentage, as a fixed-point, signed decimal number. For example, define a 19.99% interest rate as `19.99`.
 *
 * @format ppaas_common_percentage_v2
 */
export type Percentage = string;

export type PhoneType = "FAX" | "HOME" | "MOBILE" | "OTHER" | "PAGER";

/** The phone information. */
export type PhoneWithType = {
  phone_type?: PhoneType;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number: {
    /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
    national_number: string;
  };
};

/** The details about a customer in PayPal's system of record. */
export type Customer = {
  id?: MerchantPartnerCustomerId;
  email_address?: Email;
  phone?: PhoneWithType;
};

export type PlansActivate422 = unknown;

export type PlansCreate400 = unknown;

export type PlansDeactivate422 = unknown;

export type PlansPatch400 = unknown;

export type PlansPatch422 = unknown;

export type PlansUpdatePricingSchemes400 = unknown;

export type PlansUpdatePricingSchemes422 = unknown;

/** The pricing tier details. */
export type PricingTier = {
  /** The starting quantity for the tier. */
  starting_quantity: string;
  /** The ending quantity for the tier. Optional for the last tier. */
  ending_quantity?: string;
  amount: Money;
};

/** An array of pricing tiers which are used for billing volume/tiered plans. pricing_model field has to be specified. */
export type PricingTierList = PricingTier[];

/** The pricing scheme details. */
export type PricingScheme = {
  /** The version of the pricing scheme. */
  version?: number;
  fixed_price?: Money;
  pricing_model?: "VOLUME" | "TIERED";
  tiers?: PricingTierList;
  create_time?: DateTime;
  update_time?: DateTime;
};

/** The billing cycle details. */
export type BillingCycle = {
  pricing_scheme?: PricingScheme;
  frequency: Frequency;
  tenure_type: "REGULAR" | "TRIAL";
  /** The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle. */
  sequence: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
};

/** An array of billing cycles for trial billing and regular billing. A plan can have at most two trial cycles and only one regular cycle. */
export type BillingCycleList = BillingCycle[];

/** The billing cycle details to override at subscription level. The subscription billing cycle definition has to adhere to the plan billing cycle definition. */
export type BillingCycleOverride = {
  pricing_scheme?: PricingScheme;
  /** The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle. */
  sequence: number;
  /** The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). */
  total_cycles?: number;
};

/** An array of billing cycles for trial billing and regular billing. The subscription billing cycle definition has to adhere to the plan billing cycle definition. */
export type BillingCycleOverrideList = BillingCycleOverride[];

/** This value provides the category of the BIN. */
export type Products = string;

/** The type of card product assigned to the BIN by the issuer. These values are defined by the issuer and may change over time. Some examples include: PREPAID_GIFT, CONSUMER, CORPORATE. */
export type ProductsList = Products[];

/** Bank Identification Number (BIN) details used to fund a payment. */
export type BinDetails = {
  /** The Bank Identification Number (BIN) signifies the number that is being used to identify the granular level details (except the PII information) of the card. */
  bin?: string;
  /** The issuer of the card instrument. */
  issuing_bank?: string;
  bin_country_code?: CountryCode2;
  products?: ProductsList;
};

export type ShippingType = "SHIPPING" | "PICKUP" | "PICKUP_IN_STORE" | "PICKUP_FROM_PERSON";

/** The options that the payee or merchant offers to the payer to ship or pick up their items. */
export type ShippingOption = {
  /** A unique ID that identifies a payer-selected shipping option. */
  id: string;
  /** A description that the payer sees, which helps them choose an appropriate shipping option. For example, `Free Shipping`, `USPS Priority Shipping`, `Expédition prioritaire USPS`, or `USPS yōuxiān fā huò`. Localize this description to the payer's locale. */
  label: string;
  type?: ShippingType;
  amount?: Money;
  /** If the API request sets `selected = true`, it represents the shipping option that the payee or merchant expects to be pre-selected for the payer when they first view the `shipping.options` in the PayPal Checkout experience. As part of the response if a `shipping.option` contains `selected=true`, it represents the shipping option that the payer selected during the course of checkout with PayPal. Only one `shipping.option` can be set to `selected=true`. */
  selected: boolean;
};

/** An array of shipping options that the payee or merchant offers to the payer to ship or pick up their items. */
export type ShippingOptionList = ShippingOption[];

/** The shipping details. */
export type ShippingDetail = {
  /** The name of the party. */
  name?: {
    /** When the party is a person, the party's full name. */
    full_name?: string;
  };
  type?: "SHIPPING" | "PICKUP_IN_PERSON" | "PICKUP_IN_STORE" | "PICKUP_FROM_PERSON";
  options?: ShippingOptionList;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  address?: {
    /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
    address_line_1?: string;
    /** The second line of the address, for example, a suite or apartment number. */
    address_line_2?: string;
    /** A city, town, or village. Smaller than `admin_area_level_1`. */
    admin_area_2?: string;
    /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
    admin_area_1?: string;
    /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
    postal_code?: string;
    country_code: CountryCode;
  };
};

export type StoreInVaultInstruction = "ON_SUCCESS";

/** The activate subscription request details. */
export type SubscriptionActivateRequest = {
  /** The reason for activation of a subscription. Required to reactivate the subscription. */
  reason?: string;
};

/** The billing details for the subscription. If the subscription was or is active, these fields are populated. */
export type SubscriptionBillingInfo = {
  outstanding_balance: Money;
  cycle_executions?: CycleExecutionList;
  last_payment?: LastPaymentDetails;
  next_billing_time?: DateTime;
  final_payment_time?: DateTime;
  /** The number of consecutive payment failures. Resets to `0` after a successful payment. If this reaches the `payment_failure_threshold` value, the subscription updates to the `SUSPENDED` state. */
  failed_payments_count: number;
  last_failed_payment?: FailedPaymentDetails;
};

/** The cancel subscription request details. */
export type SubscriptionCancelRequest = {
  /** The reason for the cancellation of a subscription. */
  reason: string;
};

/** The charge amount from the subscriber. */
export type SubscriptionCaptureRequest = {
  /** The reason or note for the subscription charge. */
  note: string;
  capture_type: "OUTSTANDING_BALANCE";
  amount: Money;
};

/** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links). */
export type SubscriptionReviseResponseDefinitionsLinkDescriptionList = LinkDescription[];

/** The subscription status details. */
export type SubscriptionStatus = {
  status?: "APPROVAL_PENDING" | "APPROVED" | "ACTIVE" | "SUSPENDED" | "CANCELLED" | "EXPIRED";
  /** The reason or notes for the status of the subscription. */
  status_change_note?: string;
  status_update_time?: DateTime;
};

/** The suspend subscription request details. */
export type SubscriptionSuspendRequest = {
  /** The reason for suspension of the Subscription. */
  reason: string;
};

export type SubscriptionsActivate400 = unknown;

export type SubscriptionsActivate422 = unknown;

export type SubscriptionsCancel400 = unknown;

export type SubscriptionsCancel422 = unknown;

export type SubscriptionsCapture400 = unknown;

export type SubscriptionsCapture422 = unknown;

export type SubscriptionsCreate400 = unknown;

export type SubscriptionsCreate422 = unknown;

export type SubscriptionsPatch400 = unknown;

export type SubscriptionsPatch422 = unknown;

export type SubscriptionsRevise400 = unknown;

export type SubscriptionsRevise404 = unknown;

export type SubscriptionsRevise422 = unknown;

export type SubscriptionsSuspend400 = unknown;

export type SubscriptionsSuspend422 = unknown;

export type SubscriptionsTransactions400 = unknown;

/** The tax details. */
export type Taxes = {
  percentage: Percentage;
  /** Indicates whether the tax was already included in the billing amount. */
  inclusive?: boolean;
};

/** The plan details. */
export type Plan = {
  /** The unique PayPal-generated ID for the plan. */
  id?: string;
  /** The ID for the product. */
  product_id?: string;
  /** The plan name. */
  name?: string;
  status?: "CREATED" | "INACTIVE" | "ACTIVE";
  /** The detailed description of the plan. */
  description?: string;
  billing_cycles?: BillingCycleList;
  payment_preferences?: PaymentPreferences;
  taxes?: Taxes;
  /** Indicates whether you can subscribe to this plan by providing a quantity for the goods or service. */
  quantity_supported?: boolean;
  create_time?: DateTime;
  update_time?: DateTime;
  links?: LinkDescriptionList;
};

/** An array of plans. */
export type PlanList = Plan[];

/** The list of plans with details. */
export type PlanCollection = {
  plans?: PlanList;
  /** The total number of items. */
  total_items?: number;
  /** The total number of pages. */
  total_pages?: number;
  links?: LinkDescriptionList;
};

/** The create plan request details. */
export type PlanRequestPost = {
  /** The ID of the product created through Catalog Products API. */
  product_id: string;
  /** The plan name. */
  name: string;
  status?: "CREATED" | "INACTIVE" | "ACTIVE";
  /** The detailed description of the plan. */
  description?: string;
  billing_cycles: BillingCycleList;
  payment_preferences: PaymentPreferences;
  taxes?: Taxes;
  /** Indicates whether you can subscribe to this plan by providing a quantity for the goods or service. */
  quantity_supported?: boolean;
};

/** The tax details. */
export type TaxesOverride = {
  percentage?: Percentage;
  /** Indicates whether the tax was already included in the billing amount. */
  inclusive?: boolean;
};

/** An inline plan object to customise the subscription. You can override plan level default attributes by providing customised values for the subscription in this object. */
export type PlanOverride = {
  billing_cycles?: BillingCycleOverrideList;
  payment_preferences?: PaymentPreferencesOverride;
  taxes?: TaxesOverride;
};

/** The request to update the quantity of the product or service in a subscription. You can also use this method to switch the plan and update the `shipping_amount` and `shipping_address` values for the subscription. This type of update requires the buyer's consent. */
export type SubscriptionReviseRequest = {
  /** The unique PayPal-generated ID for the plan. */
  plan_id?: string;
  /** The quantity of the product or service in the subscription. */
  quantity?: string;
  shipping_amount?: Money;
  shipping_address?: ShippingDetail;
  /** The application context, which customizes the payer experience during the subscription approval process with PayPal. */
  application_context?: {
    /** The label that overrides the business name in the PayPal account on the PayPal site. */
    brand_name?: string;
    locale?: Language;
    shipping_preference?: "GET_FROM_FILE" | "NO_SHIPPING" | "SET_PROVIDED_ADDRESS";
    /** The customer and merchant payment preferences. */
    payment_method?: {
      payee_preferred?: PayeePaymentMethodPreference;
    };
    /**
     * The URL where the customer is redirected after the customer approves the payment.
     *
     * @format uri
     */
    return_url: string;
    /**
     * The URL where the customer is redirected after the customer cancels the payment.
     *
     * @format uri
     */
    cancel_url: string;
  };
  plan?: PlanOverride;
};

/** The response to a request to update the quantity of the product or service in a subscription. You can also use this method to switch the plan and update the `shipping_amount` and `shipping_address` values for the subscription. This type of update requires the buyer's consent. */
export type SubscriptionReviseResponse = {
  /** The unique PayPal-generated ID for the plan. */
  plan_id?: string;
  /** The quantity of the product or service in the subscription. */
  quantity?: string;
  shipping_amount?: Money;
  shipping_address?: ShippingDetail;
  plan?: PlanOverride;
  /** Indicates whether the subscription has overridden any plan attributes. */
  plan_overridden?: boolean;
  links?: SubscriptionReviseResponseDefinitionsLinkDescriptionList;
};

/** Results of 3D Secure Authentication. */
export type ThreeDSecureAuthenticationResponse = {
  authentication_status?: ParesStatus;
  enrollment_status?: Enrolled;
};

/** Results of Authentication such as 3D Secure. */
export type AuthenticationResponse = {
  liability_shift?: LiabilityShift;
  three_d_secure?: ThreeDSecureAuthenticationResponse;
};

/** The transaction details. */
export type Transaction = {
  status?: "COMPLETED" | "DECLINED" | "PARTIALLY_REFUNDED" | "PENDING" | "REFUNDED" | "FAILED";
  /** The PayPal-generated transaction ID. */
  id: string;
  amount_with_breakdown: AmountWithBreakdown;
  payer_name?: Name;
  payer_email?: EmailAddress;
  time: DateTime;
};

/** An array of transactions. */
export type TransactionList = Transaction[];

/** The list transactions for a subscription request details. */
export type TransactionsList = {
  transactions?: TransactionList;
  /** The total number of items. */
  total_items?: number;
  /** The total number of pages. */
  total_pages?: number;
  links?: LinkDescriptionList;
};

/** The update pricing scheme request details. */
export type UpdatePricingSchemeRequest = {
  /** The billing cycle sequence. */
  billing_cycle_sequence: number;
  pricing_scheme: PricingScheme;
};

/** An array of pricing schemes. */
export type UpdatePricingSchemeRequestList = UpdatePricingSchemeRequest[];

/** The update pricing scheme request details. */
export type UpdatePricingSchemesListRequest = {
  pricing_schemes: UpdatePricingSchemeRequestList;
};

/** Basic vault instruction specification that can be extended by specific payment sources that supports vaulting. */
export type VaultInstructionBase = {
  store_in_vault?: StoreInVaultInstruction;
};

/** Additional attributes associated with the use of this card. */
export type CardAttributes = {
  customer?: Customer;
  vault?: VaultInstructionBase;
  verification?: CardVerification;
};

/** The payment source definition. To be eligible to create subscription using debit or credit card, you will need to sign up here (https://www.paypal.com/bizsignup/entry/product/ppcp). Please note, its available only for non-3DS cards and for merchants in US and AU regions. */
export type PaymentSource = {
  /** The payment card to use to fund a payment. Can be a credit or debit card. */
  card?: {
    /** The card holder's name as it appears on the card. */
    name?: string;
    /** The primary account number (PAN) for the payment card. */
    number?: string;
    expiry?: DateYearMonth;
    /** The three- or four-digit security code of the card. Also known as the CVV, CVC, CVN, CVE, or CID. This parameter cannot be present in the request when `payment_initiator=MERCHANT`. */
    security_code?: string;
    type?: CardType;
    brand?: CardBrand;
    /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
    billing_address?: {
      /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
      address_line_1?: string;
      /** The second line of the address, for example, a suite or apartment number. */
      address_line_2?: string;
      /** A city, town, or village. Smaller than `admin_area_level_1`. */
      admin_area_2?: string;
      /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
      admin_area_1?: string;
      /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
      postal_code?: string;
      country_code: CountryCode;
    };
    attributes?: CardAttributes;
  };
};

export type SubscriberRequest =
  & PayerBase
  & unknown
  & /**
   * The customer who approves and pays for the order. The customer is also known as the payer.
   *
   * @format payer_v1
   */ Record<string, never>
  & unknown
  & /** The subscriber request information . */ Record<string, never>;

/** The create subscription request details. */
export type SubscriptionRequestPost = {
  /** The ID of the plan. */
  plan_id: string;
  start_time?: DateTime;
  /** The quantity of the product in the subscription. */
  quantity?: string;
  shipping_amount?: Money;
  subscriber?: SubscriberRequest;
  /** DEPRECATED. Indicates whether the subscription auto-renews after the billing cycles complete. */
  auto_renewal?: boolean;
  application_context?: ApplicationContext;
  /** The custom id for the subscription. Can be invoice id. */
  custom_id?: string;
  plan?: PlanOverride;
  merchant_inventory?: MerchantInventory;
};

/** The details about a saved payment source. */
export type VaultResponse = {
  /** The PayPal-generated ID for the saved payment source. */
  id?: string;
  status?: "VAULTED" | "CREATED" | "APPROVED";
  /** The details about a customer in PayPal's system of record. */
  customer?: {
    id?: MerchantPartnerCustomerId;
  };
  links?: DefinitionsLinkDescriptionList;
};

/** Additional attributes associated with the use of this card. */
export type CardAttributesResponse = {
  vault?: VaultResponse;
};

/** The payment card to use to fund a payment. Card can be a credit or debit card. */
export type CardResponse = {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The last digits of the payment card. */
  last_digits?: string;
  brand?: CardBrand;
  available_networks?: CardBrandList;
  type?: CardType;
  authentication_result?: AuthenticationResponse;
  attributes?: CardAttributesResponse;
  from_request?: CardFromRequest;
  expiry?: DateYearMonth;
  bin_details?: BinDetails;
};

export type CardResponseWithBillingAddress =
  & CardResponse
  & unknown
  & /** The payment card used to fund the payment. Card can be a credit or debit card. */ Record<string, never>;

/** The payment source used to fund the payment. */
export type PaymentSourceResponse = {
  card?: CardResponseWithBillingAddress;
};

export type Subscriber =
  & PayerBase
  & unknown
  & /**
   * The customer who approves and pays for the order. The customer is also known as the payer.
   *
   * @format payer_v1
   */ Record<string, never>
  & unknown
  & /** The subscriber response information. */ Record<string, never>;

export type Subscription =
  & SubscriptionStatus
  & unknown
  & unknown
  & /** The subscription details. */ Record<string, never>;

export const _400Schema: z.ZodType<_400> = z.unknown();

export const _401Schema: z.ZodType<_401> = z.unknown();

export const _403Schema: z.ZodType<_403> = z.unknown();

export const _404Schema: z.ZodType<_404> = z.unknown();

export const _422Schema: z.ZodType<_422> = z.unknown();

export const AccountIdSchema: z.ZodType<AccountId> = z.string().regex(new RegExp("^[2-9A-HJ-NP-Z]{13}$")).min(13).max(
  13,
);

export const CardBrandSchema: z.ZodType<CardBrand> = z.enum([
  "VISA",
  "MASTERCARD",
  "DISCOVER",
  "AMEX",
  "SOLO",
  "JCB",
  "STAR",
  "DELTA",
  "SWITCH",
  "MAESTRO",
  "CB_NATIONALE",
  "CONFIGOGA",
  "CONFIDIS",
  "ELECTRON",
  "CETELEM",
  "CHINA_UNION_PAY",
  "DINERS",
  "ELO",
  "HIPER",
  "HIPERCARD",
  "RUPAY",
  "GE",
  "SYNCHRONY",
  "UNKNOWN",
]);

export const CardBrandListSchema: z.ZodType<CardBrandList> = z.array(CardBrandSchema).min(1).max(256);

export const CardTypeSchema: z.ZodType<CardType> = z.enum(["CREDIT", "DEBIT", "PREPAID", "STORE", "UNKNOWN"]);

export const CardVerificationSchema: z.ZodType<CardVerification> = z.object({
  method: z.enum(["SCA_ALWAYS", "SCA_WHEN_REQUIRED", "3D_SECURE", "AVS_CVV"]).optional(),
});

export const CountryCodeSchema: z.ZodType<CountryCode> = z.string().regex(new RegExp("^([A-Z]{2}|C2)$")).min(2).max(2);

export const CountryCode2Schema: z.ZodType<CountryCode2> = z.string().regex(new RegExp("^([A-Z]{2}|C2)$")).min(2).max(
  2,
);

export const CurrencyCodeSchema: z.ZodType<CurrencyCode> = z.string().min(3).max(3);

export const CycleExecutionSchema: z.ZodType<CycleExecution> = z.object({
  tenure_type: z.enum(["REGULAR", "TRIAL"]),
  sequence: z.number().int().min(0).max(99),
  cycles_completed: z.number().int().min(0).max(9999),
  cycles_remaining: z.number().int().min(0).max(9999).optional(),
  current_pricing_scheme_version: z.number().int().min(1).max(99).optional(),
  total_cycles: z.number().int().min(0).max(999).optional(),
});

export const CycleExecutionListSchema: z.ZodType<CycleExecutionList> = z.array(CycleExecutionSchema).min(0).max(3);

export const DateTimeSchema: z.ZodType<DateTime> = z.string().regex(
  new RegExp(
    "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])[T,t]([0-1][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)([.][0-9]+)?([Zz]|[+-][0-9]{2}:[0-9]{2})$",
  ),
).min(20).max(64);

export const DateYearMonthSchema: z.ZodType<DateYearMonth> = z.string().regex(new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])$"))
  .min(7).max(7);

export const CardFromRequestSchema: z.ZodType<CardFromRequest> = z.object({
  expiry: DateYearMonthSchema.optional(),
  last_digits: z.string().regex(new RegExp("[0-9]{2,}")).min(2).max(4).optional(),
});

export const EmailSchema: z.ZodType<Email> = z.string().regex(
  new RegExp(
    "(?:[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+)*|(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21\x23-\x5b\x5d-\x7f]|\[\x01-\x09\x0b\x0c\x0e-\x7f])*\")@(?:(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]*[a-zA-Z0-9])?\.)+[a-zA-Z0-9](?:[a-zA-Z0-9-]*[a-zA-Z0-9])?|\[(?:(?:(2(5[0-5]|[0-4][0-9])|1[0-9][0-9]|[1-9]?[0-9]))\.){3}(?:(2(5[0-5]|[0-4][0-9])|1[0-9][0-9]|[1-9]?[0-9])|[a-zA-Z0-9-]*[a-zA-Z0-9]:(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21-\x5a\x53-\x7f]|\[\x01-\x09\x0b\x0c\x0e-\x7f])+)\])",
  ),
).min(3).max(254);

export const EmailAddressSchema: z.ZodType<EmailAddress> = z.string().regex(
  new RegExp(
    "^(?:[A-Za-z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[A-Za-z0-9!#$%&'*+/=?^_`{|}~-]+)*|\"(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21\x23-\x5b\x5d-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])*\")@(?:(?:[A-Za-z0-9](?:[A-Za-z0-9-]*[A-Za-z0-9])?\.)+[A-Za-z0-9](?:[A-Za-z0-9-]*[A-Za-z0-9])?|\[(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?|[A-Za-z0-9-]*[A-Za-z0-9]:(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21-\x5a\x53-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])+)\])$",
  ),
).min(3).max(254);

export const EnrolledSchema: z.ZodType<Enrolled> = z.enum(["Y", "N", "U", "B"]);

export const ErrorLinkDescriptionSchema: z.ZodType<ErrorLinkDescription> = z.object({
  href: z.string().regex(new RegExp("^.*$")).min(0).max(20000),
  rel: z.string().regex(new RegExp("^.*$")).min(0).max(100),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "PATCH"]).optional(),
});

export const Error500Schema: z.ZodType<Error500> = z.object({
  name: z.enum(["INTERNAL_SERVER_ERROR"]).optional(),
  message: z.enum(["An internal server error occurred."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error503Schema: z.ZodType<Error503> = z.object({
  name: z.enum(["SERVICE_UNAVAILABLE"]).optional(),
  message: z.enum(["Service Unavailable."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorLocationSchema: z.ZodType<ErrorLocation> = z.enum(["body", "path", "query"]);

export const ErrorDetailsSchema: z.ZodType<ErrorDetails> = z.object({
  field: z.string().optional(),
  value: z.string().optional(),
  location: ErrorLocationSchema.optional(),
  issue: z.string(),
  description: z.string().optional(),
});

export const Error400Schema: z.ZodType<Error400> = z.object({
  name: z.enum(["INVALID_REQUEST"]).optional(),
  message: z.enum(["Request is not well-formed, syntactically incorrect, or violates schema."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error401Schema: z.ZodType<Error401> = z.object({
  name: z.enum(["AUTHENTICATION_FAILURE"]).optional(),
  message: z.enum(["Authentication failed due to missing authorization header, or invalid authentication credentials."])
    .optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error403Schema: z.ZodType<Error403> = z.object({
  name: z.enum(["NOT_AUTHORIZED"]).optional(),
  message: z.enum(["Authorization failed due to insufficient permissions."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error404Schema: z.ZodType<Error404> = z.object({
  name: z.enum(["RESOURCE_NOT_FOUND"]).optional(),
  message: z.enum(["The specified resource does not exist."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error409Schema: z.ZodType<Error409> = z.object({
  name: z.enum(["RESOURCE_CONFLICT"]).optional(),
  message: z.enum(["The server has detected a conflict while processing this request."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error415Schema: z.ZodType<Error415> = z.object({
  name: z.enum(["UNSUPPORTED_MEDIA_TYPE"]).optional(),
  message: z.enum(["The server does not support the request payload's media type."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error422Schema: z.ZodType<Error422> = z.object({
  name: z.enum(["UNPROCESSABLE_ENTITY"]).optional(),
  message: z.enum([
    "The requested action could not be performed, semantically incorrect, or failed business validation.",
  ]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorDefaultSchema: z.ZodType<ErrorDefault> = z.union([
  Error400Schema,
  Error401Schema,
  Error403Schema,
  Error404Schema,
  Error409Schema,
  Error415Schema,
  Error422Schema,
  Error500Schema,
  Error503Schema,
]);

export const FrequencySchema: z.ZodType<Frequency> = z.object({
  interval_unit: z.enum(["DAY", "WEEK", "MONTH", "YEAR"]),
  interval_count: z.number().int().min(1).max(365).default(1).optional(),
});

export const LanguageSchema: z.ZodType<Language> = z.string().regex(
  new RegExp("^[a-z]{2}(?:-[A-Z][a-z]{3})?(?:-(?:[A-Z]{2}|[0-9]{3}))?$"),
).min(2).max(10);

export const LiabilityShiftSchema: z.ZodType<LiabilityShift> = z.enum(["NO", "POSSIBLE", "UNKNOWN"]);

export const LinkDescriptionSchema: z.ZodType<LinkDescription> = z.object({
  href: z.string(),
  rel: z.string(),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "HEAD", "CONNECT", "OPTIONS", "PATCH"]).optional(),
});

export const DefinitionsLinkDescriptionListSchema: z.ZodType<DefinitionsLinkDescriptionList> = z.array(
  LinkDescriptionSchema,
).min(1).max(10);

export const LinkDescriptionListSchema: z.ZodType<LinkDescriptionList> = z.array(LinkDescriptionSchema).min(1).max(10);

export const MerchantInventorySchema: z.ZodType<MerchantInventory> = z.unknown();

export const MerchantPartnerCustomerIdSchema: z.ZodType<MerchantPartnerCustomerId> = z.string().regex(
  new RegExp("^[0-9a-zA-Z_-]+$"),
).min(1).max(22);

export const MoneySchema: z.ZodType<Money> = z.object({
  currency_code: CurrencyCodeSchema,
  value: z.string().regex(new RegExp("^((-?[0-9]+)|(-?([0-9]+)?[.][0-9]+))$")).max(32),
});

export const AmountWithBreakdownSchema: z.ZodType<AmountWithBreakdown> = z.object({
  gross_amount: MoneySchema,
  total_item_amount: MoneySchema.optional(),
  fee_amount: MoneySchema.optional(),
  shipping_amount: MoneySchema.optional(),
  tax_amount: MoneySchema.optional(),
  net_amount: MoneySchema.optional(),
});

export const FailedPaymentDetailsSchema: z.ZodType<FailedPaymentDetails> = z.object({
  amount: MoneySchema,
  time: DateTimeSchema,
  reason_code: z.enum([
    "PAYMENT_DENIED",
    "INTERNAL_SERVER_ERROR",
    "PAYEE_ACCOUNT_RESTRICTED",
    "PAYER_ACCOUNT_RESTRICTED",
    "PAYER_CANNOT_PAY",
    "SENDING_LIMIT_EXCEEDED",
    "TRANSACTION_RECEIVING_LIMIT_EXCEEDED",
    "CURRENCY_MISMATCH",
  ]).optional(),
  next_payment_retry_time: DateTimeSchema.optional(),
});

export const LastPaymentDetailsSchema: z.ZodType<LastPaymentDetails> = z.unknown();

export const NameSchema: z.ZodType<Name> = z.object({
  prefix: z.string().max(140).optional(),
  given_name: z.string().max(140).optional(),
  surname: z.string().max(140).optional(),
  middle_name: z.string().max(140).optional(),
  suffix: z.string().max(140).optional(),
  full_name: z.string().max(300).optional(),
});

export const ParesStatusSchema: z.ZodType<ParesStatus> = z.enum(["Y", "N", "U", "A", "C", "R", "D", "I"]);

export const PatchSchema: z.ZodType<Patch> = z.object({
  op: z.enum(["add", "remove", "replace", "move", "copy", "test"]),
  path: z.string().optional(),
  value: z.unknown().optional(),
  from: z.string().optional(),
});

export const PatchRequestSchema: z.ZodType<PatchRequest> = z.array(PatchSchema);

export const PayeePaymentMethodPreferenceSchema: z.ZodType<PayeePaymentMethodPreference> = z.enum([
  "UNRESTRICTED",
  "IMMEDIATE_PAYMENT_REQUIRED",
]);

export const ApplicationContextSchema: z.ZodType<ApplicationContext> = z.object({
  brand_name: z.string().regex(new RegExp("^.*$")).min(1).max(127).optional(),
  locale: LanguageSchema.optional(),
  shipping_preference: z.enum(["GET_FROM_FILE", "NO_SHIPPING", "SET_PROVIDED_ADDRESS"]).optional(),
  user_action: z.enum(["CONTINUE", "SUBSCRIBE_NOW"]).optional(),
  payment_method: z.object({
    payee_preferred: PayeePaymentMethodPreferenceSchema.optional(),
  }).optional(),
  return_url: z.string().min(10).max(4000),
  cancel_url: z.string().min(10).max(4000),
});

export const PayerBaseSchema: z.ZodType<PayerBase> = z.object({
  email_address: EmailSchema.optional(),
  payer_id: AccountIdSchema.optional(),
});

export const PaymentPreferencesSchema: z.ZodType<PaymentPreferences> = z.object({
  auto_bill_outstanding: z.boolean().optional(),
  setup_fee: MoneySchema.optional(),
  setup_fee_failure_action: z.enum(["CONTINUE", "CANCEL"]).optional(),
  payment_failure_threshold: z.number().int().min(0).max(999).default(0).optional(),
});

export const PaymentPreferencesOverrideSchema: z.ZodType<PaymentPreferencesOverride> = z.object({
  auto_bill_outstanding: z.boolean().optional(),
  setup_fee: MoneySchema.optional(),
  setup_fee_failure_action: z.enum(["CONTINUE", "CANCEL"]).optional(),
  payment_failure_threshold: z.number().int().min(0).max(999).optional(),
});

export const PercentageSchema: z.ZodType<Percentage> = z.string().regex(
  new RegExp("^((-?[0-9]+)|(-?([0-9]+)?[.][0-9]+))$"),
);

export const PhoneTypeSchema: z.ZodType<PhoneType> = z.enum(["FAX", "HOME", "MOBILE", "OTHER", "PAGER"]);

export const PhoneWithTypeSchema: z.ZodType<PhoneWithType> = z.object({
  phone_type: PhoneTypeSchema.optional(),
  phone_number: z.object({
    national_number: z.string().regex(new RegExp("^[0-9]{1,14}?$")).min(1).max(14),
  }),
});

export const CustomerSchema: z.ZodType<Customer> = z.object({
  id: MerchantPartnerCustomerIdSchema.optional(),
  email_address: EmailSchema.optional(),
  phone: PhoneWithTypeSchema.optional(),
});

export const PlansActivate422Schema: z.ZodType<PlansActivate422> = z.unknown();

export const PlansCreate400Schema: z.ZodType<PlansCreate400> = z.unknown();

export const PlansDeactivate422Schema: z.ZodType<PlansDeactivate422> = z.unknown();

export const PlansPatch400Schema: z.ZodType<PlansPatch400> = z.unknown();

export const PlansPatch422Schema: z.ZodType<PlansPatch422> = z.unknown();

export const PlansUpdatePricingSchemes400Schema: z.ZodType<PlansUpdatePricingSchemes400> = z.unknown();

export const PlansUpdatePricingSchemes422Schema: z.ZodType<PlansUpdatePricingSchemes422> = z.unknown();

export const PricingTierSchema: z.ZodType<PricingTier> = z.object({
  starting_quantity: z.string().regex(new RegExp("^([0-9]+|([0-9]+)?[.][0-9]+)$")).min(1).max(32),
  ending_quantity: z.string().regex(new RegExp("^([0-9]+|([0-9]+)?[.][0-9]+)$")).min(1).max(32).optional(),
  amount: MoneySchema,
});

export const PricingTierListSchema: z.ZodType<PricingTierList> = z.array(PricingTierSchema).min(1).max(32);

export const PricingSchemeSchema: z.ZodType<PricingScheme> = z.object({
  version: z.number().int().min(0).max(999).optional(),
  fixed_price: MoneySchema.optional(),
  pricing_model: z.enum(["VOLUME", "TIERED"]).optional(),
  tiers: PricingTierListSchema.optional(),
  create_time: DateTimeSchema.optional(),
  update_time: DateTimeSchema.optional(),
});

export const BillingCycleSchema: z.ZodType<BillingCycle> = z.object({
  pricing_scheme: PricingSchemeSchema.optional(),
  frequency: FrequencySchema,
  tenure_type: z.enum(["REGULAR", "TRIAL"]),
  sequence: z.number().int().min(1).max(99),
  total_cycles: z.number().int().min(0).max(999).default(1).optional(),
});

export const BillingCycleListSchema: z.ZodType<BillingCycleList> = z.array(BillingCycleSchema).min(1).max(12);

export const BillingCycleOverrideSchema: z.ZodType<BillingCycleOverride> = z.object({
  pricing_scheme: PricingSchemeSchema.optional(),
  sequence: z.number().int().min(1).max(99),
  total_cycles: z.number().int().min(0).max(999).optional(),
});

export const BillingCycleOverrideListSchema: z.ZodType<BillingCycleOverrideList> = z.array(BillingCycleOverrideSchema)
  .min(1).max(12);

export const ProductsSchema: z.ZodType<Products> = z.string().min(1).max(255);

export const ProductsListSchema: z.ZodType<ProductsList> = z.array(ProductsSchema).min(1).max(256);

export const BinDetailsSchema: z.ZodType<BinDetails> = z.object({
  bin: z.string().regex(new RegExp("^[0-9]+$")).min(1).max(25).optional(),
  issuing_bank: z.string().min(1).max(64).optional(),
  bin_country_code: CountryCode2Schema.optional(),
  products: ProductsListSchema.optional(),
});

export const ShippingTypeSchema: z.ZodType<ShippingType> = z.enum([
  "SHIPPING",
  "PICKUP",
  "PICKUP_IN_STORE",
  "PICKUP_FROM_PERSON",
]);

export const ShippingOptionSchema: z.ZodType<ShippingOption> = z.object({
  id: z.string().max(127),
  label: z.string().max(127),
  type: ShippingTypeSchema.optional(),
  amount: MoneySchema.optional(),
  selected: z.boolean(),
});

export const ShippingOptionListSchema: z.ZodType<ShippingOptionList> = z.array(ShippingOptionSchema).min(0).max(10);

export const ShippingDetailSchema: z.ZodType<ShippingDetail> = z.object({
  name: z.object({
    full_name: z.string().max(300).optional(),
  }).optional(),
  type: z.enum(["SHIPPING", "PICKUP_IN_PERSON", "PICKUP_IN_STORE", "PICKUP_FROM_PERSON"]).optional(),
  options: ShippingOptionListSchema.optional(),
  address: z.object({
    address_line_1: z.string().max(300).optional(),
    address_line_2: z.string().max(300).optional(),
    admin_area_2: z.string().max(120).optional(),
    admin_area_1: z.string().max(300).optional(),
    postal_code: z.string().max(60).optional(),
    country_code: CountryCodeSchema,
  }).optional(),
});

export const StoreInVaultInstructionSchema: z.ZodType<StoreInVaultInstruction> = z.enum(["ON_SUCCESS"]);

export const SubscriptionActivateRequestSchema: z.ZodType<SubscriptionActivateRequest> = z.object({
  reason: z.string().regex(new RegExp("^.*$")).min(1).max(128).optional(),
});

export const SubscriptionBillingInfoSchema: z.ZodType<SubscriptionBillingInfo> = z.object({
  outstanding_balance: MoneySchema,
  cycle_executions: CycleExecutionListSchema.optional(),
  last_payment: LastPaymentDetailsSchema.optional(),
  next_billing_time: DateTimeSchema.optional(),
  final_payment_time: DateTimeSchema.optional(),
  failed_payments_count: z.number().int().min(0).max(999),
  last_failed_payment: FailedPaymentDetailsSchema.optional(),
});

export const SubscriptionCancelRequestSchema: z.ZodType<SubscriptionCancelRequest> = z.object({
  reason: z.string().regex(new RegExp("^.*$")).min(1).max(128),
});

export const SubscriptionCaptureRequestSchema: z.ZodType<SubscriptionCaptureRequest> = z.object({
  note: z.string().regex(new RegExp("^.*$")).min(1).max(128),
  capture_type: z.enum(["OUTSTANDING_BALANCE"]),
  amount: MoneySchema,
});

export const SubscriptionReviseResponseDefinitionsLinkDescriptionListSchema: z.ZodType<
  SubscriptionReviseResponseDefinitionsLinkDescriptionList
> = z.array(LinkDescriptionSchema);

export const SubscriptionStatusSchema: z.ZodType<SubscriptionStatus> = z.object({
  status: z.enum(["APPROVAL_PENDING", "APPROVED", "ACTIVE", "SUSPENDED", "CANCELLED", "EXPIRED"]).optional(),
  status_change_note: z.string().regex(new RegExp("^.*$")).min(1).max(128).optional(),
  status_update_time: DateTimeSchema.optional(),
});

export const SubscriptionSuspendRequestSchema: z.ZodType<SubscriptionSuspendRequest> = z.object({
  reason: z.string().regex(new RegExp("^.*$")).min(1).max(128),
});

export const SubscriptionsActivate400Schema: z.ZodType<SubscriptionsActivate400> = z.unknown();

export const SubscriptionsActivate422Schema: z.ZodType<SubscriptionsActivate422> = z.unknown();

export const SubscriptionsCancel400Schema: z.ZodType<SubscriptionsCancel400> = z.unknown();

export const SubscriptionsCancel422Schema: z.ZodType<SubscriptionsCancel422> = z.unknown();

export const SubscriptionsCapture400Schema: z.ZodType<SubscriptionsCapture400> = z.unknown();

export const SubscriptionsCapture422Schema: z.ZodType<SubscriptionsCapture422> = z.unknown();

export const SubscriptionsCreate400Schema: z.ZodType<SubscriptionsCreate400> = z.unknown();

export const SubscriptionsCreate422Schema: z.ZodType<SubscriptionsCreate422> = z.unknown();

export const SubscriptionsPatch400Schema: z.ZodType<SubscriptionsPatch400> = z.unknown();

export const SubscriptionsPatch422Schema: z.ZodType<SubscriptionsPatch422> = z.unknown();

export const SubscriptionsRevise400Schema: z.ZodType<SubscriptionsRevise400> = z.unknown();

export const SubscriptionsRevise404Schema: z.ZodType<SubscriptionsRevise404> = z.unknown();

export const SubscriptionsRevise422Schema: z.ZodType<SubscriptionsRevise422> = z.unknown();

export const SubscriptionsSuspend400Schema: z.ZodType<SubscriptionsSuspend400> = z.unknown();

export const SubscriptionsSuspend422Schema: z.ZodType<SubscriptionsSuspend422> = z.unknown();

export const SubscriptionsTransactions400Schema: z.ZodType<SubscriptionsTransactions400> = z.unknown();

export const TaxesSchema: z.ZodType<Taxes> = z.object({
  percentage: PercentageSchema,
  inclusive: z.boolean().optional(),
});

export const PlanSchema: z.ZodType<Plan> = z.object({
  id: z.string().regex(new RegExp("^P-[A-Z0-9]*$")).min(26).max(26).optional(),
  product_id: z.string().regex(new RegExp("^PROD-[A-Z0-9]*$")).min(22).max(22).optional(),
  name: z.string().regex(new RegExp("^.*$")).min(1).max(127).optional(),
  status: z.enum(["CREATED", "INACTIVE", "ACTIVE"]).optional(),
  description: z.string().regex(new RegExp("^.*$")).min(1).max(127).optional(),
  billing_cycles: BillingCycleListSchema.optional(),
  payment_preferences: PaymentPreferencesSchema.optional(),
  taxes: TaxesSchema.optional(),
  quantity_supported: z.boolean().optional(),
  create_time: DateTimeSchema.optional(),
  update_time: DateTimeSchema.optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const PlanListSchema: z.ZodType<PlanList> = z.array(PlanSchema).min(0).max(32767);

export const PlanCollectionSchema: z.ZodType<PlanCollection> = z.object({
  plans: PlanListSchema.optional(),
  total_items: z.number().int().min(0).max(500000000).optional(),
  total_pages: z.number().int().min(0).max(100000000).optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const PlanRequestPostSchema: z.ZodType<PlanRequestPost> = z.object({
  product_id: z.string().regex(new RegExp("^PROD-[A-Z0-9]*$")).min(22).max(22),
  name: z.string().regex(new RegExp("^.*$")).min(1).max(127),
  status: z.enum(["CREATED", "INACTIVE", "ACTIVE"]).optional(),
  description: z.string().regex(new RegExp("^.*$")).min(1).max(127).optional(),
  billing_cycles: BillingCycleListSchema,
  payment_preferences: PaymentPreferencesSchema,
  taxes: TaxesSchema.optional(),
  quantity_supported: z.boolean().optional(),
});

export const TaxesOverrideSchema: z.ZodType<TaxesOverride> = z.object({
  percentage: PercentageSchema.optional(),
  inclusive: z.boolean().optional(),
});

export const PlanOverrideSchema: z.ZodType<PlanOverride> = z.object({
  billing_cycles: BillingCycleOverrideListSchema.optional(),
  payment_preferences: PaymentPreferencesOverrideSchema.optional(),
  taxes: TaxesOverrideSchema.optional(),
});

export const SubscriptionReviseRequestSchema: z.ZodType<SubscriptionReviseRequest> = z.object({
  plan_id: z.string().regex(new RegExp("^P-[A-Z0-9]*$")).min(26).max(26).optional(),
  quantity: z.string().regex(new RegExp("^([0-9]+|([0-9]+)?[.][0-9]+)$")).min(1).max(32).optional(),
  shipping_amount: MoneySchema.optional(),
  shipping_address: ShippingDetailSchema.optional(),
  application_context: z.object({
    brand_name: z.string().regex(new RegExp("^.*$")).min(1).max(127).optional(),
    locale: LanguageSchema.optional(),
    shipping_preference: z.enum(["GET_FROM_FILE", "NO_SHIPPING", "SET_PROVIDED_ADDRESS"]).optional(),
    payment_method: z.object({
      payee_preferred: PayeePaymentMethodPreferenceSchema.optional(),
    }).optional(),
    return_url: z.string().min(10).max(4000),
    cancel_url: z.string().min(10).max(4000),
  }).optional(),
  plan: PlanOverrideSchema.optional(),
});

export const SubscriptionReviseResponseSchema: z.ZodType<SubscriptionReviseResponse> = z.object({
  plan_id: z.string().regex(new RegExp("^P-[A-Z0-9]*$")).min(26).max(26).optional(),
  quantity: z.string().regex(new RegExp("^([0-9]+|([0-9]+)?[.][0-9]+)$")).min(1).max(32).optional(),
  shipping_amount: MoneySchema.optional(),
  shipping_address: ShippingDetailSchema.optional(),
  plan: PlanOverrideSchema.optional(),
  plan_overridden: z.boolean().optional(),
  links: SubscriptionReviseResponseDefinitionsLinkDescriptionListSchema.optional(),
});

export const ThreeDSecureAuthenticationResponseSchema: z.ZodType<ThreeDSecureAuthenticationResponse> = z.object({
  authentication_status: ParesStatusSchema.optional(),
  enrollment_status: EnrolledSchema.optional(),
});

export const AuthenticationResponseSchema: z.ZodType<AuthenticationResponse> = z.object({
  liability_shift: LiabilityShiftSchema.optional(),
  three_d_secure: ThreeDSecureAuthenticationResponseSchema.optional(),
});

export const TransactionSchema: z.ZodType<Transaction> = z.object({
  status: z.enum(["COMPLETED", "DECLINED", "PARTIALLY_REFUNDED", "PENDING", "REFUNDED", "FAILED"]).optional(),
  id: z.string().min(3).max(50),
  amount_with_breakdown: AmountWithBreakdownSchema,
  payer_name: NameSchema.optional(),
  payer_email: EmailAddressSchema.optional(),
  time: DateTimeSchema,
});

export const TransactionListSchema: z.ZodType<TransactionList> = z.array(TransactionSchema).min(0).max(32767);

export const TransactionsListSchema: z.ZodType<TransactionsList> = z.object({
  transactions: TransactionListSchema.optional(),
  total_items: z.number().int().min(0).max(500000000).optional(),
  total_pages: z.number().int().min(0).max(100000000).optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const UpdatePricingSchemeRequestSchema: z.ZodType<UpdatePricingSchemeRequest> = z.object({
  billing_cycle_sequence: z.number().int().min(1).max(99),
  pricing_scheme: PricingSchemeSchema,
});

export const UpdatePricingSchemeRequestListSchema: z.ZodType<UpdatePricingSchemeRequestList> = z.array(
  UpdatePricingSchemeRequestSchema,
).min(1).max(99);

export const UpdatePricingSchemesListRequestSchema: z.ZodType<UpdatePricingSchemesListRequest> = z.object({
  pricing_schemes: UpdatePricingSchemeRequestListSchema,
});

export const VaultInstructionBaseSchema: z.ZodType<VaultInstructionBase> = z.object({
  store_in_vault: StoreInVaultInstructionSchema.optional(),
});

export const CardAttributesSchema: z.ZodType<CardAttributes> = z.object({
  customer: CustomerSchema.optional(),
  vault: VaultInstructionBaseSchema.optional(),
  verification: CardVerificationSchema.optional(),
});

export const PaymentSourceSchema: z.ZodType<PaymentSource> = z.object({
  card: z.object({
    name: z.string().regex(new RegExp("^.{1,300}$")).min(1).max(300).optional(),
    number: z.string().regex(new RegExp("^[0-9]{13,19}$")).min(13).max(19).optional(),
    expiry: DateYearMonthSchema.optional(),
    security_code: z.string().regex(new RegExp("^[0-9]{3,4}$")).min(3).max(4).optional(),
    type: CardTypeSchema.optional(),
    brand: CardBrandSchema.optional(),
    billing_address: z.object({
      address_line_1: z.string().max(300).optional(),
      address_line_2: z.string().max(300).optional(),
      admin_area_2: z.string().max(120).optional(),
      admin_area_1: z.string().max(300).optional(),
      postal_code: z.string().max(60).optional(),
      country_code: CountryCodeSchema,
    }).optional(),
    attributes: CardAttributesSchema.optional(),
  }).optional(),
});

export const SubscriberRequestSchema: z.ZodType<SubscriberRequest> = PayerBaseSchema.and(z.unknown()).and(z.object({}))
  .and(z.unknown()).and(z.object({}));

export const SubscriptionRequestPostSchema: z.ZodType<SubscriptionRequestPost> = z.object({
  plan_id: z.string().regex(new RegExp("^P-[A-Z0-9]*$")).min(26).max(26),
  start_time: DateTimeSchema.optional(),
  quantity: z.string().regex(new RegExp("^([0-9]+|([0-9]+)?[.][0-9]+)$")).min(1).max(32).optional(),
  shipping_amount: MoneySchema.optional(),
  subscriber: SubscriberRequestSchema.optional(),
  auto_renewal: z.boolean().optional(),
  application_context: ApplicationContextSchema.optional(),
  custom_id: z.string().regex(new RegExp("^[\x20-\x7E]+")).min(1).max(127).optional(),
  plan: PlanOverrideSchema.optional(),
  merchant_inventory: MerchantInventorySchema.optional(),
});

export const VaultResponseSchema: z.ZodType<VaultResponse> = z.object({
  id: z.string().min(1).max(255).optional(),
  status: z.enum(["VAULTED", "CREATED", "APPROVED"]).optional(),
  customer: z.object({
    id: MerchantPartnerCustomerIdSchema.optional(),
  }).optional(),
  links: DefinitionsLinkDescriptionListSchema.optional(),
});

export const CardAttributesResponseSchema: z.ZodType<CardAttributesResponse> = z.object({
  vault: VaultResponseSchema.optional(),
});

export const CardResponseSchema: z.ZodType<CardResponse> = z.object({
  name: z.string().min(2).max(300).optional(),
  last_digits: z.string().regex(new RegExp("[0-9]{2,}")).optional(),
  brand: CardBrandSchema.optional(),
  available_networks: CardBrandListSchema.optional(),
  type: CardTypeSchema.optional(),
  authentication_result: AuthenticationResponseSchema.optional(),
  attributes: CardAttributesResponseSchema.optional(),
  from_request: CardFromRequestSchema.optional(),
  expiry: DateYearMonthSchema.optional(),
  bin_details: BinDetailsSchema.optional(),
});

export const CardResponseWithBillingAddressSchema: z.ZodType<CardResponseWithBillingAddress> = CardResponseSchema.and(
  z.unknown(),
).and(z.object({}));

export const PaymentSourceResponseSchema: z.ZodType<PaymentSourceResponse> = z.object({
  card: CardResponseWithBillingAddressSchema.optional(),
});

export const SubscriberSchema: z.ZodType<Subscriber> = PayerBaseSchema.and(z.unknown()).and(z.object({})).and(
  z.unknown(),
).and(z.object({}));

export const SubscriptionSchema: z.ZodType<Subscription> = SubscriptionStatusSchema.and(z.unknown()).and(z.unknown())
  .and(z.object({}));

/**
 * API Client class with path-based access to all operations.
 * All methods are inline within the class.
 */
export class ApiClient {
  constructor(private readonly config: OpenAPIClientConfig) {}

  readonly "/v1/billing/plans" = {
    /**
     * List plans
     *
     * Lists billing plans.
     */
    get: (request?: {
      query?: {
        /** Filters the response by a Product ID. */
        product_id?: string;
        /** The number of items to return in the response. */
        page_size?: number;
        /** A non-zero integer which is the start index of the entire list of items to return in the response. The combination of `page=1` and `page_size=20` returns the first 20 items. The combination of `page=2` and `page_size=20` returns the next 20 items. */
        page?: number;
        /** Indicates whether to show the total count in the response. */
        total_required?: boolean;
      };
      headers?: {
        /** The preferred server response upon successful completion of the request. Value is:<ul><li><code>return=minimal</code>. The server returns a minimal response to optimize communication between the API caller and the server. A minimal response includes the <code>id</code>, <code>status</code> and HATEOAS links.</li><li><code>return=representation</code>. The server returns a complete resource representation, including the current state of the resource.</li></ul> */
        Prefer?: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that lists billing plans. */
        200: {
          body: PlanCollection;
        };
        /** Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & _400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/plans",
          method: "get",
          requestSchema: z.object({
            query: z.object({
              product_id: z.string().min(6).max(50).optional(),
              page_size: z.number().int().min(1).max(20).default(10).optional(),
              page: z.number().int().min(1).max(100000).default(1).optional(),
              total_required: z.boolean().optional(),
            }).optional(),
            headers: z.object({
              Prefer: z.string().default("return=minimal").optional(),
            }).optional(),
          }),
          responseSchema: {
            200: z.object({ body: PlanCollectionSchema }),
            400: z.object({ body: Error400Schema.and(_400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
    /**
     * Create plan
     *
     * Creates a plan that defines pricing and billing cycle details for subscriptions.
     */
    post: (request: {
      headers?: {
        /** The preferred server response upon successful completion of the request. Value is:<ul><li><code>return=minimal</code>. The server returns a minimal response to optimize communication between the API caller and the server. A minimal response includes the <code>id</code>, <code>status</code> and HATEOAS links.</li><li><code>return=representation</code>. The server returns a complete resource representation, including the current state of the resource.</li></ul> */
        Prefer?: string;
        /** The server stores keys for 72 hours. */
        "PayPal-Request-Id"?: string;
      };
      body: PlanRequestPost;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows billing plan details. */
        200: {
          body: Plan;
        };
        /** A successful request returns the HTTP `201 Created` status code and a JSON response body that shows billing plan details. */
        201: {
          body: Plan;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & PlansCreate400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & _422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/plans",
          method: "post",
          requestSchema: z.object({
            headers: z.object({
              Prefer: z.string().default("return=minimal").optional(),
              "PayPal-Request-Id": z.string().optional(),
            }).optional(),
            body: PlanRequestPostSchema,
          }),
          responseSchema: {
            200: z.object({ body: PlanSchema }),
            201: z.object({ body: PlanSchema }),
            400: z.object({ body: Error400Schema.and(PlansCreate400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            422: z.object({ body: Error422Schema.and(_422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/plans/{id}" = {
    /**
     * Show plan details
     *
     * Shows details for a plan, by ID.
     */
    get: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows plan details. */
        200: {
          body: Plan;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/plans/{id}",
          method: "get",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
          }),
          responseSchema: {
            200: z.object({ body: PlanSchema }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
    /**
     * Update plan
     *
     * Updates a plan with the `CREATED` or `ACTIVE` status. For an `INACTIVE` plan, you can make only status updates.<br/>You can patch these attributes and objects:<table><thead><tr><th>Attribute or object</th><th>Operations</th></tr></thead><tbody><tr><td><code>description</code></td><td>replace</td></tr><tr><td><code>payment_preferences.auto_bill_outstanding</code></td><td>replace</td></tr><tr><td><code>taxes.percentage</code></td><td>replace</td></tr><tr><td><code>payment_preferences.payment_failure_threshold</code></td><td>replace</td></tr><tr><td><code>payment_preferences.setup_fee</code></td><td>replace</td></tr><tr><td><code>payment_preferences.setup_fee_failure_action</code></td><td>replace</td></tr><tr><td><code>name</code></td><td>replace</td></tr></tbody></table>
     */
    patch: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & PlansPatch400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & PlansPatch422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/plans/{id}",
          method: "patch",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            400: z.object({ body: Error400Schema.and(PlansPatch400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(PlansPatch422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/plans/{id}/activate" = {
    /**
     * Activate plan
     *
     * Activates a plan, by ID.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & PlansActivate422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/plans/{id}/activate",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(PlansActivate422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/plans/{id}/deactivate" = {
    /**
     * Deactivate plan
     *
     * Deactivates a plan, by ID.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & PlansDeactivate422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/plans/{id}/deactivate",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(PlansDeactivate422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/plans/{id}/update-pricing-schemes" = {
    /**
     * Update pricing
     *
     * Updates pricing for a plan. For example, you can update a regular billing cycle from $5 per month to $7 per month.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      body: UpdatePricingSchemesListRequest;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & PlansUpdatePricingSchemes400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & PlansUpdatePricingSchemes422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/plans/{id}/update-pricing-schemes",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            body: UpdatePricingSchemesListRequestSchema,
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            400: z.object({ body: Error400Schema.and(PlansUpdatePricingSchemes400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(PlansUpdatePricingSchemes422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions" = {
    /**
     * Create subscription
     *
     * Creates a subscription.
     */
    post: (request: {
      headers?: {
        /** The preferred server response upon successful completion of the request. Value is:<ul><li><code>return=minimal</code>. The server returns a minimal response to optimize communication between the API caller and the server. A minimal response includes the <code>id</code>, <code>status</code> and HATEOAS links.</li><li><code>return=representation</code>. The server returns a complete resource representation, including the current state of the resource.</li></ul> */
        Prefer?: string;
        /** The server stores keys for 72 hours. */
        "PayPal-Request-Id"?: string;
      };
      body: SubscriptionRequestPost;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows subscription details. */
        200: {
          body: Subscription;
        };
        /** A successful request returns the HTTP `201 Created` status code and a JSON response body that shows subscription details. */
        201: {
          body: Subscription;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsCreate400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & SubscriptionsCreate422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions",
          method: "post",
          requestSchema: z.object({
            headers: z.object({
              Prefer: z.string().default("return=minimal").optional(),
              "PayPal-Request-Id": z.string().optional(),
            }).optional(),
            body: SubscriptionRequestPostSchema,
          }),
          responseSchema: {
            200: z.object({ body: SubscriptionSchema }),
            201: z.object({ body: SubscriptionSchema }),
            400: z.object({ body: Error400Schema.and(SubscriptionsCreate400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            422: z.object({ body: Error422Schema.and(SubscriptionsCreate422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions/{id}" = {
    /**
     * Show subscription details
     *
     * Shows details for a subscription, by ID.
     */
    get: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      query?: {
        /** List of fields that are to be returned in the response. Possible value for fields are last_failed_payment and plan. */
        fields?: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows subscription details. */
        200: {
          body: Subscription;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}",
          method: "get",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            query: z.object({
              fields: z.string().min(1).max(100).optional(),
            }).optional(),
          }),
          responseSchema: {
            200: z.object({ body: SubscriptionSchema }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
    /**
     * Update subscription
     *
     * Updates a subscription which could be in <code>ACTIVE</code> or <code>SUSPENDED</code> status. You can override plan level default attributes by providing customised values for plan path in the patch request.<br /> <ul> <li>You cannot update attributes that have already completed (Example - trial cycles can’t be updated if completed).</li> <li>Once overridden, changes to plan resource will not impact subscription.</li> <li>Any price update will not impact billing cycles within next 10 days (Applicable only for subscriptions funded by PayPal account).</li> </ul> Following are the fields eligible for patch.<table><thead><tr><th>Attribute or object</th><th>Operations</th></tr></thead><tbody><tr><td><code>billing_info.outstanding_balance</code></td><td>replace</td></tr><tr><td><code>custom_id</code></td><td>add,replace</td></tr><tr><td><code>plan.billing_cycles[@sequence==n].<br/>pricing_scheme.fixed_price</code></td><td>add,replace</td></tr><tr><td><code>plan.billing_cycles[@sequence==n].<br/>pricing_scheme.tiers</code></td><td>replace</td></tr><tr><td><code>plan.billing_cycles[@sequence==n].<br/>total_cycles</code></td><td>replace</td></tr><tr><td><code>plan.payment_preferences.<br/>auto_bill_outstanding</code></td><td>replace</td></tr><tr><td><code>plan.payment_preferences.<br/>payment_failure_threshold</code></td><td>replace</td></tr><tr><td><code>plan.taxes.inclusive</code></td><td>add,replace</td></tr><tr><td><code>plan.taxes.percentage</code></td><td>add,replace</td></tr><tr><td><code>shipping_amount</code></td><td>add,replace</td></tr><tr><td><code>start_time</code></td><td>replace</td></tr><tr><td><code>subscriber.shipping_address</code></td><td>add,replace</td></tr><tr><td><code>subscriber.payment_source (for subscriptions funded<br/>by card payments)</code></td><td>replace</td></tr></tbody></table>
     */
    patch: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsPatch400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & SubscriptionsPatch422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}",
          method: "patch",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            400: z.object({ body: Error400Schema.and(SubscriptionsPatch400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(SubscriptionsPatch422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions/{id}/activate" = {
    /**
     * Activate subscription
     *
     * Activates the subscription.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      body?: SubscriptionActivateRequest;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsActivate400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & SubscriptionsActivate422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}/activate",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            body: SubscriptionActivateRequestSchema,
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            400: z.object({ body: Error400Schema.and(SubscriptionsActivate400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(SubscriptionsActivate422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions/{id}/cancel" = {
    /**
     * Cancel subscription
     *
     * Cancels the subscription.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      body: SubscriptionCancelRequest;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsCancel400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & SubscriptionsCancel422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}/cancel",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            body: SubscriptionCancelRequestSchema,
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            400: z.object({ body: Error400Schema.and(SubscriptionsCancel400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(SubscriptionsCancel422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions/{id}/capture" = {
    /**
     * Capture authorized payment on subscription
     *
     * Captures an authorized payment from the subscriber on the subscription.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      headers?: {
        /** The server stores keys for 72 hours. */
        "PayPal-Request-Id"?: string;
      };
      body: SubscriptionCaptureRequest;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows subscription details. */
        200: {
          body: Transaction;
        };
        /** Request Accepted. */
        202: {
          body: unknown;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsCapture400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & SubscriptionsCapture422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}/capture",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            headers: z.object({
              "PayPal-Request-Id": z.string().optional(),
            }).optional(),
            body: SubscriptionCaptureRequestSchema,
          }),
          responseSchema: {
            200: z.object({ body: TransactionSchema }),
            202: z.object({ body: z.unknown() }),
            400: z.object({ body: Error400Schema.and(SubscriptionsCapture400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(SubscriptionsCapture422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions/{id}/revise" = {
    /**
     * Revise plan or quantity of subscription
     *
     * Updates the quantity of the product or service in a subscription. You can also use this method to switch the plan and update the `shipping_amount`, `shipping_address` values for the subscription. This type of update requires the buyer's consent.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      body?: SubscriptionReviseRequest;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows subscription details. */
        200: {
          body: SubscriptionReviseResponse;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsRevise400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & SubscriptionsRevise404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & SubscriptionsRevise422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}/revise",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            body: SubscriptionReviseRequestSchema,
          }),
          responseSchema: {
            200: z.object({ body: SubscriptionReviseResponseSchema }),
            400: z.object({ body: Error400Schema.and(SubscriptionsRevise400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(SubscriptionsRevise404Schema) }),
            422: z.object({ body: Error422Schema.and(SubscriptionsRevise422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions/{id}/suspend" = {
    /**
     * Suspend subscription
     *
     * Suspends the subscription.
     */
    post: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      body: SubscriptionSuspendRequest;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsSuspend400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** The requested action could not be performed, semantically incorrect, or failed business validation. */
        422: {
          body: Error422 & SubscriptionsSuspend422;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}/suspend",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            body: SubscriptionSuspendRequestSchema,
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            400: z.object({ body: Error400Schema.and(SubscriptionsSuspend400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            422: z.object({ body: Error422Schema.and(SubscriptionsSuspend422Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/billing/subscriptions/{id}/transactions" = {
    /**
     * List transactions for subscription
     *
     * Lists transactions for a subscription.
     */
    get: (request: {
      path: {
        /** The ID of the subscription. */
        id: string;
      };
      query: {
        /** The start time of the range of transactions to list. */
        start_time: string;
        /** The end time of the range of transactions to list. */
        end_time: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows subscription details. */
        200: {
          body: TransactionsList;
        };
        /** Bad Request. Request is not well-formed, syntactically incorrect, or violates schema. */
        400: {
          body: Error400 & SubscriptionsTransactions400;
        };
        /** Authentication failed due to missing authorization header, or invalid authentication credentials. */
        401: {
          body: Error401 & _401;
        };
        /** Authorization failed due to insufficient permissions. */
        403: {
          body: Error403 & _403;
        };
        /** The specified resource does not exist. */
        404: {
          body: Error404 & _404;
        };
        /** An internal server error has occurred. */
        500: {
          body: Error500;
        };
        "default": {
          body: unknown;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/billing/subscriptions/{id}/transactions",
          method: "get",
          requestSchema: z.object({
            path: z.object({
              id: z.string(),
            }),
            query: z.object({
              start_time: z.string().regex(
                new RegExp(
                  "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])[T,t]([0-1][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)([.][0-9]+)?([Zz]|[+-][0-9]{2}:[0-9]{2})$",
                ),
              ).min(20).max(64),
              end_time: z.string().regex(
                new RegExp(
                  "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])[T,t]([0-1][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)([.][0-9]+)?([Zz]|[+-][0-9]{2}:[0-9]{2})$",
                ),
              ).min(20).max(64),
            }),
          }),
          responseSchema: {
            200: z.object({ body: TransactionsListSchema }),
            400: z.object({ body: Error400Schema.and(SubscriptionsTransactions400Schema) }),
            401: z.object({ body: Error401Schema.and(_401Schema) }),
            403: z.object({ body: Error403Schema.and(_403Schema) }),
            404: z.object({ body: Error404Schema.and(_404Schema) }),
            500: z.object({ body: Error500Schema }),
            "default": z.object({ body: z.unknown() }),
          },
        },
        request,
      ) as any,
  };
}

// deno-lint-ignore-file no-explicit-any
// This file was auto-generated by @brad-jones/open-api-client

import {
  createCustomClient,
  type OpenAPIClientConfig,
  openAPIFetch,
  type OpenAPIResponses,
} from "jsr:@brad-jones/deno-net-open-api-client@0.2.1";
import { z } from "npm:zod@^4.1.12";

export type _400 = unknown;

export type _401 = unknown;

export type _403 = unknown;

export type _422 = unknown;

/** The non-portable additional address details that are sometimes needed for compliance, risk, or other scenarios where fine-grain address information might be needed. Not portable with common third party and open source. Redundant with core fields.<br/>For example, `address_portable.address_line_1` is usually a combination of `address_details.street_number`, `street_name`, and `street_type`. */
export type AddressDetails = {
  /** The street number. */
  street_number?: string;
  /** The street name. Just `Drury` in `Drury Lane`. */
  street_name?: string;
  /** The street type. For example, avenue, boulevard, road, or expressway. */
  street_type?: string;
  /** The delivery service. Post office box, bag number, or post office name. */
  delivery_service?: string;
  /** A named locations that represents the premise. Usually a building name or number or collection of buildings with a common name or number. For example, <code>Craven House</code>. */
  building_name?: string;
  /** The first-order entity below a named building or location that represents the sub-premise. Usually a single building within a collection of buildings with a common name. Can be a flat, story, floor, room, or apartment. */
  sub_building?: string;
};

/** The preference that customizes the billing experience of the customer. */
export type BillingExperiencePreference = {
  /** The ID of the payment web experience profile. */
  experience_id?: string;
  /** Indicates whether the partner has already displayed the billing context to the seller. */
  billing_context_set?: boolean;
};

/** The details of the billing agreement between the partner and a seller. */
export type BillingAgreement = {
  /** The billing agreement description. */
  description?: string;
  billing_experience_preference?: BillingExperiencePreference;
  /** The custom data for the billing agreement. */
  merchant_custom_data?: string;
  /**
   * The URL to which to redirect seller to accept the billing agreement.
   *
   * @format uri
   */
  approval_url?: string;
  /** The billing agreement token for the agreement. */
  ec_token?: string;
};

export type BusinessAddressType = "WORK";

export type BusinessDocumentType =
  | "SOCIAL_SECURITY_NUMBER"
  | "EMPLOYMENT_IDENTIFICATION_NUMBER"
  | "TAX_IDENTIFICATION_NUMBER"
  | "PASSPORT_NUMBER"
  | "PENSION_FUND_ID"
  | "MEDICAL_INSURANCE_ID"
  | "CNPJ"
  | "CPF"
  | "PAN"
  | "BUSINESS_REGISTRATION";

/** The category, subcategory and MCC code of the business. */
export type BusinessIndustry = {
  /** The customer's business category code. PayPal uses industry standard seller category codes. */
  category: string;
  /** The customer's business seller category code. PayPal uses industry standard seller category codes. */
  mcc_code: string;
  /** The customer's business subcategory code. PayPal uses industry standard seller subcategory codes. */
  subcategory: string;
};

/** The business name of the party. */
export type BusinessName = {
  /** Required. The business name of the party. */
  business_name?: string;
};

export type BusinessNameType = "DOING_BUSINESS_AS" | "LEGAL_NAME";

export type BusinessNameDetail = BusinessName & unknown & /** Name of the business provided. */ Record<string, never>;

/** Name of the business. */
export type BusinessNameDetailList = BusinessNameDetail[];

export type BusinessPhoneType = "CUSTOMER_SERVICE" | "BUSINESS";

export type BusinessSubType =
  | "ASSO_TYPE_INCORPORATED"
  | "ASSO_TYPE_NON_INCORPORATED"
  | "GOVT_TYPE_ENTITY"
  | "GOVT_TYPE_EMANATION"
  | "GOVT_TYPE_ESTD_COMM"
  | "GOVT_TYPE_ESTD_FC"
  | "GOVT_TYPE_ESTD_ST_TR";

export type BusinessType =
  | "ANY_OTHER_BUSINESS_ENTITY"
  | "ASSOCIATION"
  | "CORPORATION"
  | "GENERAL_PARTNERSHIP"
  | "GOVERNMENT"
  | "INDIVIDUAL"
  | "LIMITED_LIABILITY_PARTNERSHIP"
  | "LIMITED_LIABILITY_PROPRIETORS"
  | "LIMITED_LIABILITY_PRIVATE_CORPORATION"
  | "LIMITED_PARTNERSHIP"
  | "LIMITED_PARTNERSHIP_PRIVATE_CORPORATION"
  | "NONPROFIT"
  | "ONLY_BUY_OR_SEND_MONEY"
  | "OTHER_CORPORATE_BODY"
  | "PARTNERSHIP"
  | "PRIVATE_PARTNERSHIP"
  | "PROPRIETORSHIP"
  | "PROPRIETORSHIP_CRAFTSMAN"
  | "PROPRIETORY_COMPANY"
  | "PRIVATE_CORPORATION"
  | "PUBLIC_COMPANY"
  | "PUBLIC_CORPORATION"
  | "PUBLIC_PARTNERSHIP"
  | "REGISTERED_COOPERATIVE";

/** The type and subtype of the business. */
export type BusinessTypeInfo = {
  type?: BusinessType;
  subtype?: BusinessSubType;
};

export type Capabilities = "PAYPAL_WALLET_VAULTING_ADVANCED" | "PAY_UPON_INVOICE" | "APPLE_PAY" | "GOOGLE_PAY";

/** An array of capabilities which the partner wants to enable for the selected products. Supported only when products are specified. */
export type CapabilitiesList = Capabilities[];

/** The integration details for PayPal CLASSIC endpoints. */
export type ClassicApiIntegration = Record<string, never>;

/**
 * The [two-character ISO 3166-1 code](/docs/api/reference/country-codes/) that identifies the country or region.<blockquote><strong>Note:</strong> The country code for Great Britain is <code>GB</code> and not <code>UK</code> as used in the top-level domain names for that country. Use the `C2` country code for China worldwide for comparable uncontrolled price (CUP) method, bank card, and cross-border transactions.</blockquote>
 *
 * @format ppaas_common_country_code_v2
 */
export type CountryCode = string;

/** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
export type AddressPortable = {
  /** The first line of the address. For example, number or street. For example, `173 Drury Lane`. Required for data entry and compliance and risk checks. Must contain the full address. */
  address_line_1?: string;
  /** The second line of the address. For example, suite or apartment number. */
  address_line_2?: string;
  /** The third line of the address, if needed. For example, a street complement for Brazil, direction text, such as `next to Walmart`, or a landmark in an Indian address. */
  address_line_3?: string;
  /** The neighborhood, ward, or district. Smaller than `admin_area_level_3` or `sub_locality`. Value is:<ul><li>The postal sorting code for Guernsey and many French territories, such as French Guiana.</li><li>The fine-grained administrative levels in China.</li></ul> */
  admin_area_4?: string;
  /** A sub-locality, suburb, neighborhood, or district. Smaller than `admin_area_level_2`. Value is:<ul><li>Brazil. Suburb, bairro, or neighborhood.</li><li>India. Sub-locality or district. Street name information is not always available but a sub-locality or district can be a very small area.</li></ul> */
  admin_area_3?: string;
  /** A city, town, or village. Smaller than `admin_area_level_1`. */
  admin_area_2?: string;
  /** The highest level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. Format for postal delivery. For example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A kanton.</li></ul> */
  admin_area_1?: string;
  /** The postal code, which is the zip code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
  postal_code?: string;
  country_code: CountryCode;
  address_details?: AddressDetails;
};

export type BusinessAddressDetail =
  & AddressPortable
  & unknown
  & /** A simple postal address with coarse-grained fields. */ Record<string, never>;

/** List of addresses associated with the business entity. */
export type BusinessAddressDetailList = BusinessAddressDetail[];

/**
 * The [two-character ISO 3166-1 code](/docs/integration/direct/rest/country-codes/) that identifies the country or region.<blockquote><strong>Note:</strong> The country code for Great Britain is <code>GB</code> and not <code>UK</code> as used in the top-level domain names for that country. Use the `C2` country code for China worldwide for comparable uncontrolled price (CUP) method, bank card, and cross-border transactions.</blockquote>
 *
 * @format ppaas_common_country_code_v2
 */
export type CountryCode2 = string;

/**
 * The [three-character ISO-4217 currency code](/docs/integration/direct/rest/currency-codes/) that identifies the currency.
 *
 * @format ppaas_common_currency_code_v2
 */
export type CurrencyCode = string;

/**
 * The [three-character ISO-4217 currency code](/docs/api/reference/currency-codes/) that identifies the currency.
 *
 * @format ppaas_common_currency_code_v2
 */
export type CurrencyCode2 = string;

/** Requested Currencies for a Transfer Method. */
export type CurrencyCodeList = CurrencyCode2[];

/**
 * The stand-alone date, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). To represent special legal values, such as a date of birth, you should use dates with no associated time or time-zone data. Whenever possible, use the standard `date_time` type. This regular expression does not validate all dates. For example, February 31 is valid and nothing is known about leap years.
 *
 * @format ppaas_date_notime_v2
 */
export type DateNoTime = string;

/** Date of birth data provided by the user */
export type BirthDetails = {
  date_of_birth: DateNoTime;
};

/**
 * The date and time, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Seconds are required while fractional seconds are optional.<blockquote><strong>Note:</strong> The regular expression provides guidance but does not reject all invalid dates.</blockquote>
 *
 * @format ppaas_date_time_v3
 */
export type DateTime = string;

export type DependentProcess = unknown;

/** An array of dependent processes. */
export type DependentProcessList = DependentProcess[];

/**
 * The internationalized email address.<blockquote><strong>Note:</strong> Up to 64 characters are allowed before and 255 characters are allowed after the <code>@</code> sign. However, the generally accepted maximum length for an email address is 254 characters. The pattern verifies that an unquoted <code>@</code> sign exists.</blockquote>
 *
 * @format ppaas_common_email_address_v2
 */
export type EmailAddress = string;

/** An email address at which the person or business can be contacted. */
export type Email = {
  type: "CUSTOMER_SERVICE";
  email: EmailAddress;
};

/** Email addresses of the business. */
export type EmailList = Email[];

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export type ErrorLinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "PATCH";
};

/** This is either a system or application error, and generally indicates that although the client appeared to provide a correct request, something unexpected has gone wrong on the server. */
export type Error500 = {
  name?: "INTERNAL_SERVER_ERROR";
  message?: "An internal server error occurred.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server is temporarily unable to handle the request, for example, because of planned maintenance or downtime. */
export type Error503 = {
  name?: "SERVICE_UNAVAILABLE";
  message?: "Service Unavailable.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

export type ErrorLocation = "body" | "path" | "query";

/** The error details. Required for client-side `4XX` errors. */
export type ErrorDetails = {
  /** The field that caused the error. If this field is in the body, set this value to the field's JSON pointer value. Required for client-side errors. */
  field?: string;
  /** The value of the field that caused the error. */
  value?: string;
  location?: ErrorLocation;
  /** The unique, fine-grained application-level error code. */
  issue: string;
  /** The human-readable description for an issue. The description can change over the lifetime of an API, so clients must not depend on this value. */
  description?: string;
};

/** Request is not well-formed, syntactically incorrect, or violates schema. */
export type Error400 = {
  name?: "INVALID_REQUEST";
  message?: "Request is not well-formed, syntactically incorrect, or violates schema.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** Authentication failed due to missing Authorization header, or invalid authentication credentials. */
export type Error401 = {
  name?: "AUTHENTICATION_FAILURE";
  message?: "Authentication failed due to missing authorization header, or invalid authentication credentials.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The client is not authorized to access this resource, although it may have valid credentials.  */
export type Error403 = {
  name?: "NOT_AUTHORIZED";
  message?: "Authorization failed due to insufficient permissions.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has not found anything matching the request URI. This either means that the URI is incorrect or the resource is not available. */
export type Error404 = {
  name?: "RESOURCE_NOT_FOUND";
  message?: "The specified resource does not exist.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has detected a conflict while processing this request. */
export type Error409 = {
  name?: "RESOURCE_CONFLICT";
  message?: "The server has detected a conflict while processing this request.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server does not support the request payload's media type. */
export type Error415 = {
  name?: "UNSUPPORTED_MEDIA_TYPE";
  message?: "The server does not support the request payload's media type.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The requested action cannot be performed and may require interaction with APIs or processes outside of the current request. This is distinct from a 500 response in that there are no systemic problems limiting the API from performing the request. */
export type Error422 = {
  name?: "UNPROCESSABLE_ENTITY";
  message?: "The requested action could not be performed, semantically incorrect, or failed business validation.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

export type ErrorDefault =
  | Error400
  | Error401
  | Error403
  | Error404
  | Error409
  | Error415
  | Error422
  | Error500
  | Error503;

/** The file reference. Can be a file in PayPal MediaServ, PayPal DMS, or another custom store. */
export type FileReference = {
  /** The ID of the referenced file. */
  id?: string;
  /**
   * The reference URL for the file.
   *
   * @format uri
   */
  reference_url?: string;
  /** The [Internet Assigned Numbers Authority (IANA) media type of the file](https://www.iana.org/assignments/media-types/media-types.xhtml). */
  content_type?: string;
  create_time?: DateTime;
  /** The size of the file, in bytes. */
  size?: string;
};

/** The files contained in the document. For example, a document could be represented by a front page file and a back page file, etc. */
export type FileReferenceList = FileReference[];

/** The bank account ID. An ID with `ROUTING_NUMBER_1` is required. */
export type Identifier = {
  type?:
    | "BANK_CODE"
    | "BI_CODE"
    | "BRANCH_CODE"
    | "ROUTING_NUMBER_1"
    | "ROUTING_NUMBER_2"
    | "ROUTING_NUMBER_3"
    | "SWIFT_CODE"
    | "BRANCH_CODE"
    | "INTERMEDIARY_SWIFT_CODE"
    | "BBAN"
    | "BBAN_ENCRYPTED"
    | "BBAN_HMAC"
    | "AGGREGATOR_YODLEE";
  /** The value of account identifier. */
  value?: string;
};

/** An array of instrument institute attributes. Used with the account number to uniquely identify the instrument. Value is:<ul><li>For banks with IBAN information, the IBAN number.</li><li>For banks with BBAN information, the BBAN number.</li><li>For banks with both IBAN and BBAN information, the IBAN number.</li></ul> */
export type IdentifierList = Identifier[];

export type IndividualOwnerType = "PRIMARY";

/** A document label. */
export type Labels = string;

/** The document labels. A document could be classfied to multiple categories. For example, a bill document can be classfified as `BILL DOCUMENT` and `UTILITY DOCUMENT`. */
export type LabelsList = Labels[];

/**
 * The [language tag](https://tools.ietf.org/html/bcp47#section-2) for the language in which to localize the error-related strings, such as messages, issues, and suggested actions. The tag is made up of the [ISO 639-2 language code](https://www.loc.gov/standards/iso639-2/php/code_list.php), the optional [ISO-15924 script tag](https://www.unicode.org/iso15924/codelists.html), and the [ISO-3166 alpha-2 country code](/docs/integration/direct/rest/country-codes/).
 *
 * @format ppaas_common_language_v3
 */
export type Language = string;

/** The customer-provided consent. */
export type LegalConsent = {
  type: "SHARE_DATA_CONSENT";
  /** Indicates whether the customer agreed to share this type of data. To give consent, specify `true`. To withhold consent, specify `false`. */
  granted: boolean;
};

/** An array of all consents that the partner has received from this seller. If `SHARE_DATA_CONSENT` is not granted, PayPal does not store customer data. */
export type LegalConsentList = LegalConsent[];

/** The request-related [HATEOAS link](/docs/api/reference/api-responses/#hateoas-links) information. */
export type LinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "HEAD" | "CONNECT" | "OPTIONS" | "PATCH";
};

/** An array of request-related [HATEOAS links](/docs/api/overview/#hateoas-links). */
export type DefinitionsLinkDescriptionList = LinkDescription[];

/** The shared referral data. */
export type CreateReferralDataResponse = {
  links?: DefinitionsLinkDescriptionList;
};

/** The HATEOAS links. */
export type LinkDescriptionList = LinkDescription[];

/** The document object. */
export type Document = {
  /** The encrypted identifier for the document. */
  id?: string;
  labels?: LabelsList;
  /** The file name. */
  name?: string;
  /** The number for the document. It is the ID number if the document is `ID CARD`, the passport number if the document is `PASSPORT`, etc. */
  identification_number?: string;
  issue_date?: DateNoTime;
  expiry_date?: DateNoTime;
  issuing_country_code?: CountryCode;
  files?: FileReferenceList;
  links?: LinkDescriptionList;
};

export type BusinessDocument = Document & unknown & unknown;

/** Business Party related Document data collected from the customer.. For example SSN, ITIN, Business registration number that were collected from the user. */
export type BusinessDocumentList = BusinessDocument[];

/** Seller’s consent to operate on this financial instrument. */
export type Mandate = {
  /** Whether mandate was accepted or not. */
  accepted: boolean;
};

/** The bank account information. */
export type Bank = {
  /** The user-provided short name for the user's bank account. */
  nick_name?: string;
  /** The bank account number. */
  account_number: string;
  account_type: "CHECKING" | "SAVINGS";
  currency_code?: CurrencyCode;
  identifiers?: IdentifierList;
  /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
  branch_location?: {
    /** The first line of the address. For example, number or street. For example, `173 Drury Lane`. Required for data entry and compliance and risk checks. Must contain the full address. */
    address_line_1?: string;
    /** The second line of the address. For example, suite or apartment number. */
    address_line_2?: string;
    /** A city, town, or village. Smaller than `admin_area_level_1`. */
    admin_area_2?: string;
    /** The highest level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. Format for postal delivery. For example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A kanton.</li></ul> */
    admin_area_1?: string;
    /** The postal code, which is the zip code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
    postal_code?: string;
    country_code: CountryCode2;
  };
  mandate?: Mandate;
};

/** An array of banks attached to this managed account. */
export type BankList = Bank[];

/** Financial instruments attached to this account. */
export type FinancialInstruments = {
  banks?: BankList;
};

/** The currency and amount for a financial transaction, such as a balance or payment due. */
export type Money = {
  currency_code: CurrencyCode;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/docs/integration/direct/rest/currency-codes/). */
  value: string;
};

/** The currency range, from the minimum inclusive amount to the maximum inclusive amount. */
export type CurrencyRange = {
  minimum_amount?: Money;
  maximum_amount?: Money;
};

/** The name of the party. */
export type Name = {
  /** The prefix, or title, to the party's name. */
  prefix?: string;
  /** When the party is a person, the party's given, or first, name. */
  given_name?: string;
  /** When the party is a person, the party's surname or family name. Also known as the last name. Required when the party is a person. Use also to store multiple surnames including the matronymic, or mother's, surname. */
  surname?: string;
  /** When the party is a person, the party's middle name. Use also to store multiple middle names including the patronymic, or father's, middle name. */
  middle_name?: string;
  /** The suffix for the party's name. */
  suffix?: string;
  /** When the party is a person, the party's full name. */
  full_name?: string;
};

/** The preference details for offline onboarding without UI. */
export type OfflineOnboardingPreference = Record<string, never>;

/** The preference to customize the web experience of the customer by overriding that is set at the Partner's Account. */
export type PartnerConfigOverride = {
  /**
   * The URL to which to redirect the customer upon completion of the onboarding process.
   *
   * @format uri
   */
  return_url?: string;
  /** The description of the return URL. */
  return_url_description?: string;
  /** Indicates whether to show an add credit card page. */
  show_add_credit_card?: boolean;
};

/**
 * The percentage, as a fixed-point, signed decimal number. For example, define a 19.99% interest rate as `19.99`.
 *
 * @format ppaas_common_percentage_v2
 */
export type Percentage = string;

export type PersonAddressType = "HOME";

export type PersonAddressDetail =
  & AddressPortable
  & unknown
  & /** A simple postal address with coarse-grained fields. */ Record<string, never>;

/** The list of addresses associated with the person. */
export type PersonAddressDetailList = PersonAddressDetail[];

export type PersonDocumentType =
  | "SOCIAL_SECURITY_NUMBER"
  | "EMPLOYMENT_IDENTIFICATION_NUMBER"
  | "TAX_IDENTIFICATION_NUMBER"
  | "PASSPORT_NUMBER"
  | "PENSION_FUND_ID"
  | "MEDICAL_INSURANCE_ID"
  | "CNPJ"
  | "CPF"
  | "PAN"
  | "NATIONAL_ID_CARD";

export type PersonDocument = Document & unknown & unknown;

/** A person's or party's related document data collected from the customer. For example SSN, ITIN, or business registration number collected from the user. <blockquote><strong>Note:</strong> This field is not applicable for POST [/v2/customer/partner-referrals](/docs/api/partner-referrals/v2/#partner-referrals_create) API calls.</blockquote> */
export type PersonDocumentList = PersonDocument[];

export type PersonNameType = "LEGAL";

export type PersonName = Name & unknown & /** The name of the person. */ Record<string, never>;

/** The name of the person. */
export type PersonNameList = PersonName[];

/** The phone number in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export type Phone = {
  /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  country_code: string;
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
  /** The extension number. */
  extension_number?: string;
};

export type BusinessPhoneDetail =
  & Phone
  & unknown
  & /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */ Record<
    string,
    never
  >;

/** List of phone number associated with the business. */
export type BusinessPhoneDetailList = BusinessPhoneDetail[];

export type Business = unknown;

export type BusinessBeneficialOwner = Business & unknown & unknown;

/** Business beneficial owners. */
export type BusinessBeneficialOwnerList = BusinessBeneficialOwner[];

export type PhoneType = "FAX" | "HOME" | "MOBILE" | "OTHER" | "PAGER";

export type PersonPhoneDetail =
  & Phone
  & unknown
  & /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */ Record<
    string,
    never
  >;

/** The list of phone numbers associated with the person. */
export type PersonPhoneDetailList = PersonPhoneDetail[];

export type Person = unknown;

export type IndividualBeneficialOwner = Person & unknown & unknown;

/** Individual beneficial owners. */
export type IndividualBeneficialOwnerList = IndividualBeneficialOwner[];

/** Beneficial owners of the entity. */
export type BeneficialOwners = {
  individual_beneficial_owners?: IndividualBeneficialOwnerList;
  business_beneficial_owners?: BusinessBeneficialOwnerList;
};

export type IndividualOwner = Person & unknown & unknown;

/** List of owners in the account. There should be only one primary account owner which is mentioned in their role_type. */
export type IndividualOwnerList = IndividualOwner[];

export type Product =
  | "ALIPAY"
  | "BANCONTACT"
  | "BLIK"
  | "EPS"
  | "PPCP"
  | "EXPRESS_CHECKOUT"
  | "PAYMENT_METHODS"
  | "ADVANCED_VAULTING"
  | "IDEAL"
  | "MB_WAY"
  | "MULTIBANCO"
  | "PAYPAL_COMMERCE_PLATFORM_BUSINESS"
  | "PPPLUS"
  | "PRZELEWY24"
  | "SATISPAY"
  | "TRUSTLY"
  | "WECHAT_PAY"
  | "WEBSITE_PAYMENT_PRO"
  | "ZETTLE"
  | "PAYPAL_COMMERCE_PLATFORM_BUSINESS"
  | "HYPERWALLET_PAYOUTS";

/** An array of PayPal products to which the partner wants to onboard the customer. */
export type ProductList = Product[];

export type PurposeCodeEnum =
  | "P0104"
  | "P0301"
  | "P0801"
  | "P0802"
  | "P0803"
  | "P0805"
  | "P0806"
  | "P0902"
  | "P1004"
  | "P1005"
  | "P1006"
  | "P1007"
  | "P1008"
  | "P1009";

export type BusinessEntity = Business & unknown & unknown & unknown;

/** Common account object to hold the account related details of the customer. */
export type Account = {
  individual_owners?: IndividualOwnerList;
  business_entity?: BusinessEntity;
};

/** An array of request-related [HATEOAS links](/docs/api/overview/#hateoas-links). */
export type ReferralDataResponseDefinitionsLinkDescriptionList = LinkDescription[];

export type RestEndpointFeaturesEnum =
  | "PAYOUTS"
  | "PAYMENT"
  | "REFUND"
  | "FUTURE_PAYMENT"
  | "DIRECT_PAYMENT"
  | "PARTNER_FEE"
  | "DELAY_FUNDS_DISBURSEMENT"
  | "READ_SELLER_DISPUTE"
  | "UPDATE_SELLER_DISPUTE"
  | "ADVANCED_TRANSACTIONS_SEARCH"
  | "SWEEP_FUNDS_EXTERNAL_SINK"
  | "ACCESS_MERCHANT_INFORMATION"
  | "TRACKING_SHIPMENT_READWRITE"
  | "INVOICE_READ_WRITE"
  | "DISPUTE_READ_BUYER"
  | "UPDATE_CUSTOMER_DISPUTES"
  | "VAULT"
  | "BILLING_AGREEMENT"
  | "WITHDRAWALS"
  | "LINKED_FINANCIAL_INSTRUMENTS"
  | "USER_PROFILE"
  | "TRANSACTION_RISK_DATA"
  | "PAYPAL_BALANCE"
  | "EXCHANGE_CURRENCY";

/** An array of features that partner can access, or use, in PayPal on behalf of the seller. The seller grants permission for these features to the partner. */
export type RestApiIntegrationRestEndpointFeaturesEnumList = RestEndpointFeaturesEnum[];

/** An array of features that partner can access, or use, in PayPal on behalf of the seller. The seller grants permission for these features to the partner. */
export type RestEndpointFeaturesEnumList = RestEndpointFeaturesEnum[];

/** The integration details for PayPal first party REST endpoints. */
export type FirstPartyDetails = {
  features: RestEndpointFeaturesEnumList;
  /** S256 - The code verifier must be high-entropy cryptographic random string with a byte length of 43-128 range. */
  seller_nonce: string;
};

export type SignupMode = "VERIFY_WITH_PAYPAL";

/** The integration details for PayPal REST endpoints. */
export type ThirdPartyDetails = {
  features?: RestApiIntegrationRestEndpointFeaturesEnumList;
  signup_mode?: SignupMode;
  /** The organization to which the account will belong in the partner’s hierarchy, as a path. */
  organization?: string;
};

/** The integration details for PayPal REST endpoints. */
export type RestApiIntegration = {
  integration_method?: "BRAINTREE" | "PAYPAL" | "SDK";
  integration_type: "FIRST_PARTY" | "THIRD_PARTY";
  first_party_details?: FirstPartyDetails;
  third_party_details?: ThirdPartyDetails;
};

/** The integration details for the partner and customer relationship. Required if `operation` is `API_INTEGRATION`. */
export type IntegrationDetails = {
  classic_api_integration?: ClassicApiIntegration;
  rest_api_integration?: RestApiIntegration;
};

/** The required operation to share data. */
export type Operation = {
  operation:
    | "API_INTEGRATION"
    | "BANK_ADDITION"
    | "BILLING_AGREEMENT"
    | "CONTEXTUAL_MARKETING_CONSENT"
    | "OFFLINE_ONBOARDING"
    | "SALESFORCE_REFERRAL";
  api_integration_preference?: IntegrationDetails;
  offline_onboarding_preference?: OfflineOnboardingPreference;
  billing_agreement?: BillingAgreement;
};

/** An array of operations to perform for the customer while they share their data. */
export type OperationList = Operation[];

/** Requested transfer method and currency for a country. */
export type TransferMethod = {
  transfer_method_type?: "BANK_ACCOUNT" | "PAYPAL" | "VENMO" | "WIRE_ACCOUNT";
  currencies?: CurrencyCodeList;
};

/** Requested transfer method and currency for a country. */
export type TransferMethodList = TransferMethod[];

/** Requested country, transfer method and currency. */
export type CountryTransferMethodCurrencySelection = {
  country?: CountryCode;
  transfer_methods?: TransferMethodList;
};

/** Requested country, transfer method and currency. */
export type CountryTransferMethodCurrencySelectionList = CountryTransferMethodCurrencySelection[];

/** Payout specific attributes. */
export type PayoutAttributes = {
  /** If `true`, specifies that the merchant or platform is offering goods or services on behalf of 3rd party sellers. */
  marketplace?: boolean;
  /** If `true`, specifies that the Kyc is required for the merchant. */
  kyc_required?: boolean;
  country_transfer_method_currency_selection?: CountryTransferMethodCurrencySelectionList;
};

export type ReferralData = Account & unknown;

/** The share referral data response. */
export type ReferralDataResponse = {
  /** The ID to access the customer's data shared by the partner with PayPal. */
  partner_referral_id?: string;
  /** The payer ID of the partner who shared the referral data. */
  submitter_payer_id?: string;
  /** The client ID of the partner who shared the referral data. This cliend ID will be returned only when the caller is determined as an internal partner, which means the scope of the caller's security context only have "https://uri.paypal.com/services/customer/partner-referrals", For more information, see <a href="https://github.paypal.com/Customers-R/partnselleronbnodeserv/blob/b0a631c43e80a4dd43d34c01f479c272b3287796/src/helpers/securityContextHelpers.js#L143">Api Actor type in Typhoon service</a>. */
  submitter_client_id?: string;
  referral_data?: ReferralData;
  links?: ReferralDataResponseDefinitionsLinkDescriptionList;
};

export const _400Schema: z.ZodType<_400> = z.unknown();

export const _401Schema: z.ZodType<_401> = z.unknown();

export const _403Schema: z.ZodType<_403> = z.unknown();

export const _422Schema: z.ZodType<_422> = z.unknown();

export const AddressDetailsSchema: z.ZodType<AddressDetails> = z.object({
  street_number: z.string().max(100).optional(),
  street_name: z.string().max(100).optional(),
  street_type: z.string().max(100).optional(),
  delivery_service: z.string().max(100).optional(),
  building_name: z.string().max(100).optional(),
  sub_building: z.string().max(100).optional(),
});

export const BillingExperiencePreferenceSchema: z.ZodType<BillingExperiencePreference> = z.object({
  experience_id: z.string().regex(new RegExp("^[a-zA-Z0-9-]+$")).min(1).max(20).optional(),
  billing_context_set: z.boolean().optional(),
}).strict();

export const BillingAgreementSchema: z.ZodType<BillingAgreement> = z.object({
  description: z.string().regex(new RegExp("^.+$")).min(1).max(125).optional(),
  billing_experience_preference: BillingExperiencePreferenceSchema.optional(),
  merchant_custom_data: z.string().regex(new RegExp("^[a-zA-Z0-9-]+$")).min(1).max(125).optional(),
  approval_url: z.string().min(1).max(125).optional(),
  ec_token: z.string().regex(new RegExp("^[0-9A-Z_-]+$")).min(1).max(50).optional(),
}).strict();

export const BusinessAddressTypeSchema: z.ZodType<BusinessAddressType> = z.enum(["WORK"]);

export const BusinessDocumentTypeSchema: z.ZodType<BusinessDocumentType> = z.enum([
  "SOCIAL_SECURITY_NUMBER",
  "EMPLOYMENT_IDENTIFICATION_NUMBER",
  "TAX_IDENTIFICATION_NUMBER",
  "PASSPORT_NUMBER",
  "PENSION_FUND_ID",
  "MEDICAL_INSURANCE_ID",
  "CNPJ",
  "CPF",
  "PAN",
  "BUSINESS_REGISTRATION",
]);

export const BusinessIndustrySchema: z.ZodType<BusinessIndustry> = z.object({
  category: z.string().regex(new RegExp("^\\d+$")).min(1).max(20),
  mcc_code: z.string().regex(new RegExp("^\\d+$")).min(1).max(20),
  subcategory: z.string().regex(new RegExp("^\\d+$")).min(1).max(20),
});

export const BusinessNameSchema: z.ZodType<BusinessName> = z.object({
  business_name: z.string().max(300).optional(),
});

export const BusinessNameTypeSchema: z.ZodType<BusinessNameType> = z.enum(["DOING_BUSINESS_AS", "LEGAL_NAME"]);

export const BusinessNameDetailSchema: z.ZodType<BusinessNameDetail> = BusinessNameSchema.and(z.unknown()).and(
  z.object({}),
);

export const BusinessNameDetailListSchema: z.ZodType<BusinessNameDetailList> = z.array(BusinessNameDetailSchema).min(0)
  .max(5);

export const BusinessPhoneTypeSchema: z.ZodType<BusinessPhoneType> = z.enum(["CUSTOMER_SERVICE", "BUSINESS"]);

export const BusinessSubTypeSchema: z.ZodType<BusinessSubType> = z.enum([
  "ASSO_TYPE_INCORPORATED",
  "ASSO_TYPE_NON_INCORPORATED",
  "GOVT_TYPE_ENTITY",
  "GOVT_TYPE_EMANATION",
  "GOVT_TYPE_ESTD_COMM",
  "GOVT_TYPE_ESTD_FC",
  "GOVT_TYPE_ESTD_ST_TR",
]);

export const BusinessTypeSchema: z.ZodType<BusinessType> = z.enum([
  "ANY_OTHER_BUSINESS_ENTITY",
  "ASSOCIATION",
  "CORPORATION",
  "GENERAL_PARTNERSHIP",
  "GOVERNMENT",
  "INDIVIDUAL",
  "LIMITED_LIABILITY_PARTNERSHIP",
  "LIMITED_LIABILITY_PROPRIETORS",
  "LIMITED_LIABILITY_PRIVATE_CORPORATION",
  "LIMITED_PARTNERSHIP",
  "LIMITED_PARTNERSHIP_PRIVATE_CORPORATION",
  "NONPROFIT",
  "ONLY_BUY_OR_SEND_MONEY",
  "OTHER_CORPORATE_BODY",
  "PARTNERSHIP",
  "PRIVATE_PARTNERSHIP",
  "PROPRIETORSHIP",
  "PROPRIETORSHIP_CRAFTSMAN",
  "PROPRIETORY_COMPANY",
  "PRIVATE_CORPORATION",
  "PUBLIC_COMPANY",
  "PUBLIC_CORPORATION",
  "PUBLIC_PARTNERSHIP",
  "REGISTERED_COOPERATIVE",
]);

export const BusinessTypeInfoSchema: z.ZodType<BusinessTypeInfo> = z.object({
  type: BusinessTypeSchema.optional(),
  subtype: BusinessSubTypeSchema.optional(),
});

export const CapabilitiesSchema: z.ZodType<Capabilities> = z.enum([
  "PAYPAL_WALLET_VAULTING_ADVANCED",
  "PAY_UPON_INVOICE",
  "APPLE_PAY",
  "GOOGLE_PAY",
]);

export const CapabilitiesListSchema: z.ZodType<CapabilitiesList> = z.array(CapabilitiesSchema).min(1).max(5);

export const ClassicApiIntegrationSchema: z.ZodType<ClassicApiIntegration> = z.object({});

export const CountryCodeSchema: z.ZodType<CountryCode> = z.string().regex(new RegExp("^([A-Z]{2}|C2)$")).min(2).max(2);

export const AddressPortableSchema: z.ZodType<AddressPortable> = z.object({
  address_line_1: z.string().max(300).optional(),
  address_line_2: z.string().max(300).optional(),
  address_line_3: z.string().max(100).optional(),
  admin_area_4: z.string().max(100).optional(),
  admin_area_3: z.string().max(100).optional(),
  admin_area_2: z.string().max(120).optional(),
  admin_area_1: z.string().max(300).optional(),
  postal_code: z.string().max(60).optional(),
  country_code: CountryCodeSchema,
  address_details: AddressDetailsSchema.optional(),
});

export const BusinessAddressDetailSchema: z.ZodType<BusinessAddressDetail> = AddressPortableSchema.and(z.unknown()).and(
  z.object({}),
);

export const BusinessAddressDetailListSchema: z.ZodType<BusinessAddressDetailList> = z.array(
  BusinessAddressDetailSchema,
).min(0).max(5);

export const CountryCode2Schema: z.ZodType<CountryCode2> = z.string().regex(new RegExp("^([A-Z]{2}|C2)$")).min(2).max(
  2,
);

export const CurrencyCodeSchema: z.ZodType<CurrencyCode> = z.string().min(3).max(3);

export const CurrencyCode2Schema: z.ZodType<CurrencyCode2> = z.string().min(3).max(3);

export const CurrencyCodeListSchema: z.ZodType<CurrencyCodeList> = z.array(CurrencyCode2Schema).min(1).max(50);

export const DateNoTimeSchema: z.ZodType<DateNoTime> = z.string().regex(
  new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])$"),
).min(10).max(10);

export const BirthDetailsSchema: z.ZodType<BirthDetails> = z.object({
  date_of_birth: DateNoTimeSchema,
});

export const DateTimeSchema: z.ZodType<DateTime> = z.string().regex(
  new RegExp(
    "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])[T,t]([0-1][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)([.][0-9]+)?([Zz]|[+-][0-9]{2}:[0-9]{2})$",
  ),
).min(20).max(64);

export const DependentProcessSchema: z.ZodType<DependentProcess> = z.unknown();

export const DependentProcessListSchema: z.ZodType<DependentProcessList> = z.array(DependentProcessSchema).min(1).max(
  5,
);

export const EmailAddressSchema: z.ZodType<EmailAddress> = z.string().regex(new RegExp("^.+@[^\"\\-].+$")).min(3).max(
  254,
);

export const EmailSchema: z.ZodType<Email> = z.object({
  type: z.enum(["CUSTOMER_SERVICE"]),
  email: EmailAddressSchema,
});

export const EmailListSchema: z.ZodType<EmailList> = z.array(EmailSchema).min(0).max(5);

export const ErrorLinkDescriptionSchema: z.ZodType<ErrorLinkDescription> = z.object({
  href: z.string().regex(new RegExp("^.*$")).min(0).max(20000),
  rel: z.string().regex(new RegExp("^.*$")).min(0).max(100),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "PATCH"]).optional(),
});

export const Error500Schema: z.ZodType<Error500> = z.object({
  name: z.enum(["INTERNAL_SERVER_ERROR"]).optional(),
  message: z.enum(["An internal server error occurred."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error503Schema: z.ZodType<Error503> = z.object({
  name: z.enum(["SERVICE_UNAVAILABLE"]).optional(),
  message: z.enum(["Service Unavailable."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorLocationSchema: z.ZodType<ErrorLocation> = z.enum(["body", "path", "query"]);

export const ErrorDetailsSchema: z.ZodType<ErrorDetails> = z.object({
  field: z.string().optional(),
  value: z.string().optional(),
  location: ErrorLocationSchema.optional(),
  issue: z.string(),
  description: z.string().optional(),
});

export const Error400Schema: z.ZodType<Error400> = z.object({
  name: z.enum(["INVALID_REQUEST"]).optional(),
  message: z.enum(["Request is not well-formed, syntactically incorrect, or violates schema."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error401Schema: z.ZodType<Error401> = z.object({
  name: z.enum(["AUTHENTICATION_FAILURE"]).optional(),
  message: z.enum(["Authentication failed due to missing authorization header, or invalid authentication credentials."])
    .optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error403Schema: z.ZodType<Error403> = z.object({
  name: z.enum(["NOT_AUTHORIZED"]).optional(),
  message: z.enum(["Authorization failed due to insufficient permissions."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error404Schema: z.ZodType<Error404> = z.object({
  name: z.enum(["RESOURCE_NOT_FOUND"]).optional(),
  message: z.enum(["The specified resource does not exist."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error409Schema: z.ZodType<Error409> = z.object({
  name: z.enum(["RESOURCE_CONFLICT"]).optional(),
  message: z.enum(["The server has detected a conflict while processing this request."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error415Schema: z.ZodType<Error415> = z.object({
  name: z.enum(["UNSUPPORTED_MEDIA_TYPE"]).optional(),
  message: z.enum(["The server does not support the request payload's media type."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error422Schema: z.ZodType<Error422> = z.object({
  name: z.enum(["UNPROCESSABLE_ENTITY"]).optional(),
  message: z.enum([
    "The requested action could not be performed, semantically incorrect, or failed business validation.",
  ]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorDefaultSchema: z.ZodType<ErrorDefault> = z.union([
  Error400Schema,
  Error401Schema,
  Error403Schema,
  Error404Schema,
  Error409Schema,
  Error415Schema,
  Error422Schema,
  Error500Schema,
  Error503Schema,
]);

export const FileReferenceSchema: z.ZodType<FileReference> = z.object({
  id: z.string().min(1).max(255).optional(),
  reference_url: z.string().min(1).max(2000).optional(),
  content_type: z.string().optional(),
  create_time: DateTimeSchema.optional(),
  size: z.string().regex(new RegExp("^[0-9]+$")).optional(),
});

export const FileReferenceListSchema: z.ZodType<FileReferenceList> = z.array(FileReferenceSchema).min(1).max(50);

export const IdentifierSchema: z.ZodType<Identifier> = z.object({
  type: z.enum([
    "BANK_CODE",
    "BI_CODE",
    "BRANCH_CODE",
    "ROUTING_NUMBER_1",
    "ROUTING_NUMBER_2",
    "ROUTING_NUMBER_3",
    "SWIFT_CODE",
    "BRANCH_CODE",
    "INTERMEDIARY_SWIFT_CODE",
    "BBAN",
    "BBAN_ENCRYPTED",
    "BBAN_HMAC",
    "AGGREGATOR_YODLEE",
  ]).optional(),
  value: z.string().regex(new RegExp("^[A-Za-z0-9-_.+/ =]+")).min(1).max(125).optional(),
}).strict();

export const IdentifierListSchema: z.ZodType<IdentifierList> = z.array(IdentifierSchema).min(0).max(20);

export const IndividualOwnerTypeSchema: z.ZodType<IndividualOwnerType> = z.enum(["PRIMARY"]);

export const LabelsSchema: z.ZodType<Labels> = z.string();

export const LabelsListSchema: z.ZodType<LabelsList> = z.array(LabelsSchema).min(1).max(50);

export const LanguageSchema: z.ZodType<Language> = z.string().regex(
  new RegExp("^[a-z]{2}(?:-[A-Z][a-z]{3})?(?:-(?:[A-Z]{2}))?$"),
).min(2).max(10);

export const LegalConsentSchema: z.ZodType<LegalConsent> = z.object({
  type: z.enum(["SHARE_DATA_CONSENT"]),
  granted: z.boolean(),
});

export const LegalConsentListSchema: z.ZodType<LegalConsentList> = z.array(LegalConsentSchema).min(1).max(5);

export const LinkDescriptionSchema: z.ZodType<LinkDescription> = z.object({
  href: z.string(),
  rel: z.string(),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "HEAD", "CONNECT", "OPTIONS", "PATCH"]).optional(),
});

export const DefinitionsLinkDescriptionListSchema: z.ZodType<DefinitionsLinkDescriptionList> = z.array(
  LinkDescriptionSchema,
).min(1).max(10);

export const CreateReferralDataResponseSchema: z.ZodType<CreateReferralDataResponse> = z.object({
  links: DefinitionsLinkDescriptionListSchema.optional(),
});

export const LinkDescriptionListSchema: z.ZodType<LinkDescriptionList> = z.array(LinkDescriptionSchema).min(1).max(10);

export const DocumentSchema: z.ZodType<Document> = z.object({
  id: z.string().regex(new RegExp("^[0-9A-Z]+$")).min(1).max(20).optional(),
  labels: LabelsListSchema.optional(),
  name: z.string().regex(new RegExp("^[0-9A-Za-z_-]+$")).min(1).max(100).optional(),
  identification_number: z.string().regex(new RegExp("^[a-zA-Z0-9-]+$")).min(1).max(100).optional(),
  issue_date: DateNoTimeSchema.optional(),
  expiry_date: DateNoTimeSchema.optional(),
  issuing_country_code: CountryCodeSchema.optional(),
  files: FileReferenceListSchema.optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const BusinessDocumentSchema: z.ZodType<BusinessDocument> = DocumentSchema.and(z.unknown()).and(z.unknown());

export const BusinessDocumentListSchema: z.ZodType<BusinessDocumentList> = z.array(BusinessDocumentSchema).min(0).max(
  20,
);

export const MandateSchema: z.ZodType<Mandate> = z.object({
  accepted: z.boolean(),
}).strict();

export const BankSchema: z.ZodType<Bank> = z.object({
  nick_name: z.string().regex(new RegExp("^[0-9A-Za-z_-]+$")).min(1).max(50).optional(),
  account_number: z.string().regex(new RegExp("\\d+")).min(1).max(50),
  account_type: z.enum(["CHECKING", "SAVINGS"]),
  currency_code: CurrencyCodeSchema.optional(),
  identifiers: IdentifierListSchema.optional(),
  branch_location: z.object({
    address_line_1: z.string().max(300).optional(),
    address_line_2: z.string().max(300).optional(),
    admin_area_2: z.string().max(120).optional(),
    admin_area_1: z.string().max(300).optional(),
    postal_code: z.string().max(60).optional(),
    country_code: CountryCode2Schema,
  }).optional(),
  mandate: MandateSchema.optional(),
}).strict();

export const BankListSchema: z.ZodType<BankList> = z.array(BankSchema).min(0).max(5);

export const FinancialInstrumentsSchema: z.ZodType<FinancialInstruments> = z.object({
  banks: BankListSchema.optional(),
}).strict();

export const MoneySchema: z.ZodType<Money> = z.object({
  currency_code: CurrencyCodeSchema,
  value: z.string().regex(new RegExp("^((-?[0-9]+)|(-?([0-9]+)?[.][0-9]+))$")).max(32),
});

export const CurrencyRangeSchema: z.ZodType<CurrencyRange> = z.object({
  minimum_amount: MoneySchema.optional(),
  maximum_amount: MoneySchema.optional(),
}).strict();

export const NameSchema: z.ZodType<Name> = z.object({
  prefix: z.string().max(140).optional(),
  given_name: z.string().max(140).optional(),
  surname: z.string().max(140).optional(),
  middle_name: z.string().max(140).optional(),
  suffix: z.string().max(140).optional(),
  full_name: z.string().max(300).optional(),
});

export const OfflineOnboardingPreferenceSchema: z.ZodType<OfflineOnboardingPreference> = z.object({});

export const PartnerConfigOverrideSchema: z.ZodType<PartnerConfigOverride> = z.object({
  return_url: z.string().min(1).max(127).optional(),
  return_url_description: z.string().regex(new RegExp("^.+$")).min(1).max(127).optional(),
  show_add_credit_card: z.boolean().optional(),
}).strict();

export const PercentageSchema: z.ZodType<Percentage> = z.string().regex(
  new RegExp("^((-?[0-9]+)|(-?([0-9]+)?[.][0-9]+))$"),
);

export const PersonAddressTypeSchema: z.ZodType<PersonAddressType> = z.enum(["HOME"]);

export const PersonAddressDetailSchema: z.ZodType<PersonAddressDetail> = AddressPortableSchema.and(z.unknown()).and(
  z.object({}),
);

export const PersonAddressDetailListSchema: z.ZodType<PersonAddressDetailList> = z.array(PersonAddressDetailSchema).min(
  0,
).max(5);

export const PersonDocumentTypeSchema: z.ZodType<PersonDocumentType> = z.enum([
  "SOCIAL_SECURITY_NUMBER",
  "EMPLOYMENT_IDENTIFICATION_NUMBER",
  "TAX_IDENTIFICATION_NUMBER",
  "PASSPORT_NUMBER",
  "PENSION_FUND_ID",
  "MEDICAL_INSURANCE_ID",
  "CNPJ",
  "CPF",
  "PAN",
  "NATIONAL_ID_CARD",
]);

export const PersonDocumentSchema: z.ZodType<PersonDocument> = DocumentSchema.and(z.unknown()).and(z.unknown());

export const PersonDocumentListSchema: z.ZodType<PersonDocumentList> = z.array(PersonDocumentSchema).min(0).max(20);

export const PersonNameTypeSchema: z.ZodType<PersonNameType> = z.enum(["LEGAL"]);

export const PersonNameSchema: z.ZodType<PersonName> = NameSchema.and(z.unknown()).and(z.object({}));

export const PersonNameListSchema: z.ZodType<PersonNameList> = z.array(PersonNameSchema).min(0).max(5);

export const PhoneSchema: z.ZodType<Phone> = z.object({
  country_code: z.string().regex(new RegExp("^[0-9]{1,3}?$")).min(1).max(3),
  national_number: z.string().regex(new RegExp("^[0-9]{1,14}?$")).min(1).max(14),
  extension_number: z.string().regex(new RegExp("^[0-9]{1,15}?$")).min(1).max(15).optional(),
});

export const BusinessPhoneDetailSchema: z.ZodType<BusinessPhoneDetail> = PhoneSchema.and(z.unknown()).and(z.object({}));

export const BusinessPhoneDetailListSchema: z.ZodType<BusinessPhoneDetailList> = z.array(BusinessPhoneDetailSchema).min(
  0,
).max(5);

export const BusinessSchema: z.ZodType<Business> = z.unknown();

export const BusinessBeneficialOwnerSchema: z.ZodType<BusinessBeneficialOwner> = BusinessSchema.and(z.unknown()).and(
  z.unknown(),
);

export const BusinessBeneficialOwnerListSchema: z.ZodType<BusinessBeneficialOwnerList> = z.array(
  BusinessBeneficialOwnerSchema,
).min(0).max(5);

export const PhoneTypeSchema: z.ZodType<PhoneType> = z.enum(["FAX", "HOME", "MOBILE", "OTHER", "PAGER"]);

export const PersonPhoneDetailSchema: z.ZodType<PersonPhoneDetail> = PhoneSchema.and(z.unknown()).and(z.object({}));

export const PersonPhoneDetailListSchema: z.ZodType<PersonPhoneDetailList> = z.array(PersonPhoneDetailSchema).min(0)
  .max(5);

export const PersonSchema: z.ZodType<Person> = z.unknown();

export const IndividualBeneficialOwnerSchema: z.ZodType<IndividualBeneficialOwner> = PersonSchema.and(z.unknown()).and(
  z.unknown(),
);

export const IndividualBeneficialOwnerListSchema: z.ZodType<IndividualBeneficialOwnerList> = z.array(
  IndividualBeneficialOwnerSchema,
).min(0).max(5);

export const BeneficialOwnersSchema: z.ZodType<BeneficialOwners> = z.object({
  individual_beneficial_owners: IndividualBeneficialOwnerListSchema.optional(),
  business_beneficial_owners: BusinessBeneficialOwnerListSchema.optional(),
});

export const IndividualOwnerSchema: z.ZodType<IndividualOwner> = PersonSchema.and(z.unknown()).and(z.unknown());

export const IndividualOwnerListSchema: z.ZodType<IndividualOwnerList> = z.array(IndividualOwnerSchema).min(0).max(2);

export const ProductSchema: z.ZodType<Product> = z.enum([
  "ALIPAY",
  "BANCONTACT",
  "BLIK",
  "EPS",
  "PPCP",
  "EXPRESS_CHECKOUT",
  "PAYMENT_METHODS",
  "ADVANCED_VAULTING",
  "IDEAL",
  "MB_WAY",
  "MULTIBANCO",
  "PAYPAL_COMMERCE_PLATFORM_BUSINESS",
  "PPPLUS",
  "PRZELEWY24",
  "SATISPAY",
  "TRUSTLY",
  "WECHAT_PAY",
  "WEBSITE_PAYMENT_PRO",
  "ZETTLE",
  "PAYPAL_COMMERCE_PLATFORM_BUSINESS",
  "HYPERWALLET_PAYOUTS",
]);

export const ProductListSchema: z.ZodType<ProductList> = z.array(ProductSchema).min(1).max(5);

export const PurposeCodeEnumSchema: z.ZodType<PurposeCodeEnum> = z.enum([
  "P0104",
  "P0301",
  "P0801",
  "P0802",
  "P0803",
  "P0805",
  "P0806",
  "P0902",
  "P1004",
  "P1005",
  "P1006",
  "P1007",
  "P1008",
  "P1009",
]);

export const BusinessEntitySchema: z.ZodType<BusinessEntity> = BusinessSchema.and(z.unknown()).and(z.unknown()).and(
  z.unknown(),
);

export const AccountSchema: z.ZodType<Account> = z.object({
  individual_owners: IndividualOwnerListSchema.optional(),
  business_entity: BusinessEntitySchema.optional(),
});

export const ReferralDataResponseDefinitionsLinkDescriptionListSchema: z.ZodType<
  ReferralDataResponseDefinitionsLinkDescriptionList
> = z.array(LinkDescriptionSchema).min(0).max(2);

export const RestEndpointFeaturesEnumSchema: z.ZodType<RestEndpointFeaturesEnum> = z.enum([
  "PAYOUTS",
  "PAYMENT",
  "REFUND",
  "FUTURE_PAYMENT",
  "DIRECT_PAYMENT",
  "PARTNER_FEE",
  "DELAY_FUNDS_DISBURSEMENT",
  "READ_SELLER_DISPUTE",
  "UPDATE_SELLER_DISPUTE",
  "ADVANCED_TRANSACTIONS_SEARCH",
  "SWEEP_FUNDS_EXTERNAL_SINK",
  "ACCESS_MERCHANT_INFORMATION",
  "TRACKING_SHIPMENT_READWRITE",
  "INVOICE_READ_WRITE",
  "DISPUTE_READ_BUYER",
  "UPDATE_CUSTOMER_DISPUTES",
  "VAULT",
  "BILLING_AGREEMENT",
  "WITHDRAWALS",
  "LINKED_FINANCIAL_INSTRUMENTS",
  "USER_PROFILE",
  "TRANSACTION_RISK_DATA",
  "PAYPAL_BALANCE",
  "EXCHANGE_CURRENCY",
]);

export const RestApiIntegrationRestEndpointFeaturesEnumListSchema: z.ZodType<
  RestApiIntegrationRestEndpointFeaturesEnumList
> = z.array(RestEndpointFeaturesEnumSchema).min(0).max(20);

export const RestEndpointFeaturesEnumListSchema: z.ZodType<RestEndpointFeaturesEnumList> = z.array(
  RestEndpointFeaturesEnumSchema,
);

export const FirstPartyDetailsSchema: z.ZodType<FirstPartyDetails> = z.object({
  features: RestEndpointFeaturesEnumListSchema,
  seller_nonce: z.string().regex(new RegExp("^[a-zA-Z0-9-_:]+$")).min(44).max(128),
});

export const SignupModeSchema: z.ZodType<SignupMode> = z.enum(["VERIFY_WITH_PAYPAL"]);

export const ThirdPartyDetailsSchema: z.ZodType<ThirdPartyDetails> = z.object({
  features: RestApiIntegrationRestEndpointFeaturesEnumListSchema.optional(),
  signup_mode: SignupModeSchema.optional(),
  organization: z.string().regex(new RegExp("^.*$")).min(1).max(127).optional(),
});

export const RestApiIntegrationSchema: z.ZodType<RestApiIntegration> = z.object({
  integration_method: z.enum(["BRAINTREE", "PAYPAL", "SDK"]).optional(),
  integration_type: z.enum(["FIRST_PARTY", "THIRD_PARTY"]),
  first_party_details: FirstPartyDetailsSchema.optional(),
  third_party_details: ThirdPartyDetailsSchema.optional(),
});

export const IntegrationDetailsSchema: z.ZodType<IntegrationDetails> = z.object({
  classic_api_integration: ClassicApiIntegrationSchema.optional(),
  rest_api_integration: RestApiIntegrationSchema.optional(),
});

export const OperationSchema: z.ZodType<Operation> = z.object({
  operation: z.enum([
    "API_INTEGRATION",
    "BANK_ADDITION",
    "BILLING_AGREEMENT",
    "CONTEXTUAL_MARKETING_CONSENT",
    "OFFLINE_ONBOARDING",
    "SALESFORCE_REFERRAL",
  ]),
  api_integration_preference: IntegrationDetailsSchema.optional(),
  offline_onboarding_preference: OfflineOnboardingPreferenceSchema.optional(),
  billing_agreement: BillingAgreementSchema.optional(),
});

export const OperationListSchema: z.ZodType<OperationList> = z.array(OperationSchema).min(1).max(5);

export const TransferMethodSchema: z.ZodType<TransferMethod> = z.object({
  transfer_method_type: z.enum(["BANK_ACCOUNT", "PAYPAL", "VENMO", "WIRE_ACCOUNT"]).optional(),
  currencies: CurrencyCodeListSchema.optional(),
}).strict();

export const TransferMethodListSchema: z.ZodType<TransferMethodList> = z.array(TransferMethodSchema).min(1).max(50);

export const CountryTransferMethodCurrencySelectionSchema: z.ZodType<CountryTransferMethodCurrencySelection> = z.object(
  {
    country: CountryCodeSchema.optional(),
    transfer_methods: TransferMethodListSchema.optional(),
  },
).strict();

export const CountryTransferMethodCurrencySelectionListSchema: z.ZodType<CountryTransferMethodCurrencySelectionList> = z
  .array(CountryTransferMethodCurrencySelectionSchema).min(1).max(50);

export const PayoutAttributesSchema: z.ZodType<PayoutAttributes> = z.object({
  marketplace: z.boolean().optional(),
  kyc_required: z.boolean().optional(),
  country_transfer_method_currency_selection: CountryTransferMethodCurrencySelectionListSchema.optional(),
}).strict();

export const ReferralDataSchema: z.ZodType<ReferralData> = AccountSchema.and(z.unknown());

export const ReferralDataResponseSchema: z.ZodType<ReferralDataResponse> = z.object({
  partner_referral_id: z.string().regex(new RegExp("^[A-Za-z0-9+/=]+$")).min(1).max(255).optional(),
  submitter_payer_id: z.string().regex(new RegExp("^[0-9A-Z]+$")).min(1).max(20).optional(),
  submitter_client_id: z.string().regex(new RegExp("^[a-zA-Z0-9-_]+$")).min(1).max(255).optional(),
  referral_data: ReferralDataSchema.optional(),
  links: ReferralDataResponseDefinitionsLinkDescriptionListSchema.optional(),
});

/**
 * Create partner referral
 *
 * Creates a partner referral that is shared by the partner or API caller. The partner referral is used to onboard the seller, and contains the seller's personal, business, financial and operations.
 */
export const partnerReferralsCreate = (
  config: OpenAPIClientConfig,
  request: { body: ReferralData },
): Promise<
  OpenAPIResponses<{
    /** A successful request returns the HTTP `201 Created` status code and a JSON response body that contains a [HATEOAS link](/docs/api/overview/#hateoas-links) to show the referral data and an `action_url` to which you redirect the customer in a browser to complete the signup process. The `partner_referral_id` token is appended to the URL. */
    201: {
      body: CreateReferralDataResponse;
    };
    /** Request is not well-formed, syntactically incorrect, or violates schema. */
    400: {
      body: Error400 & _400;
    };
    /** The API call is not authorized. */
    401: {
      body: Error401 & _401;
    };
    /** Authorization failed due to insufficient permissions or missing scopes on the caller account. */
    403: {
      body: Error403 & _403;
    };
    /** The caller is not a business account or the account is closed or locked. */
    422: {
      body: Error422 & _422;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error500;
    };
    "default": {
      body: unknown;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v2/customer/partner-referrals",
      method: "post",
      requestSchema: z.object({
        body: ReferralDataSchema,
      }),
      responseSchema: {
        201: z.object({ body: CreateReferralDataResponseSchema }),
        400: z.object({ body: Error400Schema.and(_400Schema) }),
        401: z.object({ body: Error401Schema.and(_401Schema) }),
        403: z.object({ body: Error403Schema.and(_403Schema) }),
        422: z.object({ body: Error422Schema.and(_422Schema) }),
        500: z.object({ body: Error500Schema }),
        "default": z.object({ body: z.unknown() }),
      },
    },
    request,
  ) as any;

/**
 * Show referral data
 *
 * Shows details by ID for referral data that was shared by the partner or API caller.
 */
export const partnerReferralsRead = (config: OpenAPIClientConfig, request: {
  path: {
    /** The ID of the partner-referrals data for which to show details. */
    partner_referral_id: string;
  };
}): Promise<
  OpenAPIResponses<{
    /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows referral data. */
    200: {
      body: ReferralDataResponse;
    };
    /** The API call is not authorized. */
    401: {
      body: Error401 & _401;
    };
    /** Authorization failed due to insufficient permissions or missing scopes on the caller Account. */
    403: {
      body: Error403 & _403;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error500;
    };
    "default": {
      body: unknown;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v2/customer/partner-referrals/{partner_referral_id}",
      method: "get",
      requestSchema: z.object({
        path: z.object({
          partner_referral_id: z.string(),
        }),
      }),
      responseSchema: {
        200: z.object({ body: ReferralDataResponseSchema }),
        401: z.object({ body: Error401Schema.and(_401Schema) }),
        403: z.object({ body: Error403Schema.and(_403Schema) }),
        500: z.object({ body: Error500Schema }),
        "default": z.object({ body: z.unknown() }),
      },
    },
    request,
  ) as any;

export { createCustomClient } from "jsr:@brad-jones/deno-net-open-api-client@0.2.1";

/**
 * Create a client with all available operations.
 * This is convenient but will include all operations in your bundle.
 * For better tree-shaking, use createCustomClient() to select specific operations.
 */
export function createClient(config: OpenAPIClientConfig) {
  return createCustomClient(config, { partnerReferralsCreate, partnerReferralsRead });
}

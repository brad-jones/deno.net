// deno-lint-ignore-file no-explicit-any
// This file was auto-generated by @brad-jones/open-api-client

import {
  type OpenAPIClientConfig,
  openAPIFetch,
  type OpenAPIResponses,
} from "jsr:@brad-jones/deno-net-open-api-client@0.2.1";
import { z } from "npm:zod@^4.1.12";

export type Error = unknown;

/** The error details. Required for client-side `4XX` errors. */
export type ErrorDetails2 = {
  /** The field that caused the error. If this field is in the body, set this value to the field's JSON pointer value. Required for client-side errors. */
  field?: string;
  /** The value of the field that caused the error. */
  value?: string;
  /** The location of the field that caused the error. Value is `body`, `path`, or `query`. */
  location?: string;
  /** The unique, fine-grained application-level error code. */
  issue: string;
  /** The human-readable description for an issue. The description can change over the lifetime of an API, so clients must not depend on this value. */
  description?: string;
};

/** An array of additional details about the error. */
export type ErrorDetailsList = ErrorDetails2[];

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export type ErrorLinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "PATCH";
};

/** This is either a system or application error, and generally indicates that although the client appeared to provide a correct request, something unexpected has gone wrong on the server. */
export type Error500 = {
  name?: "INTERNAL_SERVER_ERROR";
  message?: "An internal server error occurred.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server is temporarily unable to handle the request, for example, because of planned maintenance or downtime. */
export type Error503 = {
  name?: "SERVICE_UNAVAILABLE";
  message?: "Service Unavailable.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

export type ErrorLocation = "body" | "path" | "query";

/** The error details. Required for client-side `4XX` errors. */
export type ErrorDetails = {
  /** The field that caused the error. If this field is in the body, set this value to the field's JSON pointer value. Required for client-side errors. */
  field?: string;
  /** The value of the field that caused the error. */
  value?: string;
  location?: ErrorLocation;
  /** The unique, fine-grained application-level error code. */
  issue: string;
  /** The human-readable description for an issue. The description can change over the lifetime of an API, so clients must not depend on this value. */
  description?: string;
};

/** Request is not well-formed, syntactically incorrect, or violates schema. */
export type Error400 = {
  name?: "INVALID_REQUEST";
  message?: "Request is not well-formed, syntactically incorrect, or violates schema.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** Authentication failed due to missing Authorization header, or invalid authentication credentials. */
export type Error401 = {
  name?: "AUTHENTICATION_FAILURE";
  message?: "Authentication failed due to missing authorization header, or invalid authentication credentials.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The client is not authorized to access this resource, although it may have valid credentials.  */
export type Error403 = {
  name?: "NOT_AUTHORIZED";
  message?: "Authorization failed due to insufficient permissions.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has not found anything matching the request URI. This either means that the URI is incorrect or the resource is not available. */
export type Error404 = {
  name?: "RESOURCE_NOT_FOUND";
  message?: "The specified resource does not exist.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has detected a conflict while processing this request. */
export type Error409 = {
  name?: "RESOURCE_CONFLICT";
  message?: "The server has detected a conflict while processing this request.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server does not support the request payload's media type. */
export type Error415 = {
  name?: "UNSUPPORTED_MEDIA_TYPE";
  message?: "The server does not support the request payload's media type.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The requested action cannot be performed and may require interaction with APIs or processes outside of the current request. This is distinct from a 500 response in that there are no systemic problems limiting the API from performing the request. */
export type Error422 = {
  name?: "UNPROCESSABLE_ENTITY";
  message?: "The requested action could not be performed, semantically incorrect, or failed business validation.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The type of webhook error that occurred. */
export type ErrorType = {
  /** The ID for the webhook error type. */
  id?: string;
  /** The unique error type name. */
  name: string;
  /** A human-readable description of the error type. */
  description?: string;
};

/** The event version in the webhook notification. */
export type EventVersion = string;

/** The request-related [HATEOAS link](/docs/api/reference/api-responses/#hateoas-links) information. */
export type LinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "HEAD" | "CONNECT" | "OPTIONS" | "PATCH";
};

/** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links/). */
export type DefinitionsLinkDescriptionList = LinkDescription[];

/** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links). */
export type LinkDescriptionList = LinkDescription[];

/** The error details. */
export type Error2 = {
  /** The human-readable, unique name of the error. */
  name: string;
  /** The message that describes the error. */
  message: string;
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id: string;
  /** The information link, or URI, that shows detailed information about this error for the developer. */
  information_link?: string;
  details?: ErrorDetailsList;
  links?: LinkDescriptionList;
};

/** The JSON patch object to apply partial updates to resources. */
export type Patch = {
  op: "add" | "remove" | "replace" | "move" | "copy" | "test";
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location at which to complete the operation. */
  path?: string;
  /** The value to apply. The <code>remove</code> operation does not require a value. */
  value?:
    | number
    | /** The value to apply. The <code>remove</code> operation does not require a value. */ number
    | /** The value to apply. The <code>remove</code> operation does not require a value. */ string
    | /** The value to apply. The <code>remove</code> operation does not require a value. */ boolean
    | /** The value to apply. The <code>remove</code> operation does not require a value. */ null
    | /** The value to apply. The <code>remove</code> operation does not require a value. */ unknown[]
    | /** The value to apply. The <code>remove</code> operation does not require a value. */ Record<string, never>;
  /** The <a href="https://tools.ietf.org/html/rfc6901">JSON Pointer</a> to the target document location from which to move the value. Required for the <code>move</code> operation. */
  from?: string;
};

/** An array of JSON patch objects to apply partial updates to resources. */
export type PatchRequest = Patch[];

/** The resource that triggered the webhook event notification. */
export type Resource = Record<string, unknown>;

/** The resource version in the webhook notification. */
export type ResourceVersion = string;

/** A webhook event notification. */
export type Event = {
  /** The ID of the webhook event notification. */
  id?: string;
  /**
   * The date and time when the webhook event notification was created, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6).
   *
   * @format date-time
   */
  create_time?: string;
  /** The name of the resource related to the webhook notification event. */
  resource_type?: string;
  event_version?: EventVersion;
  /** The event that triggered the webhook event notification. */
  event_type?: string;
  /** A summary description for the event notification. */
  summary?: string;
  resource_version?: ResourceVersion;
  resource?: Resource;
  links?: LinkDescriptionList;
};

/** An array of webhooks events. */
export type EventList = Event[];

/** A list of webhooks events. */
export type EventList_1 = {
  events?: EventList;
  /** The number of items in each range of results. Note that the response might have fewer items than the requested `page_size` value. */
  count?: number;
  links?: LinkDescriptionList;
};

/** The ID for an event type. For example, `1.0` or `2.0`. */
export type ResourceVersions = string;

/** Identifier for the event type example: 1.0/2.0 etc. */
export type ResourceVersionsList = ResourceVersions[];

/** An event type. */
export type EventType = {
  /** The unique event name.<blockquote><strong>Note:</strong> To subscribe to all events, including events as they are added, specify an `*` as the value to represent a wildcard.</blockquote> */
  name: string;
  /** A human-readable description of the event. */
  description?: string;
  /** The status of a webhook event. */
  status?: string;
  resource_versions?: ResourceVersionsList;
};

/** An array of events to which to subscribe your webhook. To subscribe to all events, including events as they are added, specify the asterisk wild card. To replace the `event_types` array, specify the asterisk wild card. To list all supported events, <a href="#event-type_list">list available events</a>. */
export type DefinitionsEventTypeList = EventType[];

/** An array of webhook events. */
export type EventTypeList = EventType[];

/** A list of webhook events. */
export type EventTypeList_1 = {
  event_types?: EventTypeList;
};

/** Simulates a mock webhook event. */
export type SimulateEvent = {
  /** The ID of the webhook. If omitted, the URL is required. */
  webhook_id?: string;
  /**
   * The URL for the webhook endpoint. If omitted, the webhook ID is required.
   *
   * @format uri
   */
  url?: string;
  /** The event name. Specify one of the subscribed events. For each request, provide only one event. */
  event_type: string;
  /** The identifier for event type ex: 1.0/2.0 etc. */
  resource_version?: string;
};

export type SwaggerLinkDescription = LinkDescription;

/** A verify webhook signature request. */
export type VerifyWebhookSignature = {
  /** The algorithm that PayPal uses to generate the signature and that you can use to verify the signature. Extract this value from the `PAYPAL-AUTH-ALGO` response header, which is received with the webhook notification. */
  auth_algo: string;
  /**
   * The X.509 public key certificate. Download the certificate from this URL and use it to verify the signature. Extract this value from the `PAYPAL-CERT-URL` response header, which is received with the webhook notification.
   *
   * @format uri
   */
  cert_url: string;
  /** The ID of the HTTP transmission. Contained in the `PAYPAL-TRANSMISSION-ID` header of the notification message. */
  transmission_id: string;
  /** The PayPal-generated asymmetric signature. Appears in the `PAYPAL-TRANSMISSION-SIG` header of the notification message. */
  transmission_sig: string;
  /**
   * The date and time of the HTTP transmission, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Appears in the `PAYPAL-TRANSMISSION-TIME` header of the notification message.
   *
   * @format date-time
   */
  transmission_time: string;
  /** The ID of the webhook as configured in your Developer Portal account. */
  webhook_id: string;
  webhook_event: Event;
};

/** The verify webhook signature response. */
export type VerifyWebhookSignatureResponse = {
  verification_status: "SUCCESS" | "FAILURE";
};

/** One or more webhook objects. */
export type Webhook = {
  /** The ID of the webhook. */
  id?: string;
  /**
   * The URL that is configured to listen on `localhost` for incoming `POST` notification messages that contain event information.
   *
   * @format uri
   */
  url: string;
  event_types: DefinitionsEventTypeList;
  links?: DefinitionsLinkDescriptionList;
};

/** The ID of the webhook event notification to resend. */
export type WebhookIds = string;

/** An array of webhook account IDs. */
export type WebhookIdsList = WebhookIds[];

/** Resends a webhook event notification, by ID. */
export type EventResend = {
  webhook_ids?: WebhookIdsList;
};

/** An array of webhooks. */
export type WebhookList = Webhook[];

/** A list of webhooks. */
export type WebhookList_1 = {
  webhooks?: WebhookList;
};

/** The webhook lookup details. */
export type WebhooksLookup = {
  /** The ID of the webhook lookup. */
  id?: string;
  /** The application client ID. */
  client_id?: string;
  links?: DefinitionsLinkDescriptionList;
};

/** An array of webhook lookups. */
export type WebhooksLookupList = WebhooksLookup[];

/** A list of webhook lookups. */
export type WebhookLookupList = {
  webhooks_lookups?: WebhooksLookupList;
};

export const ErrorSchema: z.ZodType<Error> = z.unknown();

export const ErrorDetails2Schema: z.ZodType<ErrorDetails2> = z.object({
  field: z.string().optional(),
  value: z.string().optional(),
  location: z.string().default("body").optional(),
  issue: z.string(),
  description: z.string().optional(),
});

export const ErrorDetailsListSchema: z.ZodType<ErrorDetailsList> = z.array(ErrorDetails2Schema);

export const ErrorLinkDescriptionSchema: z.ZodType<ErrorLinkDescription> = z.object({
  href: z.string().regex(new RegExp("^.*$")).min(0).max(20000),
  rel: z.string().regex(new RegExp("^.*$")).min(0).max(100),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "PATCH"]).optional(),
});

export const Error500Schema: z.ZodType<Error500> = z.object({
  name: z.enum(["INTERNAL_SERVER_ERROR"]).optional(),
  message: z.enum(["An internal server error occurred."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error503Schema: z.ZodType<Error503> = z.object({
  name: z.enum(["SERVICE_UNAVAILABLE"]).optional(),
  message: z.enum(["Service Unavailable."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorLocationSchema: z.ZodType<ErrorLocation> = z.enum(["body", "path", "query"]);

export const ErrorDetailsSchema: z.ZodType<ErrorDetails> = z.object({
  field: z.string().optional(),
  value: z.string().optional(),
  location: ErrorLocationSchema.optional(),
  issue: z.string(),
  description: z.string().optional(),
});

export const Error400Schema: z.ZodType<Error400> = z.object({
  name: z.enum(["INVALID_REQUEST"]).optional(),
  message: z.enum(["Request is not well-formed, syntactically incorrect, or violates schema."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error401Schema: z.ZodType<Error401> = z.object({
  name: z.enum(["AUTHENTICATION_FAILURE"]).optional(),
  message: z.enum(["Authentication failed due to missing authorization header, or invalid authentication credentials."])
    .optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error403Schema: z.ZodType<Error403> = z.object({
  name: z.enum(["NOT_AUTHORIZED"]).optional(),
  message: z.enum(["Authorization failed due to insufficient permissions."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error404Schema: z.ZodType<Error404> = z.object({
  name: z.enum(["RESOURCE_NOT_FOUND"]).optional(),
  message: z.enum(["The specified resource does not exist."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error409Schema: z.ZodType<Error409> = z.object({
  name: z.enum(["RESOURCE_CONFLICT"]).optional(),
  message: z.enum(["The server has detected a conflict while processing this request."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error415Schema: z.ZodType<Error415> = z.object({
  name: z.enum(["UNSUPPORTED_MEDIA_TYPE"]).optional(),
  message: z.enum(["The server does not support the request payload's media type."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error422Schema: z.ZodType<Error422> = z.object({
  name: z.enum(["UNPROCESSABLE_ENTITY"]).optional(),
  message: z.enum([
    "The requested action could not be performed, semantically incorrect, or failed business validation.",
  ]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorTypeSchema: z.ZodType<ErrorType> = z.object({
  id: z.string().optional(),
  name: z.string(),
  description: z.string().optional(),
});

export const EventVersionSchema: z.ZodType<EventVersion> = z.string().regex(new RegExp("^([0-9]+.[0-9]+)$"));

export const LinkDescriptionSchema: z.ZodType<LinkDescription> = z.object({
  href: z.string(),
  rel: z.string(),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "HEAD", "CONNECT", "OPTIONS", "PATCH"]).optional(),
});

export const DefinitionsLinkDescriptionListSchema: z.ZodType<DefinitionsLinkDescriptionList> = z.array(
  LinkDescriptionSchema,
);

export const LinkDescriptionListSchema: z.ZodType<LinkDescriptionList> = z.array(LinkDescriptionSchema);

export const Error2Schema: z.ZodType<Error2> = z.object({
  name: z.string(),
  message: z.string(),
  debug_id: z.string(),
  information_link: z.string().optional(),
  details: ErrorDetailsListSchema.optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const PatchSchema: z.ZodType<Patch> = z.object({
  op: z.enum(["add", "remove", "replace", "move", "copy", "test"]),
  path: z.string().optional(),
  value: z.union([z.number(), z.number().int(), z.string(), z.boolean(), z.null(), z.array(z.unknown()), z.object({})])
    .optional(),
  from: z.string().optional(),
});

export const PatchRequestSchema: z.ZodType<PatchRequest> = z.array(PatchSchema);

export const ResourceSchema: z.ZodType<Resource> = z.record(z.string(), z.unknown());

export const ResourceVersionSchema: z.ZodType<ResourceVersion> = z.string().regex(new RegExp("^([0-9]+.[0-9]+)$"));

export const EventSchema: z.ZodType<Event> = z.object({
  id: z.string().optional(),
  create_time: z.string().optional(),
  resource_type: z.string().optional(),
  event_version: EventVersionSchema.optional(),
  event_type: z.string().optional(),
  summary: z.string().optional(),
  resource_version: ResourceVersionSchema.optional(),
  resource: ResourceSchema.optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const EventListSchema: z.ZodType<EventList> = z.array(EventSchema);

export const EventList_1Schema: z.ZodType<EventList_1> = z.object({
  events: EventListSchema.optional(),
  count: z.number().int().optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const ResourceVersionsSchema: z.ZodType<ResourceVersions> = z.string();

export const ResourceVersionsListSchema: z.ZodType<ResourceVersionsList> = z.array(ResourceVersionsSchema);

export const EventTypeSchema: z.ZodType<EventType> = z.object({
  name: z.string(),
  description: z.string().optional(),
  status: z.string().optional(),
  resource_versions: ResourceVersionsListSchema.optional(),
});

export const DefinitionsEventTypeListSchema: z.ZodType<DefinitionsEventTypeList> = z.array(EventTypeSchema).max(500);

export const EventTypeListSchema: z.ZodType<EventTypeList> = z.array(EventTypeSchema);

export const EventTypeList_1Schema: z.ZodType<EventTypeList_1> = z.object({
  event_types: EventTypeListSchema.optional(),
});

export const SimulateEventSchema: z.ZodType<SimulateEvent> = z.object({
  webhook_id: z.string().regex(new RegExp("^[a-zA-Z0-9]+$")).max(50).optional(),
  url: z.string().max(2048).optional(),
  event_type: z.string().regex(new RegExp("^[a-zA-Z0-9.]+$")).max(50),
  resource_version: z.string().optional(),
});

export const SwaggerLinkDescriptionSchema: z.ZodType<SwaggerLinkDescription> = LinkDescriptionSchema;

export const VerifyWebhookSignatureSchema: z.ZodType<VerifyWebhookSignature> = z.object({
  auth_algo: z.string().regex(new RegExp("^[a-zA-Z0-9]+$")).max(100),
  cert_url: z.string().max(500),
  transmission_id: z.string().regex(new RegExp("^(?!\\d+$)\\w+\\S+")).max(50),
  transmission_sig: z.string().regex(new RegExp("^(?!\\d+$)\\w+\\S+")).max(500),
  transmission_time: z.string().max(100),
  webhook_id: z.string().regex(new RegExp("^[a-zA-Z0-9]+$")).max(50),
  webhook_event: EventSchema,
});

export const VerifyWebhookSignatureResponseSchema: z.ZodType<VerifyWebhookSignatureResponse> = z.object({
  verification_status: z.enum(["SUCCESS", "FAILURE"]),
});

export const WebhookSchema: z.ZodType<Webhook> = z.object({
  id: z.string().optional(),
  url: z.string().max(2048),
  event_types: DefinitionsEventTypeListSchema,
  links: DefinitionsLinkDescriptionListSchema.optional(),
});

export const WebhookIdsSchema: z.ZodType<WebhookIds> = z.string();

export const WebhookIdsListSchema: z.ZodType<WebhookIdsList> = z.array(WebhookIdsSchema).max(500);

export const EventResendSchema: z.ZodType<EventResend> = z.object({
  webhook_ids: WebhookIdsListSchema.optional(),
});

export const WebhookListSchema: z.ZodType<WebhookList> = z.array(WebhookSchema);

export const WebhookList_1Schema: z.ZodType<WebhookList_1> = z.object({
  webhooks: WebhookListSchema.optional(),
});

export const WebhooksLookupSchema: z.ZodType<WebhooksLookup> = z.object({
  id: z.string().optional(),
  client_id: z.string().regex(new RegExp("^(?!\\d+$)\\w+\\S+")).max(128).optional(),
  links: DefinitionsLinkDescriptionListSchema.optional(),
});

export const WebhooksLookupListSchema: z.ZodType<WebhooksLookupList> = z.array(WebhooksLookupSchema);

export const WebhookLookupListSchema: z.ZodType<WebhookLookupList> = z.object({
  webhooks_lookups: WebhooksLookupListSchema.optional(),
});

/**
 * API Client class with path-based access to all operations.
 * All methods are inline within the class.
 */
export class ApiClient {
  constructor(private readonly config: OpenAPIClientConfig) {}

  readonly "/v1/notifications/simulate-event" = {
    /**
     * Simulate webhook event
     *
     * Simulates a webhook event. In the JSON request body, specify a sample payload.<br>You need to subscribe to the following webhook events for Pay upon Invoice:<br><table><thead><tr><th>Event</th><th>Trigger</th></tr></thead><tbody><tr><td><code>PAYMENT.CAPTURE.COMPLETED</code></td><td>A payment capture completes.</td></tr><tr><td><code>PAYMENT.CAPTURE.DENIED</code></td><td>A payment capture is denied.</td></tr><tr><td><code>CHECKOUT.PAYMENT-APPROVAL.REVERSED</code></td><td>PayPal reverses a payment capture.</td></tr></tbody></table>
     */
    post: (request: { body: SimulateEvent }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `202 Accepted` status code and a JSON response body that shows details for the mock event. */
        202: {
          body: Event;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/simulate-event",
          method: "post",
          requestSchema: z.object({
            body: SimulateEventSchema,
          }),
          responseSchema: {
            202: z.object({ body: EventSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/verify-webhook-signature" = {
    /**
     * Verify webhook signature
     *
     * Verifies a webhook signature.
     */
    post: (request: { body: VerifyWebhookSignature }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows the verification status. */
        200: {
          body: VerifyWebhookSignatureResponse;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/verify-webhook-signature",
          method: "post",
          requestSchema: z.object({
            body: VerifyWebhookSignatureSchema,
          }),
          responseSchema: {
            200: z.object({ body: VerifyWebhookSignatureResponseSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/webhooks" = {
    /**
     * List webhooks
     *
     * Lists webhooks for an app.
     */
    get: (request?: {
      query?: {
        /** Filters the webhooks in the response by an `anchor_id` entity type. */
        anchor_type?: "APPLICATION" | "ACCOUNT";
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that lists webhooks with webhook details. */
        200: {
          body: WebhookList_1;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks",
          method: "get",
          requestSchema: z.object({
            query: z.object({
              anchor_type: z.enum(["APPLICATION", "ACCOUNT"]).optional(),
            }).optional(),
          }),
          responseSchema: {
            200: z.object({ body: WebhookList_1Schema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
    /**
     * Create webhook
     *
     * Subscribes your webhook listener to events.
     */
    post: (request: { body: Webhook }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `201 Created` status code and a JSON response body with a [`webhook`](/docs/api/webhooks/v1/#definition-webhook) object that includes the webhook ID for later use. */
        201: {
          body: Webhook;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks",
          method: "post",
          requestSchema: z.object({
            body: WebhookSchema,
          }),
          responseSchema: {
            201: z.object({ body: WebhookSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/webhooks-event-types" = {
    /**
     * List available events
     *
     * Lists available events to which any webhook can subscribe. For a list of supported events, see [Webhook event names](/docs/api/notifications/webhooks/event-names/).
     */
    get: (): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that lists available events to which any webhook can subscribe. */
        200: {
          body: EventTypeList_1;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-event-types",
          method: "get",
          responseSchema: {
            200: z.object({ body: EventTypeList_1Schema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
      ) as any,
  };

  readonly "/v1/notifications/webhooks-events" = {
    /**
     * List event notifications
     *
     * Lists webhooks event notifications. Use query parameters to filter the response.
     */
    get: (request?: {
      query?: {
        /** The number of webhook event notifications to return in the response. */
        page_size?: number;
        /** Filters the webhook event notifications in the response to those created on or after this date and time and on or before the `end_time` value. Both values are in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6) format. Example: `start_time=2013-03-06T11:00:00Z`. */
        start_time?: string;
        /** Filters the webhook event notifications in the response to those created on or after the `start_time` and on or before this date and time. Both values are in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6) format. Example: `end_time=2013-03-06T11:00:00Z`. */
        end_time?: string;
        /** Filters the response to a single transaction, by ID. */
        transaction_id?: string;
        /** Filters the response to a single event. */
        event_type?: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that lists webhooks event notifications. */
        200: {
          body: EventList_1;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-events",
          method: "get",
          requestSchema: z.object({
            query: z.object({
              page_size: z.number().int().default(10).optional(),
              start_time: z.string().optional(),
              end_time: z.string().optional(),
              transaction_id: z.string().optional(),
              event_type: z.string().optional(),
            }).optional(),
          }),
          responseSchema: {
            200: z.object({ body: EventList_1Schema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/webhooks-events/{event_id}" = {
    /**
     * Show event notification details
     *
     * Shows details for a webhooks event notification, by ID.
     */
    get: (request: {
      path: {
        /** The ID of the webhook event notification to resend. */
        event_id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows webhooks event notification details. */
        200: {
          body: Event;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-events/{event_id}",
          method: "get",
          requestSchema: z.object({
            path: z.object({
              event_id: z.string().regex(new RegExp("^[a-zA-Z0-9]+$")).max(50),
            }),
          }),
          responseSchema: {
            200: z.object({ body: EventSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/webhooks-events/{event_id}/resend" = {
    /**
     * Resend event notification
     *
     * Resends a webhook event notification, by ID. Any pending notifications are not resent.
     */
    post: (request: {
      path: {
        /** The ID of the webhook event notification to resend. */
        event_id: string;
      };
      body?: EventResend;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `202 Accepted` status code and a JSON response body that shows webhook event notification details. */
        202: {
          body: Event;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-events/{event_id}/resend",
          method: "post",
          requestSchema: z.object({
            path: z.object({
              event_id: z.string().regex(new RegExp("^[a-zA-Z0-9]+$")).max(50),
            }),
            body: EventResendSchema,
          }),
          responseSchema: {
            202: z.object({ body: EventSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/webhooks-lookup" = {
    /**
     * List webhook lookups
     *
     * Lists webhook lookups.
     */
    get: (): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that lists webhook lookups with webhook lookup details. */
        200: {
          body: WebhookLookupList;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-lookup",
          method: "get",
          responseSchema: {
            200: z.object({ body: WebhookLookupListSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
      ) as any,
    /**
     * Create webhook lookup
     *
     * Creates a webhook lookup. A webhook lookup ties the API caller's REST API app to the subject account (or, if no subject is specified, to the API caller's account). If a webhook event is generated for an event that is tied to the account but not to a particular REST API app (for example, payments initiated with the NVP/SOAP APIs or through the user interface on PayPal.com), those webhook events will treated as if they were intended for the REST API app registered in the webhook lookup instead. Webhook events will then be delivered to any webhooks registered to that REST API app.
     */
    post: (): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `201 Created` status code and a JSON response body that shows webhook lookup details. */
        201: {
          body: WebhooksLookup;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-lookup",
          method: "post",
          responseSchema: {
            201: z.object({ body: WebhooksLookupSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
      ) as any,
  };

  readonly "/v1/notifications/webhooks-lookup/{webhook_lookup_id}" = {
    /**
     * Show webhook lookup details
     *
     * Shows details for a webhook lookup, by ID.
     */
    get: (request: {
      path: {
        /** The ID of the webhook lookup to delete. */
        webhook_lookup_id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows webhook lookup details. */
        200: {
          body: WebhooksLookup;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-lookup/{webhook_lookup_id}",
          method: "get",
          requestSchema: z.object({
            path: z.object({
              webhook_lookup_id: z.string(),
            }),
          }),
          responseSchema: {
            200: z.object({ body: WebhooksLookupSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
    /**
     * Delete webhook lookup
     *
     * Deletes a webhook lookup, by ID.
     */
    delete: (request: {
      path: {
        /** The ID of the webhook lookup to delete. */
        webhook_lookup_id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks-lookup/{webhook_lookup_id}",
          method: "delete",
          requestSchema: z.object({
            path: z.object({
              webhook_lookup_id: z.string(),
            }),
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/webhooks/{webhook_id}" = {
    /**
     * Show webhook details
     *
     * Shows details for a webhook, by ID.
     */
    get: (request: {
      path: {
        /** The ID of the webhook for which to list subscriptions. */
        webhook_id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows webhook details. */
        200: {
          body: Webhook;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks/{webhook_id}",
          method: "get",
          requestSchema: z.object({
            path: z.object({
              webhook_id: z.string(),
            }),
          }),
          responseSchema: {
            200: z.object({ body: WebhookSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
    /**
     * Update webhook
     *
     * Updates a webhook to replace webhook fields with new values. Supports only the `replace` operation. Pass a `json_patch` object with `replace` operation and `path`, which is `/url` for a URL or `/event_types` for events. The `value` is either the URL or a list of events.
     */
    patch: (request: {
      path: {
        /** The ID of the webhook for which to list subscriptions. */
        webhook_id: string;
      };
      body: PatchRequest;
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that shows webhook details. */
        200: {
          body: Webhook;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks/{webhook_id}",
          method: "patch",
          requestSchema: z.object({
            path: z.object({
              webhook_id: z.string(),
            }),
            body: PatchRequestSchema,
          }),
          responseSchema: {
            200: z.object({ body: WebhookSchema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
    /**
     * Delete webhook
     *
     * Deletes a webhook, by ID.
     */
    delete: (request: {
      path: {
        /** The ID of the webhook for which to list subscriptions. */
        webhook_id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `204 No Content` status code with no JSON response body. */
        204: {
          body: unknown;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks/{webhook_id}",
          method: "delete",
          requestSchema: z.object({
            path: z.object({
              webhook_id: z.string(),
            }),
          }),
          responseSchema: {
            204: z.object({ body: z.unknown() }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };

  readonly "/v1/notifications/webhooks/{webhook_id}/event-types" = {
    /**
     * List event subscriptions for webhook
     *
     * Lists event subscriptions for a webhook, by ID.
     */
    get: (request: {
      path: {
        /** The ID of the webhook for which to list subscriptions. */
        webhook_id: string;
      };
    }): Promise<
      OpenAPIResponses<{
        /** A successful request returns the HTTP `200 OK` status code and a JSON response body that lists event subscriptions for a webhook. */
        200: {
          body: EventTypeList_1;
        };
        /** The error response. */
        "default": {
          body: Error2;
        };
      }>
    > =>
      openAPIFetch(
        this.config,
        {
          path: "/v1/notifications/webhooks/{webhook_id}/event-types",
          method: "get",
          requestSchema: z.object({
            path: z.object({
              webhook_id: z.string(),
            }),
          }),
          responseSchema: {
            200: z.object({ body: EventTypeList_1Schema }),
            "default": z.object({ body: Error2Schema }),
          },
        },
        request,
      ) as any,
  };
}

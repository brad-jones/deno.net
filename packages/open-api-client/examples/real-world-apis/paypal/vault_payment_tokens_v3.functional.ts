// deno-lint-ignore-file no-explicit-any
// This file was auto-generated by @brad-jones/open-api-client

import {
  createCustomClient,
  type OpenAPIClientConfig,
  openAPIFetch,
  type OpenAPIResponses,
} from "jsr:@brad-jones/deno-net-open-api-client@0.2.0";
import { z } from "npm:zod@^4.1.12";

/**
 * The account identifier for a PayPal account.
 *
 * @format ppaas_payer_id_v3
 */
export type AccountId = string;

export type CardBrand =
  | "VISA"
  | "MASTERCARD"
  | "DISCOVER"
  | "AMEX"
  | "SOLO"
  | "JCB"
  | "STAR"
  | "DELTA"
  | "SWITCH"
  | "MAESTRO"
  | "CB_NATIONALE"
  | "CONFIGOGA"
  | "CONFIDIS"
  | "ELECTRON"
  | "CETELEM"
  | "CHINA_UNION_PAY"
  | "DINERS"
  | "ELO"
  | "HIPER"
  | "HIPERCARD"
  | "RUPAY"
  | "GE"
  | "SYNCHRONY"
  | "EFTPOS"
  | "UNKNOWN";

export type CardType = "CREDIT" | "DEBIT" | "PREPAID" | "STORE" | "UNKNOWN";

export type CardVerificationMethod = "SCA_WHEN_REQUIRED" | "SCA_ALWAYS";

export type CardVerificationStatus = "VERIFIED" | "FAILED";

/**
 * The [2-character ISO 3166-1 code](/api/rest/reference/country-codes/) that identifies the country or region.<blockquote><strong>Note:</strong> The country code for Great Britain is <code>GB</code> and not <code>UK</code> as used in the top-level domain names for that country. Use the `C2` country code for China worldwide for comparable uncontrolled price (CUP) method, bank card, and cross-border transactions.</blockquote>
 *
 * @format ppaas_common_country_code_v2
 */
export type CountryCode = string;

/** A resource representing a response for Apple Pay. */
export type ApplePayPaymentTokenResponse = {
  /** The payment card to be used to fund a payment. Can be a credit or debit card. */
  card?: {
    /** The card holder's name as it appears on the card. */
    name?: string;
    /** The last digits of the payment card. */
    last_digits?: string;
    type?: CardType;
    brand?: CardBrand;
    /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
    billing_address?: {
      /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
      address_line_1?: string;
      /** The second line of the address, for example, a suite or apartment number. */
      address_line_2?: string;
      /** A city, town, or village. Smaller than `admin_area_level_1`. */
      admin_area_2?: string;
      /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
      admin_area_1?: string;
      /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
      postal_code?: string;
      country_code: CountryCode;
    };
  };
};

/**
 * The [two-character ISO 3166-1 code](/api/rest/reference/country-codes/) that identifies the country or region.<blockquote><strong>Note:</strong> The country code for Great Britain is <code>GB</code> and not <code>UK</code> as used in the top-level domain names for that country. Use the `C2` country code for China worldwide for comparable uncontrolled price (CUP) method, bank card, and cross-border transactions.</blockquote>
 *
 * @format ppaas_common_country_code_v2
 */
export type CountryCode2 = string;

/** The [three-character ISO-4217 currency code](/api/rest/reference/currency-codes/) that identifies the currency. */
export type CurrencyCode = string;

/**
 * The stand-alone date, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). To represent special legal values, such as a date of birth, you should use dates with no associated time or time-zone data. Whenever possible, use the standard `date_time` type. This regular expression does not validate all dates. For example, February 31 is valid and nothing is known about leap years.
 *
 * @format ppaas_date_notime_v2
 */
export type DateNoTime = string;

/**
 * The date and time, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Seconds are required while fractional seconds are optional.<blockquote><strong>Note:</strong> The regular expression provides guidance but does not reject all invalid dates.</blockquote>
 *
 * @format ppaas_date_time_v3
 */
export type DateTime = string;

/** The year and month, in ISO-8601 `YYYY-MM` date format. See [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). */
export type DateYearMonth = string;

/**
 * The internationalized email address.<blockquote><strong>Note:</strong> Up to 64 characters are allowed before and 255 characters are allowed after the <code>@</code> sign. However, the generally accepted maximum length for an email address is 254 characters. The pattern verifies that an unquoted <code>@</code> sign exists.</blockquote>
 *
 * @format merchant_common_email_address_v2
 */
export type Email = string;

export type Enrolled = "Y" | "N" | "U" | "B";

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export type ErrorLinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "PATCH";
};

/** This is either a system or application error, and generally indicates that although the client appeared to provide a correct request, something unexpected has gone wrong on the server. */
export type Error500 = {
  name?: "INTERNAL_SERVER_ERROR";
  message?: "An internal server error occurred.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server is temporarily unable to handle the request, for example, because of planned maintenance or downtime. */
export type Error503 = {
  name?: "SERVICE_UNAVAILABLE";
  message?: "Service Unavailable.";
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

export type ErrorLocation = "body" | "path" | "query";

/** The error details. Required for client-side `4XX` errors. */
export type ErrorDetails = {
  /** The field that caused the error. If this field is in the body, set this value to the field's JSON pointer value. Required for client-side errors. */
  field?: string;
  /** The value of the field that caused the error. */
  value?: string;
  location?: ErrorLocation;
  /** The unique, fine-grained application-level error code. */
  issue: string;
  /** The human-readable description for an issue. The description can change over the lifetime of an API, so clients must not depend on this value. */
  description?: string;
};

/** Request is not well-formed, syntactically incorrect, or violates schema. */
export type Error400 = {
  name?: "INVALID_REQUEST";
  message?: "Request is not well-formed, syntactically incorrect, or violates schema.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** Authentication failed due to missing Authorization header, or invalid authentication credentials. */
export type Error401 = {
  name?: "AUTHENTICATION_FAILURE";
  message?: "Authentication failed due to missing authorization header, or invalid authentication credentials.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The client is not authorized to access this resource, although it may have valid credentials.  */
export type Error403 = {
  name?: "NOT_AUTHORIZED";
  message?: "Authorization failed due to insufficient permissions.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has not found anything matching the request URI. This either means that the URI is incorrect or the resource is not available. */
export type Error404 = {
  name?: "RESOURCE_NOT_FOUND";
  message?: "The specified resource does not exist.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server has detected a conflict while processing this request. */
export type Error409 = {
  name?: "RESOURCE_CONFLICT";
  message?: "The server has detected a conflict while processing this request.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The server does not support the request payload's media type. */
export type Error415 = {
  name?: "UNSUPPORTED_MEDIA_TYPE";
  message?: "The server does not support the request payload's media type.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

/** The requested action cannot be performed and may require interaction with APIs or processes outside of the current request. This is distinct from a 500 response in that there are no systemic problems limiting the API from performing the request. */
export type Error422 = {
  name?: "UNPROCESSABLE_ENTITY";
  message?: "The requested action could not be performed, semantically incorrect, or failed business validation.";
  details?: ErrorDetails[];
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id?: string;
  /** An array of request-related [HATEOAS links](https://en.wikipedia.org/wiki/HATEOAS). */
  links?: ErrorLinkDescription[];
};

export type ErrorDefault =
  | Error400
  | Error401
  | Error403
  | Error404
  | Error409
  | Error415
  | Error422
  | Error500
  | Error503;

export type Frequency = unknown;

/**
 * The [language tag](https://tools.ietf.org/html/bcp47#section-2) for the language in which to localize the error-related strings, such as messages, issues, and suggested actions. The tag is made up of the [ISO 639-2 language code](https://www.loc.gov/standards/iso639-2/php/code_list.php), the optional [ISO-15924 script tag](https://www.unicode.org/iso15924/codelists.html), and the [ISO-3166 alpha-2 country code](/api/rest/reference/country-codes/) or [M49 region code](https://unstats.un.org/unsd/methodology/m49/).
 *
 * @format ppaas_common_language_v3
 */
export type Language = string;

/** The request-related [HATEOAS link](/api/rest/responses/#hateoas-links) information. */
export type LinkDescription = {
  /** The complete target URL. To make the related call, combine the method with this [URI Template-formatted](https://tools.ietf.org/html/rfc6570) link. For pre-processing, include the `$`, `(`, and `)` characters. The `href` is the key HATEOAS component that links a completed call with a subsequent call. */
  href: string;
  /** The [link relation type](https://tools.ietf.org/html/rfc5988#section-4), which serves as an ID for a link that unambiguously describes the semantics of the link. See [Link Relations](https://www.iana.org/assignments/link-relations/link-relations.xhtml). */
  rel: string;
  method?: "GET" | "POST" | "PUT" | "DELETE" | "HEAD" | "CONNECT" | "OPTIONS" | "PATCH";
};

/** An array of request-related [HATEOAS links](/api/rest/responses/#hateoas-links) that are either relevant to the issue by providing additional information or offering potential resolutions. */
export type DefinitionsLinkDescriptionList = LinkDescription[];

/** An array of request-related [HATEOAS links](/api/rest/responses/#hateoas-links). */
export type ErrorDefinitionsLinkDescriptionList = LinkDescription[];

/** The error details. Required for client-side `4XX` errors. */
export type ErrorDetails2 = {
  /** The field that caused the error. If this field is in the body, set this value to the field's JSON pointer value. Required for client-side errors. */
  field?: string;
  /** The value of the field that caused the error. */
  value?: string;
  /** The location of the field that caused the error. Value is `body`, `path`, or `query`. */
  location?: string;
  /** The unique, fine-grained application-level error code. */
  issue: string;
  links?: DefinitionsLinkDescriptionList;
  /** The human-readable description for an issue. The description can change over the lifetime of an API, so clients must not depend on this value. */
  description?: string;
};

/** An array of additional details about the error. */
export type ErrorDetailsList = ErrorDetails2[];

/** The error details. */
export type Error = {
  /** The human-readable, unique name of the error. */
  name: string;
  /** The message that describes the error. */
  message: string;
  /** The PayPal internal ID. Used for correlation purposes. */
  debug_id: string;
  details?: ErrorDetailsList;
  links?: ErrorDefinitionsLinkDescriptionList;
};

/** An array of related [HATEOAS links](/api/rest/responses/#hateoas). */
export type LinkDescriptionList = LinkDescription[];

/** The unique ID for a customer generated by PayPal. */
export type MerchantPartnerCustomerId = string;

/** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
export type Customer = {
  id?: MerchantPartnerCustomerId;
  /** Merchants and partners may already have a data-store where their customer information is persisted. Use merchant_customer_id to associate the PayPal-generated customer.id to your representation of a customer. */
  merchant_customer_id?: string;
};

/** The currency and amount for a financial transaction, such as a balance or payment due. */
export type Money = {
  currency_code: CurrencyCode;
  /** The value, which might be:<ul><li>An integer for currencies like `JPY` that are not typically fractional.</li><li>A decimal fraction for currencies like `TND` that are subdivided into thousandths.</li></ul>For the required number of decimal places for a currency code, see [Currency Codes](/api/rest/reference/currency-codes/). */
  value: string;
};

/** Card Verification details including the authorization details and 3D SECURE details. */
export type CardVerificationDetails = {
  /** DEPRECATED. This field is DEPRECATED. Please find the network transaction id data in the 'id' field under the 'network_transaction_reference' object instead of the 'verification' object. */
  network_transaction_id?: string;
  /** DEPRECATED. This field is DEPRECATED. Please find the date data in the 'date' field under the 'network_transaction_reference' object instead of the 'verification' object. */
  date?: string;
  network?: CardBrand;
  time?: DateTime;
  amount?: Money;
  /** The processor response information for payment requests, such as direct credit card transactions. */
  processor_response?: {
    avs_code?:
      | "A"
      | "B"
      | "C"
      | "D"
      | "E"
      | "F"
      | "G"
      | "I"
      | "M"
      | "N"
      | "P"
      | "R"
      | "S"
      | "U"
      | "W"
      | "X"
      | "Y"
      | "Z"
      | "Null"
      | "0"
      | "1"
      | "2"
      | "3"
      | "4";
    cvv_code?: "E" | "I" | "M" | "N" | "P" | "S" | "U" | "X" | "All others" | "0" | "1" | "2" | "3" | "4";
  };
  three_d_secure?: unknown;
};

export type NetworkTokenEntity = unknown;

/** Previous network transaction reference including id and network. */
export type NetworkTransactionReferenceEntity = {
  /** Transaction reference id returned by the scheme. For Visa and Amex, this is the "Tran id" field in response. For MasterCard, this is the "BankNet reference id" field in response. For Discover, this is the "NRID" field in response. The pattern we expect for this field from Visa/Amex/CB/Discover is numeric, Mastercard/BNPP is alphanumeric and Paysecure is alphanumeric with special character -. */
  id: string;
  /** The date that the transaction was authorized by the scheme. This field may not be returned for all networks. MasterCard refers to this field as "BankNet reference date. */
  date?: string;
  network?: CardBrand;
  time?: DateTime;
};

export type NetworkTransactionReferenceRequest = unknown;

export type NetworkTransactionReferenceResponse = NetworkTransactionReferenceEntity;

/** The one-time charge info at the time of checkout. */
export type OneTimeCharges = {
  setup_fee?: Money;
  shipping_amount?: Money;
  taxes?: Money;
  product_price?: Money;
  subtotal?: Money;
  total_amount: Money;
};

export type ParesStatus = "Y" | "N" | "U" | "A" | "C" | "R" | "D" | "I";

/** The customer who approves and pays for the order. The customer is also known as the payer. */
export type PayerBase = {
  email_address?: Email;
  payer_id?: AccountId;
};

export type PaymentTokenStatus = "CREATED" | "PAYER_ACTION_REQUIRED" | "APPROVED" | "VAULTED" | "TOKENIZED";

/** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
export type Phone = {
  /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  country_code: string;
  /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
  national_number: string;
  /** The extension number. */
  extension_number?: string;
};

export type PhoneType = "FAX" | "HOME" | "MOBILE" | "OTHER" | "PAGER";

/** The phone information. */
export type PhoneWithType = {
  phone_type?: PhoneType;
  /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
  phone_number: {
    /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
    national_number: string;
  };
};

/** The pricing scheme details. */
export type PricingScheme = {
  price?: Money;
  pricing_model: "FIXED" | "VARIABLE" | "AUTO_RELOAD";
  reload_threshold_amount?: Money;
};

/** The billing cycle providing details of the billing frequency, amount, duration and if the billing cycle is a free, discounted or regular billing cycle. The sequence of the billing cycle will be in the following order - free trial billing cycle(s), discounted trial billing cycle(s), regular billing cycle(s). */
export type BillingCycle = {
  tenure_type: "REGULAR" | "TRIAL";
  pricing_scheme?: PricingScheme;
  frequency?: Frequency;
  /**
   * The number of times this billing cycle gets executed. Trial billing cycles can only be executed a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>). Regular billing cycles can be executed infinite times (value of <code>0</code> for <code>total_cycles</code>) or a finite number of times (value between <code>1</code> and <code>999</code> for <code>total_cycles</code>).
   *
   * @format int32
   */
  total_cycles?: number;
  /**
   * The order in which this cycle is to run among other billing cycles. For example, a trial billing cycle has a `sequence` of `1` while a regular billing cycle has a `sequence` of `2`, so that trial cycle runs before the regular cycle.
   *
   * @format int32
   */
  sequence?: number;
  start_date?: DateNoTime;
};

/** An array of billing cycles for trial billing and regular billing. A plan can have at most two trial cycles and only one regular cycle. */
export type BillingCycleList = BillingCycle[];

export type ProductOverride = unknown;

/** The merchant level Recurring Billing plan metadata for the Billing Agreement. */
export type Plan = {
  billing_cycles: BillingCycleList;
  product?: ProductOverride;
  one_time_charges: OneTimeCharges;
  /** Name of the recurring plan. */
  name?: string;
};

/** This value provides the category of the BIN. */
export type Products = string;

/** The type of card product assigned to the BIN by the issuer. These values are defined by the issuer and may change over time. Some examples include: PREPAID_GIFT, CONSUMER, CORPORATE. */
export type ProductsList = Products[];

/** Bank Identification Number (BIN) details used to fund a payment. */
export type BinDetails = {
  /** The Bank Identification Number (BIN) signifies the number that is being used to identify the granular level details (except the PII information) of the card. */
  bin?: string;
  /** The issuer of the card instrument. */
  issuing_bank?: string;
  bin_country_code?: CountryCode2;
  products?: ProductsList;
};

/** Results of 3D Secure Authentication. */
export type ThreeDSecureAuthenticationResponse = {
  authentication_status?: ParesStatus;
  enrollment_status?: Enrolled;
};

export type ThreeDSecureAuthenticationResponse2 = ThreeDSecureAuthenticationResponse & unknown;

/** Full representation of a Card Payment Token. */
export type CardResponseEntity = {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The last digits of the payment card. */
  last_digits?: string;
  brand?: CardBrand;
  expiry?: DateYearMonth;
  /** Address request details. */
  billing_address?: {
    /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
    address_line_1?: string;
    /** The second line of the address, for example, a suite or apartment number. */
    address_line_2?: string;
    /** A city, town, or village. Smaller than `admin_area_level_1`. */
    admin_area_2?: string;
    /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
    admin_area_1?: string;
    /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
    postal_code?: string;
    country_code: CountryCode;
    /** The resource ID of the address. */
    id?: string;
  };
  verification_status?: CardVerificationStatus;
  verification?: CardVerificationDetails;
  network_transaction_reference?: NetworkTransactionReferenceResponse;
  /** Results of Authentication such as 3D Secure. */
  authentication_result?: {
    three_d_secure?: ThreeDSecureAuthenticationResponse2;
  };
  bin_details?: BinDetails;
  type?: CardType;
};

export type CardResponse = CardResponseEntity & unknown;

/** The Tokenized Payment Source representing a Request to Vault a Token. */
export type TokenIdRequest = {
  /** The PayPal-generated ID for the token. */
  id: string;
  type: "SETUP_TOKEN";
};

/** The payment method to vault with the instrument details. */
export type DefinitionsPaymentSource = {
  /** A Resource representing a request to vault a Card. */
  card?: {
    /** The card holder's name as it appears on the card. */
    name?: string;
    /** The primary account number (PAN) for the payment card. */
    number?: string;
    expiry?: DateYearMonth;
    /** The three- or four-digit security code of the card. Also known as the CVV, CVC, CVN, CVE, or CID. This parameter cannot be present in the request when `payment_initiator=MERCHANT`. */
    security_code?: string;
    brand?: CardBrand;
    /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
    billing_address?: {
      /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
      address_line_1?: string;
      /** The second line of the address, for example, a suite or apartment number. */
      address_line_2?: string;
      /** A city, town, or village. Smaller than `admin_area_level_1`. */
      admin_area_2?: string;
      /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
      admin_area_1?: string;
      /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
      postal_code?: string;
      country_code: CountryCode;
    };
    network_transaction_reference?: NetworkTransactionReferenceRequest;
  };
  token?: TokenIdRequest;
};

/** Payment Token Request where the `source` defines the type of instrument to be stored. */
export type PaymentTokenRequest = {
  /** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
  customer?: {
    id?: MerchantPartnerCustomerId;
    /** Merchants and partners may already have a data-store where their customer information is persisted. Use merchant_customer_id to associate the PayPal-generated customer.id to your representation of a customer. */
    merchant_customer_id?: string;
  };
  payment_source: DefinitionsPaymentSource;
};

/** The PayPal-generated ID for the vault token. */
export type VaultId = string;

export type VaultInstruction = "ON_CREATE_PAYMENT_TOKENS" | "ON_PAYER_APPROVAL";

/** Customizes the Vault creation flow experience for your customers. */
export type ExperienceContext = {
  /** The label that overrides the business name in the PayPal account on the PayPal site. The pattern is defined by an external party and supports Unicode. */
  brand_name?: string;
  locale?: Language;
  /**
   * The URL where the customer is redirected after customer approves leaves the flow. It is a required field for contingency flows like PayPal wallet, 3DS.
   *
   * @format uri
   */
  return_url?: string;
  /**
   * The URL where the customer is redirected after customer cancels or leaves the flow. It is a required field for contingency flows like PayPal wallet, 3DS.
   *
   * @format uri
   */
  cancel_url?: string;
  shipping_preference?: "GET_FROM_FILE" | "NO_SHIPPING" | "SET_PROVIDED_ADDRESS";
  vault_instruction?: VaultInstruction;
};

/** Resource consolidating common request and response attributes for vaulting a Digital Wallet. */
export type WalletBase = {
  /** The description displayed to the consumer on the approval flow for a digital wallet, as well as on the merchant view of the payment token management experience. exp: PayPal.com. */
  description?: string;
  usage_pattern?:
    | "IMMEDIATE"
    | "DEFERRED"
    | "RECURRING_PREPAID"
    | "RECURRING_POSTPAID"
    | "THRESHOLD_PREPAID"
    | "THRESHOLD_POSTPAID"
    | "UNSCHEDULED_POSTPAID"
    | "UNSCHEDULED_PREPAID"
    | "SUBSCRIPTION_PREPAID"
    | "SUBSCRIPTION_POSTPAID"
    | "INSTALLMENT_POSTPAID"
    | "INSTALLMENT_PREPAID";
  /** The shipping details. */
  shipping?: {
    /** The name of the party. */
    name?: {
      /** When the party is a person, the party's full name. */
      full_name?: string;
    };
    /** The phone number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). */
    phone_number?: {
      /** The country calling code (CC), in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the CC and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
      country_code: string;
      /** The national number, in its canonical international [E.164 numbering plan format](https://www.itu.int/rec/T-REC-E.164/en). The combined length of the country calling code (CC) and the national number must not be greater than 15 digits. The national number consists of a national destination code (NDC) and subscriber number (SN). */
      national_number: string;
    };
    type?: "SHIPPING" | "PICKUP_IN_PERSON" | "PICKUP_IN_STORE" | "PICKUP_FROM_PERSON";
    /** The portable international postal address. Maps to [AddressValidationMetadata](https://github.com/googlei18n/libaddressinput/wiki/AddressValidationMetadata) and HTML 5.1 [Autofilling form controls: the autocomplete attribute](https://www.w3.org/TR/html51/sec-forms.html#autofilling-form-controls-the-autocomplete-attribute). */
    address?: {
      /** The first line of the address, such as number and street, for example, `173 Drury Lane`. Needed for data entry, and Compliance and Risk checks. This field needs to pass the full address. */
      address_line_1?: string;
      /** The second line of the address, for example, a suite or apartment number. */
      address_line_2?: string;
      /** A city, town, or village. Smaller than `admin_area_level_1`. */
      admin_area_2?: string;
      /** The highest-level sub-division in a country, which is usually a province, state, or ISO-3166-2 subdivision. This data is formatted for postal delivery, for example, `CA` and not `California`. Value, by country, is:<ul><li>UK. A county.</li><li>US. A state.</li><li>Canada. A province.</li><li>Japan. A prefecture.</li><li>Switzerland. A *kanton*.</li></ul> */
      admin_area_1?: string;
      /** The postal code, which is the ZIP code or equivalent. Typically required for countries with a postal code or an equivalent. See [postal code](https://en.wikipedia.org/wiki/Postal_code). */
      postal_code?: string;
      country_code: CountryCode;
    };
  };
  /** Create multiple payment tokens for the same payer, merchant/platform combination. Use this when the customer has not logged in at merchant/platform. The payment token thus generated, can then also be used to create the customer account at merchant/platform. Use this also when multiple payment tokens are required for the same payer, different customer at merchant/platform. This helps to identify customers distinctly even though they may share the same PayPal account. This only applies to PayPal payment source. */
  permit_multiple_payment_tokens?: boolean;
  usage_type?: "MERCHANT" | "PLATFORM";
  customer_type?: "CONSUMER" | "BUSINESS";
};

export type PaypalWalletRequest = WalletBase & unknown;

export type PaypalWalletResponse =
  & WalletBase
  & unknown
  & unknown
  & PayerBase
  & unknown
  & /**
   * The customer who approves and pays for the order. The customer is also known as the payer.
   *
   * @format payer_v1
   */ Record<string, never>
  & unknown
  & /** Full representation of a PayPal Payment Token. */ Record<string, never>;

export type VenmoRequest =
  & WalletBase
  & unknown
  & /** A resource representing a request to vault Venmo. */ Record<string, never>;

/** Setup Token Request where the `source` defines the type of instrument to be stored. */
export type SetupTokenRequest = {
  /** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
  customer?: {
    id?: MerchantPartnerCustomerId;
    /** Merchants and partners may already have a data-store where their customer information is persisted. Use merchant_customer_id to associate the PayPal-generated customer.id to your representation of a customer. */
    merchant_customer_id?: string;
  };
  payment_source: unknown;
};

export type VenmoResponse =
  & WalletBase
  & unknown
  & unknown
  & PayerBase
  & unknown
  & /**
   * The customer who approves and pays for the order. The customer is also known as the payer.
   *
   * @format payer_v1
   */ Record<string, never>
  & unknown
  & /** Full representation of a Venmo Payment Token. */ Record<string, never>;

/** The vaulted payment method details. */
export type PaymentSource = {
  card?: CardResponse;
  paypal?: PaypalWalletResponse;
  venmo?: VenmoResponse;
  apple_pay?: ApplePayPaymentTokenResponse;
};

/** Full representation of a saved payment token. */
export type PaymentTokenResponse = {
  id?: VaultId;
  customer?: Customer;
  payment_source?: PaymentSource;
  links?: LinkDescriptionList;
};

export type PaymentTokenResponseList = PaymentTokenResponse[];

export type CustomerVaultPaymentTokensResponse = unknown;

/** Minimal representation of a cached setup token. */
export type SetupTokenResponse = {
  id?: VaultId;
  /** This object defines a customer in your system. Use it to manage customer profiles, save payment methods and contact details. */
  customer?: {
    id?: MerchantPartnerCustomerId;
    /** Merchants and partners may already have a data-store where their customer information is persisted. Use merchant_customer_id to associate the PayPal-generated customer.id to your representation of a customer. */
    merchant_customer_id?: string;
  };
  status?: PaymentTokenStatus;
  payment_source?: unknown;
  links?: LinkDescriptionList;
};

export const AccountIdSchema: z.ZodType<AccountId> = z.string().regex(new RegExp("^[2-9A-HJ-NP-Z]{13}$")).min(13).max(
  13,
);

export const CardBrandSchema: z.ZodType<CardBrand> = z.enum([
  "VISA",
  "MASTERCARD",
  "DISCOVER",
  "AMEX",
  "SOLO",
  "JCB",
  "STAR",
  "DELTA",
  "SWITCH",
  "MAESTRO",
  "CB_NATIONALE",
  "CONFIGOGA",
  "CONFIDIS",
  "ELECTRON",
  "CETELEM",
  "CHINA_UNION_PAY",
  "DINERS",
  "ELO",
  "HIPER",
  "HIPERCARD",
  "RUPAY",
  "GE",
  "SYNCHRONY",
  "EFTPOS",
  "UNKNOWN",
]);

export const CardTypeSchema: z.ZodType<CardType> = z.enum(["CREDIT", "DEBIT", "PREPAID", "STORE", "UNKNOWN"]);

export const CardVerificationMethodSchema: z.ZodType<CardVerificationMethod> = z.enum([
  "SCA_WHEN_REQUIRED",
  "SCA_ALWAYS",
]);

export const CardVerificationStatusSchema: z.ZodType<CardVerificationStatus> = z.enum(["VERIFIED", "FAILED"]);

export const CountryCodeSchema: z.ZodType<CountryCode> = z.string().regex(new RegExp("^([A-Z]{2}|C2)$")).min(2).max(2);

export const ApplePayPaymentTokenResponseSchema: z.ZodType<ApplePayPaymentTokenResponse> = z.object({
  card: z.object({
    name: z.string().regex(new RegExp("^.{1,300}$")).min(1).max(300).optional(),
    last_digits: z.string().regex(new RegExp("^[0-9]{2,4}$")).min(2).max(4).optional(),
    type: CardTypeSchema.optional(),
    brand: CardBrandSchema.optional(),
    billing_address: z.object({
      address_line_1: z.string().max(300).optional(),
      address_line_2: z.string().max(300).optional(),
      admin_area_2: z.string().max(120).optional(),
      admin_area_1: z.string().max(300).optional(),
      postal_code: z.string().max(60).optional(),
      country_code: CountryCodeSchema,
    }).optional(),
  }).optional(),
});

export const CountryCode2Schema: z.ZodType<CountryCode2> = z.string().regex(new RegExp("^([A-Z]{2}|C2)$")).min(2).max(
  2,
);

export const CurrencyCodeSchema: z.ZodType<CurrencyCode> = z.string().min(3).max(3);

export const DateNoTimeSchema: z.ZodType<DateNoTime> = z.string().regex(
  new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])$"),
).min(10).max(10);

export const DateTimeSchema: z.ZodType<DateTime> = z.string().regex(
  new RegExp(
    "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[1-2][0-9]|3[0-1])[T,t]([0-1][0-9]|2[0-3]):[0-5][0-9]:([0-5][0-9]|60)([.][0-9]+)?([Zz]|[+-][0-9]{2}:[0-9]{2})$",
  ),
).min(20).max(64);

export const DateYearMonthSchema: z.ZodType<DateYearMonth> = z.string().regex(new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])$"))
  .min(7).max(7);

export const EmailSchema: z.ZodType<Email> = z.string().regex(
  new RegExp(
    "(?:[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+(?:\\.[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+)*|(?:[\\x01-\\x08\\x0b\\x0c\\x0e-\\x1f\\x21\\x23-\\x5b\\x5d-\\x7f]|\\[\\x01-\\x09\\x0b\\x0c\\x0e-\\x7f])*\")@(?:(?:[a-zA-Z0-9](?:[a-zA-Z0-9-]*[a-zA-Z0-9])?\\.)+[a-zA-Z0-9](?:[a-zA-Z0-9-]*[a-zA-Z0-9])?|\\[(?:(?:(2(5[0-5]|[0-4][0-9])|1[0-9][0-9]|[1-9]?[0-9]))\\.){3}(?:(2(5[0-5]|[0-4][0-9])|1[0-9][0-9]|[1-9]?[0-9])|[a-zA-Z0-9-]*[a-zA-Z0-9]:(?:[\\x01-\\x08\\x0b\\x0c\\x0e-\\x1f\\x21-\\x5a\\x53-\\x7f]|\\[\\x01-\\x09\\x0b\\x0c\\x0e-\\x7f])+)\\])",
  ),
).min(3).max(254);

export const EnrolledSchema: z.ZodType<Enrolled> = z.enum(["Y", "N", "U", "B"]);

export const ErrorLinkDescriptionSchema: z.ZodType<ErrorLinkDescription> = z.object({
  href: z.string().regex(new RegExp("^.*$")).min(0).max(20000),
  rel: z.string().regex(new RegExp("^.*$")).min(0).max(100),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "PATCH"]).optional(),
});

export const Error500Schema: z.ZodType<Error500> = z.object({
  name: z.enum(["INTERNAL_SERVER_ERROR"]).optional(),
  message: z.enum(["An internal server error occurred."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error503Schema: z.ZodType<Error503> = z.object({
  name: z.enum(["SERVICE_UNAVAILABLE"]).optional(),
  message: z.enum(["Service Unavailable."]).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorLocationSchema: z.ZodType<ErrorLocation> = z.enum(["body", "path", "query"]);

export const ErrorDetailsSchema: z.ZodType<ErrorDetails> = z.object({
  field: z.string().optional(),
  value: z.string().optional(),
  location: ErrorLocationSchema.optional(),
  issue: z.string(),
  description: z.string().optional(),
});

export const Error400Schema: z.ZodType<Error400> = z.object({
  name: z.enum(["INVALID_REQUEST"]).optional(),
  message: z.enum(["Request is not well-formed, syntactically incorrect, or violates schema."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error401Schema: z.ZodType<Error401> = z.object({
  name: z.enum(["AUTHENTICATION_FAILURE"]).optional(),
  message: z.enum(["Authentication failed due to missing authorization header, or invalid authentication credentials."])
    .optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error403Schema: z.ZodType<Error403> = z.object({
  name: z.enum(["NOT_AUTHORIZED"]).optional(),
  message: z.enum(["Authorization failed due to insufficient permissions."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error404Schema: z.ZodType<Error404> = z.object({
  name: z.enum(["RESOURCE_NOT_FOUND"]).optional(),
  message: z.enum(["The specified resource does not exist."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error409Schema: z.ZodType<Error409> = z.object({
  name: z.enum(["RESOURCE_CONFLICT"]).optional(),
  message: z.enum(["The server has detected a conflict while processing this request."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error415Schema: z.ZodType<Error415> = z.object({
  name: z.enum(["UNSUPPORTED_MEDIA_TYPE"]).optional(),
  message: z.enum(["The server does not support the request payload's media type."]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const Error422Schema: z.ZodType<Error422> = z.object({
  name: z.enum(["UNPROCESSABLE_ENTITY"]).optional(),
  message: z.enum([
    "The requested action could not be performed, semantically incorrect, or failed business validation.",
  ]).optional(),
  details: z.array(ErrorDetailsSchema).optional(),
  debug_id: z.string().optional(),
  links: z.array(ErrorLinkDescriptionSchema).min(0).max(10000).optional(),
});

export const ErrorDefaultSchema: z.ZodType<ErrorDefault> = z.union([
  Error400Schema,
  Error401Schema,
  Error403Schema,
  Error404Schema,
  Error409Schema,
  Error415Schema,
  Error422Schema,
  Error500Schema,
  Error503Schema,
]);

export const FrequencySchema: z.ZodType<Frequency> = z.unknown();

export const LanguageSchema: z.ZodType<Language> = z.string().regex(
  new RegExp("^[a-z]{2}(?:-[A-Z][a-z]{3})?(?:-(?:[A-Z]{2}|[0-9]{3}))?$"),
).min(2).max(10);

export const LinkDescriptionSchema: z.ZodType<LinkDescription> = z.object({
  href: z.string(),
  rel: z.string(),
  method: z.enum(["GET", "POST", "PUT", "DELETE", "HEAD", "CONNECT", "OPTIONS", "PATCH"]).optional(),
});

export const DefinitionsLinkDescriptionListSchema: z.ZodType<DefinitionsLinkDescriptionList> = z.array(
  LinkDescriptionSchema,
).min(1).max(4);

export const ErrorDefinitionsLinkDescriptionListSchema: z.ZodType<ErrorDefinitionsLinkDescriptionList> = z.array(
  LinkDescriptionSchema,
);

export const ErrorDetails2Schema: z.ZodType<ErrorDetails2> = z.object({
  field: z.string().optional(),
  value: z.string().optional(),
  location: z.string().default("body").optional(),
  issue: z.string(),
  links: DefinitionsLinkDescriptionListSchema.optional(),
  description: z.string().optional(),
});

export const ErrorDetailsListSchema: z.ZodType<ErrorDetailsList> = z.array(ErrorDetails2Schema);

export const ErrorSchema: z.ZodType<Error> = z.object({
  name: z.string(),
  message: z.string(),
  debug_id: z.string(),
  details: ErrorDetailsListSchema.optional(),
  links: ErrorDefinitionsLinkDescriptionListSchema.optional(),
});

export const LinkDescriptionListSchema: z.ZodType<LinkDescriptionList> = z.array(LinkDescriptionSchema).min(1).max(32);

export const MerchantPartnerCustomerIdSchema: z.ZodType<MerchantPartnerCustomerId> = z.string().regex(
  new RegExp("^[0-9a-zA-Z_-]+$"),
).min(1).max(22);

export const CustomerSchema: z.ZodType<Customer> = z.object({
  id: MerchantPartnerCustomerIdSchema.optional(),
  merchant_customer_id: z.string().regex(new RegExp("^[0-9a-zA-Z-_.^*$@#]+$")).min(1).max(64).optional(),
});

export const MoneySchema: z.ZodType<Money> = z.object({
  currency_code: CurrencyCodeSchema,
  value: z.string().regex(new RegExp("^((-?[0-9]+)|(-?([0-9]+)?[.][0-9]+))$")).max(32),
});

export const CardVerificationDetailsSchema: z.ZodType<CardVerificationDetails> = z.object({
  network_transaction_id: z.string().regex(new RegExp("^[a-zA-Z0-9-_@.:&+=*^'~#!$%()]+$")).min(1).max(1024).optional(),
  date: z.string().regex(new RegExp("^[0-9]+$")).min(4).max(4).optional(),
  network: CardBrandSchema.optional(),
  time: DateTimeSchema.optional(),
  amount: MoneySchema.optional(),
  processor_response: z.object({
    avs_code: z.enum([
      "A",
      "B",
      "C",
      "D",
      "E",
      "F",
      "G",
      "I",
      "M",
      "N",
      "P",
      "R",
      "S",
      "U",
      "W",
      "X",
      "Y",
      "Z",
      "Null",
      "0",
      "1",
      "2",
      "3",
      "4",
    ]).optional(),
    cvv_code: z.enum(["E", "I", "M", "N", "P", "S", "U", "X", "All others", "0", "1", "2", "3", "4"]).optional(),
  }).optional(),
  three_d_secure: z.unknown().optional(),
});

export const NetworkTokenEntitySchema: z.ZodType<NetworkTokenEntity> = z.unknown();

export const NetworkTransactionReferenceEntitySchema: z.ZodType<NetworkTransactionReferenceEntity> = z.object({
  id: z.string().regex(new RegExp("^[a-zA-Z0-9-_@.:&+=*^'~#!$%()]+$")).min(9).max(36),
  date: z.string().regex(new RegExp("^[0-9]+$")).min(4).max(4).optional(),
  network: CardBrandSchema.optional(),
  time: DateTimeSchema.optional(),
});

export const NetworkTransactionReferenceRequestSchema: z.ZodType<NetworkTransactionReferenceRequest> = z.unknown();

export const NetworkTransactionReferenceResponseSchema: z.ZodType<NetworkTransactionReferenceResponse> =
  NetworkTransactionReferenceEntitySchema;

export const OneTimeChargesSchema: z.ZodType<OneTimeCharges> = z.object({
  setup_fee: MoneySchema.optional(),
  shipping_amount: MoneySchema.optional(),
  taxes: MoneySchema.optional(),
  product_price: MoneySchema.optional(),
  subtotal: MoneySchema.optional(),
  total_amount: MoneySchema,
});

export const ParesStatusSchema: z.ZodType<ParesStatus> = z.enum(["Y", "N", "U", "A", "C", "R", "D", "I"]);

export const PayerBaseSchema: z.ZodType<PayerBase> = z.object({
  email_address: EmailSchema.optional(),
  payer_id: AccountIdSchema.optional(),
});

export const PaymentTokenStatusSchema: z.ZodType<PaymentTokenStatus> = z.enum([
  "CREATED",
  "PAYER_ACTION_REQUIRED",
  "APPROVED",
  "VAULTED",
  "TOKENIZED",
]);

export const PhoneSchema: z.ZodType<Phone> = z.object({
  country_code: z.string().regex(new RegExp("^[0-9]{1,3}?$")).min(1).max(3),
  national_number: z.string().regex(new RegExp("^[0-9]{1,14}?$")).min(1).max(14),
  extension_number: z.string().regex(new RegExp("^[0-9]{1,15}?$")).min(1).max(15).optional(),
});

export const PhoneTypeSchema: z.ZodType<PhoneType> = z.enum(["FAX", "HOME", "MOBILE", "OTHER", "PAGER"]);

export const PhoneWithTypeSchema: z.ZodType<PhoneWithType> = z.object({
  phone_type: PhoneTypeSchema.optional(),
  phone_number: z.object({
    national_number: z.string().regex(new RegExp("^[0-9]{1,14}?$")).min(1).max(14),
  }),
});

export const PricingSchemeSchema: z.ZodType<PricingScheme> = z.object({
  price: MoneySchema.optional(),
  pricing_model: z.enum(["FIXED", "VARIABLE", "AUTO_RELOAD"]),
  reload_threshold_amount: MoneySchema.optional(),
});

export const BillingCycleSchema: z.ZodType<BillingCycle> = z.object({
  tenure_type: z.enum(["REGULAR", "TRIAL"]),
  pricing_scheme: PricingSchemeSchema.optional(),
  frequency: FrequencySchema.optional(),
  total_cycles: z.number().int().min(0).max(999).default(1).optional(),
  sequence: z.number().int().min(1).max(3).default(1).optional(),
  start_date: DateNoTimeSchema.optional(),
});

export const BillingCycleListSchema: z.ZodType<BillingCycleList> = z.array(BillingCycleSchema).min(1).max(3);

export const ProductOverrideSchema: z.ZodType<ProductOverride> = z.unknown();

export const PlanSchema: z.ZodType<Plan> = z.object({
  billing_cycles: BillingCycleListSchema,
  product: ProductOverrideSchema.optional(),
  one_time_charges: OneTimeChargesSchema,
  name: z.string().regex(new RegExp("^[A-Za-z0-9() +',.:-]+$")).min(1).max(127).optional(),
});

export const ProductsSchema: z.ZodType<Products> = z.string().min(1).max(255);

export const ProductsListSchema: z.ZodType<ProductsList> = z.array(ProductsSchema).min(1).max(256);

export const BinDetailsSchema: z.ZodType<BinDetails> = z.object({
  bin: z.string().regex(new RegExp("^[0-9]+$")).min(1).max(25).optional(),
  issuing_bank: z.string().min(1).max(64).optional(),
  bin_country_code: CountryCode2Schema.optional(),
  products: ProductsListSchema.optional(),
});

export const ThreeDSecureAuthenticationResponseSchema: z.ZodType<ThreeDSecureAuthenticationResponse> = z.object({
  authentication_status: ParesStatusSchema.optional(),
  enrollment_status: EnrolledSchema.optional(),
});

export const ThreeDSecureAuthenticationResponse2Schema: z.ZodType<ThreeDSecureAuthenticationResponse2> =
  ThreeDSecureAuthenticationResponseSchema.and(z.unknown());

export const CardResponseEntitySchema: z.ZodType<CardResponseEntity> = z.object({
  name: z.string().regex(new RegExp("^[A-Za-z ]+$")).min(2).max(300).optional(),
  last_digits: z.string().regex(new RegExp("[0-9]{2,}")).min(2).max(4).optional(),
  brand: CardBrandSchema.optional(),
  expiry: DateYearMonthSchema.optional(),
  billing_address: z.object({
    address_line_1: z.string().max(300).optional(),
    address_line_2: z.string().max(300).optional(),
    admin_area_2: z.string().max(120).optional(),
    admin_area_1: z.string().max(300).optional(),
    postal_code: z.string().max(60).optional(),
    country_code: CountryCodeSchema,
    id: z.string().regex(new RegExp("^[0-9A-Za-z-_]+$")).min(1).max(36).optional(),
  }).optional(),
  verification_status: CardVerificationStatusSchema.optional(),
  verification: CardVerificationDetailsSchema.optional(),
  network_transaction_reference: NetworkTransactionReferenceResponseSchema.optional(),
  authentication_result: z.object({
    three_d_secure: ThreeDSecureAuthenticationResponse2Schema.optional(),
  }).optional(),
  bin_details: BinDetailsSchema.optional(),
  type: CardTypeSchema.optional(),
});

export const CardResponseSchema: z.ZodType<CardResponse> = CardResponseEntitySchema.and(z.unknown());

export const TokenIdRequestSchema: z.ZodType<TokenIdRequest> = z.object({
  id: z.string().regex(new RegExp("^[0-9A-Za-z_-]+$")).min(1).max(255),
  type: z.enum(["SETUP_TOKEN"]),
});

export const DefinitionsPaymentSourceSchema: z.ZodType<DefinitionsPaymentSource> = z.object({
  card: z.object({
    name: z.string().regex(new RegExp("^.{1,300}$")).min(1).max(300).optional(),
    number: z.string().regex(new RegExp("^[0-9]{13,19}$")).min(13).max(19).optional(),
    expiry: DateYearMonthSchema.optional(),
    security_code: z.string().regex(new RegExp("^[0-9]{3,4}$")).min(3).max(4).optional(),
    brand: CardBrandSchema.optional(),
    billing_address: z.object({
      address_line_1: z.string().max(300).optional(),
      address_line_2: z.string().max(300).optional(),
      admin_area_2: z.string().max(120).optional(),
      admin_area_1: z.string().max(300).optional(),
      postal_code: z.string().max(60).optional(),
      country_code: CountryCodeSchema,
    }).optional(),
    network_transaction_reference: NetworkTransactionReferenceRequestSchema.optional(),
  }).optional(),
  token: TokenIdRequestSchema.optional(),
});

export const PaymentTokenRequestSchema: z.ZodType<PaymentTokenRequest> = z.object({
  customer: z.object({
    id: MerchantPartnerCustomerIdSchema.optional(),
    merchant_customer_id: z.string().regex(new RegExp("^[0-9a-zA-Z-_.^*$@#]+$")).min(1).max(64).optional(),
  }).optional(),
  payment_source: DefinitionsPaymentSourceSchema,
});

export const VaultIdSchema: z.ZodType<VaultId> = z.string().regex(new RegExp("^[0-9a-zA-Z_-]+$")).min(1).max(36);

export const VaultInstructionSchema: z.ZodType<VaultInstruction> = z.enum([
  "ON_CREATE_PAYMENT_TOKENS",
  "ON_PAYER_APPROVAL",
]);

export const ExperienceContextSchema: z.ZodType<ExperienceContext> = z.object({
  brand_name: z.string().regex(new RegExp("^.*$")).min(1).max(300).optional(),
  locale: LanguageSchema.optional(),
  return_url: z.string().min(1).max(4000).optional(),
  cancel_url: z.string().min(1).max(4000).optional(),
  shipping_preference: z.enum(["GET_FROM_FILE", "NO_SHIPPING", "SET_PROVIDED_ADDRESS"]).optional(),
  vault_instruction: VaultInstructionSchema.optional(),
});

export const WalletBaseSchema: z.ZodType<WalletBase> = z.object({
  description: z.string().regex(new RegExp("^.*$")).min(1).max(128).optional(),
  usage_pattern: z.enum([
    "IMMEDIATE",
    "DEFERRED",
    "RECURRING_PREPAID",
    "RECURRING_POSTPAID",
    "THRESHOLD_PREPAID",
    "THRESHOLD_POSTPAID",
    "UNSCHEDULED_POSTPAID",
    "UNSCHEDULED_PREPAID",
    "SUBSCRIPTION_PREPAID",
    "SUBSCRIPTION_POSTPAID",
    "INSTALLMENT_POSTPAID",
    "INSTALLMENT_PREPAID",
  ]).optional(),
  shipping: z.object({
    name: z.object({
      full_name: z.string().max(300).optional(),
    }).optional(),
    phone_number: z.object({
      country_code: z.string().regex(new RegExp("^[0-9]{1,3}?$")).min(1).max(3),
      national_number: z.string().regex(new RegExp("^[0-9]{1,14}?$")).min(1).max(14),
    }).optional(),
    type: z.enum(["SHIPPING", "PICKUP_IN_PERSON", "PICKUP_IN_STORE", "PICKUP_FROM_PERSON"]).optional(),
    address: z.object({
      address_line_1: z.string().max(300).optional(),
      address_line_2: z.string().max(300).optional(),
      admin_area_2: z.string().max(120).optional(),
      admin_area_1: z.string().max(300).optional(),
      postal_code: z.string().max(60).optional(),
      country_code: CountryCodeSchema,
    }).optional(),
  }).optional(),
  permit_multiple_payment_tokens: z.boolean().optional(),
  usage_type: z.enum(["MERCHANT", "PLATFORM"]).optional(),
  customer_type: z.enum(["CONSUMER", "BUSINESS"]).optional(),
});

export const PaypalWalletRequestSchema: z.ZodType<PaypalWalletRequest> = WalletBaseSchema.and(z.unknown());

export const PaypalWalletResponseSchema: z.ZodType<PaypalWalletResponse> = WalletBaseSchema.and(z.unknown()).and(
  z.unknown(),
).and(PayerBaseSchema).and(z.unknown()).and(z.object({})).and(z.unknown()).and(z.object({}));

export const VenmoRequestSchema: z.ZodType<VenmoRequest> = WalletBaseSchema.and(z.unknown()).and(z.object({}));

export const SetupTokenRequestSchema: z.ZodType<SetupTokenRequest> = z.object({
  customer: z.object({
    id: MerchantPartnerCustomerIdSchema.optional(),
    merchant_customer_id: z.string().regex(new RegExp("^[0-9a-zA-Z-_.^*$@#]+$")).min(1).max(64).optional(),
  }).optional(),
  payment_source: z.unknown(),
});

export const VenmoResponseSchema: z.ZodType<VenmoResponse> = WalletBaseSchema.and(z.unknown()).and(z.unknown()).and(
  PayerBaseSchema,
).and(z.unknown()).and(z.object({})).and(z.unknown()).and(z.object({}));

export const PaymentSourceSchema: z.ZodType<PaymentSource> = z.object({
  card: CardResponseSchema.optional(),
  paypal: PaypalWalletResponseSchema.optional(),
  venmo: VenmoResponseSchema.optional(),
  apple_pay: ApplePayPaymentTokenResponseSchema.optional(),
});

export const PaymentTokenResponseSchema: z.ZodType<PaymentTokenResponse> = z.object({
  id: VaultIdSchema.optional(),
  customer: CustomerSchema.optional(),
  payment_source: PaymentSourceSchema.optional(),
  links: LinkDescriptionListSchema.optional(),
});

export const PaymentTokenResponseListSchema: z.ZodType<PaymentTokenResponseList> = z.array(PaymentTokenResponseSchema)
  .min(0).max(64);

export const CustomerVaultPaymentTokensResponseSchema: z.ZodType<CustomerVaultPaymentTokensResponse> = z.unknown();

export const SetupTokenResponseSchema: z.ZodType<SetupTokenResponse> = z.object({
  id: VaultIdSchema.optional(),
  customer: z.object({
    id: MerchantPartnerCustomerIdSchema.optional(),
    merchant_customer_id: z.string().regex(new RegExp("^[0-9a-zA-Z-_.^*$@#]+$")).min(1).max(64).optional(),
  }).optional(),
  status: PaymentTokenStatusSchema.optional(),
  payment_source: z.unknown().optional(),
  links: LinkDescriptionListSchema.optional(),
});

/**
 * List all payment tokens
 *
 * Returns all payment tokens for a customer.
 */
export const customerPaymentTokensGet = (config: OpenAPIClientConfig, request: {
  query: {
    /** A unique identifier representing a specific customer in merchant's/partner's system or records. */
    customer_id: string;
    /** A non-negative, non-zero integer indicating the maximum number of results to return at one time. */
    page_size?: number;
    /** A non-negative, non-zero integer representing the page of the results. */
    page?: number;
    /** A boolean indicating total number of items (total_items) and pages (total_pages) are expected to be returned in the response. */
    total_required?: boolean;
  };
}): Promise<
  OpenAPIResponses<{
    /** Successful execution. */
    200: {
      body: CustomerVaultPaymentTokensResponse;
    };
    /** Request is not well-formed, syntactically incorrect, or violates schema. */
    400: {
      body: Error;
    };
    /** Authorization failed due to insufficient permissions. */
    403: {
      body: Error;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v3/vault/payment-tokens",
      method: "get",
      requestSchema: z.object({
        query: z.object({
          customer_id: z.string().regex(new RegExp("^[0-9a-zA-Z_-]+$")).min(7).max(36),
          page_size: z.number().int().min(1).max(5).default(5).optional(),
          page: z.number().int().min(1).max(10).default(1).optional(),
          total_required: z.boolean().optional(),
        }),
      }),
      responseSchema: {
        200: z.object({ body: CustomerVaultPaymentTokensResponseSchema }),
        400: z.object({ body: ErrorSchema }),
        403: z.object({ body: ErrorSchema }),
        500: z.object({ body: ErrorSchema }),
      },
    },
    request,
  ) as any;

/**
 * Create payment token for a given payment source
 *
 * Creates a Payment Token from the given payment source and adds it to the Vault of the associated customer.
 */
export const paymentTokensCreate = (config: OpenAPIClientConfig, request: {
  headers?: {
    /** The server stores keys for 3 hours. */
    "PayPal-Request-Id"?: string;
  };
  body: PaymentTokenRequest;
}): Promise<
  OpenAPIResponses<{
    /** Idempotent response for a successful creation of payment token. */
    200: {
      body: PaymentTokenResponse;
    };
    /** A successful creation of payment token. */
    201: {
      body: PaymentTokenResponse;
    };
    /** Request is not well-formed, syntactically incorrect, or violates schema. */
    400: {
      body: Error;
    };
    /** Authorization failed due to insufficient permissions. */
    403: {
      body: Error;
    };
    /** Request contains reference to resources that do not exist. */
    404: {
      body: Error;
    };
    /** The requested action could not be performed, semantically incorrect, or failed business validation. */
    422: {
      body: Error;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v3/vault/payment-tokens",
      method: "post",
      requestSchema: z.object({
        headers: z.object({
          "PayPal-Request-Id": z.string().regex(new RegExp("^.*$")).min(1).max(10000).optional(),
        }).optional(),
        body: PaymentTokenRequestSchema,
      }),
      responseSchema: {
        200: z.object({ body: PaymentTokenResponseSchema }),
        201: z.object({ body: PaymentTokenResponseSchema }),
        400: z.object({ body: ErrorSchema }),
        403: z.object({ body: ErrorSchema }),
        404: z.object({ body: ErrorSchema }),
        422: z.object({ body: ErrorSchema }),
        500: z.object({ body: ErrorSchema }),
      },
    },
    request,
  ) as any;

/**
 * Retrieve a payment token
 *
 * Returns a readable representation of vaulted payment source associated with the payment token id.
 */
export const paymentTokensGet = (config: OpenAPIClientConfig, request: {
  path: {
    /** ID of the setup token. */
    id: string;
  };
}): Promise<
  OpenAPIResponses<{
    /** Successful execution. */
    200: {
      body: PaymentTokenResponse;
    };
    /** Authorization failed due to insufficient permissions. */
    403: {
      body: Error;
    };
    /** The specified resource does not exist. */
    404: {
      body: Error;
    };
    /** The requested action could not be performed, semantically incorrect, or failed business validation. */
    422: {
      body: Error;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v3/vault/payment-tokens/{id}",
      method: "get",
      requestSchema: z.object({
        path: z.object({
          id: z.string().regex(new RegExp("^[0-9a-zA-Z_-]+$")).min(7).max(36),
        }),
      }),
      responseSchema: {
        200: z.object({ body: PaymentTokenResponseSchema }),
        403: z.object({ body: ErrorSchema }),
        404: z.object({ body: ErrorSchema }),
        422: z.object({ body: ErrorSchema }),
        500: z.object({ body: ErrorSchema }),
      },
    },
    request,
  ) as any;

/**
 * Delete payment token
 *
 * Delete the payment token associated with the payment token id.
 */
export const paymentTokensDelete = (config: OpenAPIClientConfig, request: {
  path: {
    /** ID of the setup token. */
    id: string;
  };
}): Promise<
  OpenAPIResponses<{
    /** The server has successfully executed the method, but there is no entity body to return. */
    204: {
      body: unknown;
    };
    /** Request is not well-formed, syntactically incorrect, or violates schema. */
    400: {
      body: Error;
    };
    /** Authorization failed due to insufficient permissions. */
    403: {
      body: Error;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v3/vault/payment-tokens/{id}",
      method: "delete",
      requestSchema: z.object({
        path: z.object({
          id: z.string().regex(new RegExp("^[0-9a-zA-Z_-]+$")).min(7).max(36),
        }),
      }),
      responseSchema: {
        204: z.object({ body: z.unknown() }),
        400: z.object({ body: ErrorSchema }),
        403: z.object({ body: ErrorSchema }),
        500: z.object({ body: ErrorSchema }),
      },
    },
    request,
  ) as any;

/**
 * Create a setup token
 *
 * Creates a Setup Token from the given payment source and adds it to the Vault of the associated customer.
 */
export const setupTokensCreate = (config: OpenAPIClientConfig, request: {
  headers?: {
    /** The server stores keys for 3 hours. */
    "PayPal-Request-Id"?: string;
  };
  body: SetupTokenRequest;
}): Promise<
  OpenAPIResponses<{
    /** Idempotent response for a successful creation of setup token. */
    200: {
      body: SetupTokenResponse;
    };
    /** A successful creation of setup token. */
    201: {
      body: SetupTokenResponse;
    };
    /** Request is not well-formed, syntactically incorrect, or violates schema. */
    400: {
      body: Error;
    };
    /** Authorization failed due to insufficient permissions. */
    403: {
      body: Error;
    };
    /** The requested action could not be performed, semantically incorrect, or failed business validation. */
    422: {
      body: Error;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v3/vault/setup-tokens",
      method: "post",
      requestSchema: z.object({
        headers: z.object({
          "PayPal-Request-Id": z.string().regex(new RegExp("^.*$")).min(1).max(10000).optional(),
        }).optional(),
        body: SetupTokenRequestSchema,
      }),
      responseSchema: {
        200: z.object({ body: SetupTokenResponseSchema }),
        201: z.object({ body: SetupTokenResponseSchema }),
        400: z.object({ body: ErrorSchema }),
        403: z.object({ body: ErrorSchema }),
        422: z.object({ body: ErrorSchema }),
        500: z.object({ body: ErrorSchema }),
      },
    },
    request,
  ) as any;

/**
 * Retrieve a setup token
 *
 * Returns a readable representation of temporarily vaulted payment source associated with the setup token id.
 */
export const setupTokensGet = (config: OpenAPIClientConfig, request: {
  path: {
    /** ID of the setup token. */
    id: string;
  };
}): Promise<
  OpenAPIResponses<{
    /** Found requested setup-token, returned a payment method associated with the token. */
    200: {
      body: SetupTokenResponse;
    };
    /** Authorization failed due to insufficient permissions. */
    403: {
      body: Error;
    };
    /** The specified resource does not exist. */
    404: {
      body: Error;
    };
    /** The requested action could not be performed, semantically incorrect, or failed business validation. */
    422: {
      body: Error;
    };
    /** An internal server error has occurred. */
    500: {
      body: Error;
    };
  }>
> =>
  openAPIFetch(
    config,
    {
      path: "/v3/vault/setup-tokens/{id}",
      method: "get",
      requestSchema: z.object({
        path: z.object({
          id: z.string().regex(new RegExp("^[0-9a-zA-Z_-]+$")).min(7).max(36),
        }),
      }),
      responseSchema: {
        200: z.object({ body: SetupTokenResponseSchema }),
        403: z.object({ body: ErrorSchema }),
        404: z.object({ body: ErrorSchema }),
        422: z.object({ body: ErrorSchema }),
        500: z.object({ body: ErrorSchema }),
      },
    },
    request,
  ) as any;

export { createCustomClient } from "jsr:@brad-jones/deno-net-open-api-client@0.2.0";

/**
 * Create a client with all available operations.
 * This is convenient but will include all operations in your bundle.
 * For better tree-shaking, use createCustomClient() to select specific operations.
 */
export function createClient(config: OpenAPIClientConfig) {
  return createCustomClient(config, {
    customerPaymentTokensGet,
    paymentTokensCreate,
    paymentTokensGet,
    paymentTokensDelete,
    setupTokensCreate,
    setupTokensGet,
  });
}
